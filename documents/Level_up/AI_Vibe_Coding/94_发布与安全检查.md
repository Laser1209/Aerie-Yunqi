---
title: 发布与安全检查
kind: control
tags: [aerie, release, security]
---
# 发布与安全检查
> [!info] 门禁归属
> 本清单是最终发布门禁，不替代各 Phase 的阶段验收。阶段推进时只复核与当前 Phase 直接相关的条目；完整清单在发布前统一关闭。

- [x] Python 全测、Node 检查、Electron 启动烟测（2026-07-21：World Dashboard snapshot contract 后 `python -m pytest tests -q` → `553 passed, 6 warnings`；`node --test electron/tests/*.test.js` → `25 passed`；Electron 关键入口 `node --check` 通过，且 `node --check electron/src/world-dashboard-host.js`、`node --check electron/src/main.js`、`node --check electron/src/preload.js`、`node --check electron/src/renderer/js/world-dashboard.js`、`python -m py_compile core/api_server.py core/companion.py` 通过；`npm run check:all` 在 `electron` 工作目录通过 emoji/forbidden/token 三段检查；前序 Electron smoke 以临时 `tmp/release-electron-smoke-20260721-033242`、`AERIE_DISABLE_QQ=true`、`AERIE_DISABLE_MODEL_CALLS=true` 启动，`/api/health` 返回临时 `data_path_id`、`components.backend=healthy`、QQ 离线导致预期 `degraded`，结束后 7890 无残留监听；提交 `f291bbd` 后再次复验 `python -m pytest tests -q` → `553 passed, 6 warnings`、`node --test electron/tests/*.test.js` → `25 passed`、`npm run check:all` 通过；新增 history remediation preflight 后 `python -m pytest tests -q` → `555 passed, 6 warnings`；预检工作区内容判断加固后 → `557 passed, 6 warnings`）
- [x] 迁移 dry-run、恢复、旧 API 观测（`91_数据迁移核对.md` 与 `92_回滚演练.md` 已无未勾项；2026-07-21 窄门禁 `tests/test_phase3_conversation_model.py tests/test_phase4_migration.py tests/test_phase4_api.py tests/test_phase4_integration.py::test_task12_flag_rollback_matrix_reenables_queue_without_data_loss tests/test_phase5_event_stream.py ...` → `84 passed, 4 warnings`，覆盖 dry-run 零写入、幂等、部分应用恢复、旧 API/Flag-off legacy 观测）
- [x] Renderer IPC 白名单且无任意 Sidecar 网络能力（Phase 11/15 目标测试随 84 passed 复验；`preload.js` 仅暴露 `worldDashboard` 专用 IPC，`main.js` 无 `world-dashboard:raw`，CapabilityBroker 拒绝 `qq.credential.read`/`shell.execute`，dashboard approval 仅上传 sanitized 字段；2026-07-21 新增实际 renderer dashboard shell 后，`index.html` 有 `world-dashboard` tab/panel/script/style，renderer 只调用 `window.aerie.worldDashboard` 窄 API，候选审批 payload 仅含白名单字段，Creative Workshop 预览不渲染原始 secret 文本；同日新增 `/api/world/candidates/approve` 与 `/api/world/dashboard/snapshot` 后端合同、Companion 手动审批/snapshot handler、`world-dashboard:get-snapshot` 窄 IPC，Flag off 不调用 handler，Flag on 只传/显示白名单字段，handler 通过 WorldPort replay 查 canonical candidate，响应不回显 raw prompt/secret）
- [x] 图片魔数/MIME/尺寸/EXIF/路径穿越检查（`tests/test_upload.py` 与 `tests/test_phase10_image_workflow.py` 随 84 passed/540 passed 复验；覆盖 flag-off legacy、魔数拒绝、pixel bomb、EXIF transpose 后重编码、sha256 去重、owner/audit、路径穿越拒绝、prompt/vision 审核与失败脱敏、引用保护和 GC；2026-07-21 新增显式图片/视觉 OpenAI-compatible provider 接线测试，未配置时保持无外呼 stub，配置时可返回 `image_bytes_b64`/answer 并接入资产持久化）
- [x] 语音/TTS Provider 显式配置边界（2026-07-21：`Brain.speak_text()` 在显式 `AERIE_TTS_*`/`OPENAI_TTS_*` 配置存在时调用 OpenAI-compatible `/audio/speech` 并返回 base64 音频字节；`EnhancedTTSEngine` 的 `TTSProvider.OPENAI` 显式调用 OpenAI-compatible `/audio/speech` 并只写本地音频文件；未显式配置时保持 `voice_tts` stub 或 `no API key`，不落到 Edge 外呼；`tests/test_brain_provider_routing.py`、`tests/test_multimodal_output_tts.py` 随完整 `542 passed` 复验）
- [x] Embedding Provider 显式配置边界（2026-07-21：`Brain.bge_embed()` 在显式 `AERIE_EMBEDDING_*`/`OPENAI_EMBEDDING_*` 配置存在时调用 OpenAI-compatible `/embeddings` 并返回 embeddings；未显式配置 embedding key 时保持 `bge_embedding` stub，即使存在通用 `OPENAI_API_KEY` 也不外呼；`tests/test_brain_provider_routing.py` → `11 passed`，随完整 `542 passed` 复验）
- [x] 日志、Fixture、截图、Evidence 脱敏（Phase 10/11/13/15 测试随 84 passed 复验 secret redaction；Electron smoke 使用临时 DB/data/log、禁用 QQ/模型、未采集截图、未连接真实外部通道；`python tools/scan_provider_key_patterns.py` 覆盖 `documents/logs/tmp/config/core/electron/src/tools/tests` 共 774 个文本文件，结果 `PROVIDER_KEY_SCAN_OK`）
- [x] 疑似凭据未进入文档、提交、历史或日志（2026-07-21：用户确认旧 SiliconFlow/DeepSeek key 已吊销并在本地 `.env` 中换新，随后明确授权本地 history rewrite；`git filter-repo 2.47.0 --sensitive-data-removal` 对 `Aerie`、`Aerie-Model-X`、`main` 三分支完成重写，严格替换已确认的 provider-key 形态值，并从全部历史移除 NapCat 登录二维码/账号配置、运行 DB/WAL/SHM、日志、上传文件、测试产物和 Electron 构建产物；`python tools/scan_provider_key_patterns.py --history` → `PROVIDER_KEY_SCAN_OK`，`history_findings=0`，GitHub `>=100,000,000` bytes blob 数量为 0，高风险历史路径数量为 0；扩展 Gitleaks 复核仅剩 7 条已人工确认误报，来自 vendored NapCat 固定代码标识和 `YOUR_API_KEY` 文档占位符；隔离镜像验证 `python -m pytest tests -q` → `557 passed, 6 warnings`、`node --test electron/tests/*.test.js` → `25 passed`、`npm run check:all` 与 `git fsck --full --strict` 通过；[[95_历史凭据处置方案]] 记录授权、备份、预演、清洗和回滚证据）
- [x] Feature Flag 与回滚步骤可执行（`05_Feature_Flag与回滚矩阵.md`、`91_数据迁移核对.md`、`92_回滚演练.md` 均无未勾项；2026-07-21 Electron smoke 在迁移/身份/persona/conversation/queue/stream/context/image/world 相关 Flag 显式开启下启动成功；84 passed 窄门禁复验 migration flag、queue rollback、stream legacy、image flag-off、world/dashboard flag-off/降级路径）
