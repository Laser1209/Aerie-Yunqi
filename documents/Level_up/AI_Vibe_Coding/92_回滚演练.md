---
title: 回滚演练
kind: control
tags: [aerie, rollback, acceptance]
---
# 回滚演练
1. 关闭对应 Feature Flag。
2. 恢复数据库备份或切回旧读路径。
3. 验证旧 API、聊天、QQ 与桌面继续可用。
4. 保留新表、元数据、Outbox 和旧文件。
5. 记录恢复时间、数据损失与未完成任务。

## Phase 00：安全基线、迁移器与标识合同
- [x] `migration_framework_v1=false` 时不创建 `migration_ledger`，旧 `chat_log` Schema 继续可用。
- [x] 使用临时脱敏数据库完成在线备份与实际恢复，恢复后 `chat_log=1/1`、`quick_check=ok`、数据损失 0。
- [x] 演练总耗时 `0.060643s`；未修改生产库，未删除新表、元数据或旧文件。
- [x] 当前 Phase 0 专项 `14 passed`，完整 Python 回归 `353 passed, 6 warnings`。
- [x] `rollback_ready=true`；Phase 00 重新审计门禁通过。

## Phase 01：主动消息 P0
- [x] 将 `proactive_delivery_v2` 设为 `false` 时恢复 QQ-only 投递。
- [x] 旧路径不写主动桌面气泡、不发送 `assistant`、`proactive_message` 或 `proactive_delivery` 事件。
- [x] QQ 断线恢复旧 Scheduler 暂停行为；V2 开启时 QQ 断线不阻断本地气泡和系统通知。
- [x] Trigger、Event Engine 生命周期、Desire 属性和 YAML 去重修复不随 Flag 回滚，避免重新引入 P0 断点。
- [x] 2026-07-20 重新审计：专项 `19 passed`，Phase 0/1/API/Pipeline 定向 `83 passed`，完整 Python `353 passed`，Electron 四入口语法检查通过。
- [x] 自动化证据位于 [[Task 01-baseline]]，未记录消息正文、账号或凭据；`rollback_ready=true`。

## Phase 02：Actor、Channel 与 Persona 真源
- [x] `identity_contract_v1=false` 时 IdentityResolver 不写 `actor_id`，聊天与长期记忆继续按 legacy `user_id` 寻址。
- [x] 关闭 Flag 后保留 `actors`、`channel_accounts`、可空身份列和新数据，不执行破坏性回滚。
- [x] 旧聊天和长期记忆的规范身份列保持 `NULL`，不猜测历史来源。
- [x] `persona_hub_source_v1=false` 时恢复旧 `persona.yaml` 读写适配路径；Hub JSON 和 active 指针保留。
- [x] Persona JSON 与 `_active.json` 使用原子替换；写盘失败时内存状态不提前切换。
- [x] 2026-07-20 再次审计：Phase 2 身份与 Persona 专项 `41 passed, 4 warnings`；Phase 0–2/API/Pipeline 定向 `124 passed, 4 warnings`；完整 Python `353 passed, 6 warnings`。
- [x] Electron Persona Hub `3 passed`，`persona-hub.js` 语法检查通过；错误的 `npm test` 调用因项目无该脚本被废弃，不计为产品失败。
- [x] 当前源码 `core/` 中静态 `emotion.get_state(0)` / `emotion.update(0)` 门禁无匹配；构建产物中的旧副本不作为源码修改对象。
- [x] 自动化重新验证 Actor Emotion 生命周期、迁移框架兼容、身份/Persona Flag 关闭路径和完整回归。
- [x] 剩余 `user_id=0` 已分类为全局/系统/legacy 边界；无 Actor 来源的旧 Evolution 记录不做猜测式回填。

## Phase 04：持久 Request 队列

- [x] 自动化验证 `migration_framework_v1=false` 时不运行 006；即使 `chat_request_queue_v1=true` 也不会静默创建版本化规范表。
- [x] 自动化验证 `chat_request_queue_v1=false` 不阻止 006 在迁移框架开启时预建兼容 Schema；新字段和历史 completed 行被保留。
- [x] 006 dry-run、幂等、部分应用恢复与 `quick_check=ok` 已通过；专项 `9 passed`，完整 Python `362 passed, 6 warnings`。
- [x] `chat_request_queue_v1=false` 时旧 `/api/chat/send` 同步 200 与空消息 400 已由 Task 9 API 回归验证；Companion 不启动 Worker，既有 queued 行保持 queued。
- [x] Repository 层不读取 queue Flag、不启动 Worker；关闭运行 Flag 不会删除已建立的 queued/终态数据，Task 9 已验证旧入口和 Flag-off 停止消费，Task 11 已验证端到端状态恢复与 Electron smoke。
- [x] ChatRequestService 为未接线新增层，未修改旧同步 `/api/chat/send`、Pipeline、QQ 或 Renderer；Task 6 回归确认现有路径保持不变。
- [x] Task 7 独立 Worker stop 已验证先停止 claim，再将本 Worker 持有的 running/cancelling 收口为 `failed/worker_stopped`，同 Conversation 后续 queued 保持 queued；Task 9 已将 Worker 接入 Feature Flag 和组合根，且验证启动早于 QQ readiness wait。
- [x] Task 8 Pipeline 只扩展兼容入口与队列路径取消合同；旧 `Pipeline.handle(msg, force_full=True)` 回归 `36 passed`，完整 `tests` 收集 `447 passed`。Task 9 已接入 Companion/API，但 Renderer 仍未接入请求级状态。
- [x] Task 9 验证 queue Flag 开启时 `/api/chat/send` 返回 202 queued 且不调用 Pipeline；依赖缺失 fail closed/503；新增 status/cancel/retry 端点稳定映射 404/409/503/400；API 不实例化第二 Repository。
- [x] Task 10 Renderer 去除发送入口全局 `_loading`，改为请求级状态 Map、统一 ingest、event_id 去重、sequence 缓冲和 GET status 恢复；SSE 断线不伪造 failed。旧 chat role guard 保留，避免主动事件误渲染。Electron Node `13 passed`，完整 Python `462 passed`。
- [x] Task 11 端到端集成验证提交、claim、Pipeline completed/status/events、同 Conversation 串行、四路并发、cancel/retry 副作用守恒、启动恢复和事件失败后 GET status 恢复；Electron smoke 使用临时 `E:\Agent_reply\tmp\aerie-phase4-smoke3.db`、`AERIE_DISABLE_QQ=true`、`AERIE_DISABLE_MODEL_CALLS=true`，三次真实 `/api/chat/send` 均 202 并最终 completed，临时 DB 守恒 `requests=6`、`turns=6`、`messages=18`、`chat_log=18`；未连接真实 QQ，未调用真实模型，未写生产库。关联 Python `188 passed`、Electron Node `14 passed`、完整显式 `tests` 收集 `471 passed`。
- [x] Task 12 使用 SQLite Backup API 完成生产一致性快照 A、rehearsal B 与 restore C，源主文件 SHA 前后不变；当前源库已是 006 completed，因此 B 上 dry-run/首次/二次 pending 均为 `[]`，schema/count 不变；A/C 关键表计数和脱敏摘要一致，数据损失 0，`foreign_key_check=0`，`quick_check=ok`。
- [x] Task 12 Flag 回滚矩阵自动化通过：queue on 完成提交，queue off 恢复旧同步 200 且不消费既有 queued，依赖缺失 + queue true 返回 503，重新开启且依赖完整后 queued 继续完成。
- [x] Phase 04 `rollback_ready=true`；Phase 00–04 关联门禁 `259 passed`，完整显式 `tests` 收集 `472 passed`，Electron Node `14 passed`，四个 `node --check` 通过。

## Phase 05：事件统一、SSE 恢复与 Renderer 去重

- [x] `chat_stream_v1=false` 时 `/api/events/stream` 不读取或传递 `Last-Event-ID`，以无参数方式调用旧 `event_stream_generator()`。
- [x] legacy `stream()` 默认保持旧 `data: ...\n\n` 帧，不 replay、不输出 `id:`；新增 replay 和 `id:` 仅由 Flag-on API 路径显式启用。
- [x] `chat_stream_v1=true` 时 API 读取 `Last-Event-ID`、`last_event_id` 或 `lastEventId` cursor，后端从 bounded recovery window replay cursor 后事件；未知 cursor replay 整个 bounded window，由 Renderer event_id 去重兜底。
- [x] Electron 主进程解析 SSE `id:` 和 payload `event_id`，保存 webContents 级 cursor，重连时发送 `Last-Event-ID`；unsubscribe/window destroy 不触发误重连，window destroy 清理 cursor。
- [x] 本阶段无数据库迁移、无生产库写入、无 ACK/Outbox/Sidecar 可靠协议语义；关闭 Flag 不破坏已有新数据。
- [x] 验证：后端专项 `5 passed`；Electron Node `16 passed`；Phase 00–05 显式门禁 `264 passed, 4 warnings`；完整 `tests` 收集 `477 passed, 6 warnings`；`py_compile`、`node --check` 与 `git diff --check` 收口。
- [x] `rollback_ready=true`。

## Phase 06：完整 Turn Context、Token Budget、摘要与长期记忆

- [x] `context_budget_v1=false` 时 Pipeline 保持旧 ContextBuilder kwargs，不传 `context_budget_enabled/actor_id/channel/channel_account_id`，也不读取 `get_last_context_audit()`。
- [x] `context_budget_v1=true` 时 Pipeline 将已解析的 actor/channel identity 传入 ContextBuilder，ContextBuilder 只从当前 Companion 已接线的轻量长期记忆与知识库接口检索，不创建第二套 memory 系统。
- [x] assistant 连续多气泡历史在 context budget path 合并为完整 assistant 响应；短期历史仍由 ConversationRepository 或 legacy actor/channel SQL 负责隔离，不跨 Channel。
- [x] cognition context audit 仅记录 allowlist 后的计数、标识、字符数、估算 token 和是否截断；不记录消息正文、记忆正文、知识正文或凭据。
- [x] 本阶段无数据库迁移、无生产库写入、无新表/回填/摘要持久化；关闭 Flag 不破坏已有数据。
- [x] 验证：Phase 06 专项 `4 passed`；关联 `66 passed`；Phase 00–06 显式门禁 `289 passed, 4 warnings`；完整 `tests` 收集 `481 passed, 6 warnings`；Electron Node `16 passed`；`py_compile`、`node --check` 与 `git diff --check` 收口。
- [x] `rollback_ready=true`。

## Phase 03：Conversation / Turn / Message / Request
- [x] 自动化验证 `conversation_model_v1=false` 时 Pipeline 不调用规范历史读取或 Turn 镜像入口。
- [x] Flag 关闭后继续使用 legacy `chat_log`，并保持 FULL `LIMIT 20`、BASIC `LIMIT 10` 及原查询参数合同。
- [x] 规范历史读取异常时回退 legacy，旧回复路径继续可用。
- [x] Flag 开启时 FULL/BASIC 均按完整 Turn 读取历史，助手多分段不会从中间截断。
- [x] 新表与规范数据不因关闭 Flag 被删除；仓储禁用态写入为 no-op。
- [x] 规范 Turn 写入中途失败时 SAVEPOINT 整体回滚，不遗留孤立 Conversation、Turn、Request 或 Message。
- [x] 使用临时真实 `Database` 构造真实 Companion，验证 Flag true/false 会将同一个启用/禁用 Repository 注入 Pipeline，且未启动后台任务。
- [x] 005 第二批被故障中断时 Ledger 保留最后成功 cursor；移除故障后恢复运行只补剩余行，并保持跨批 Turn/sequence 连续。
- [x] 已从默认生产库生成 SQLite 在线一致性备份与独立 rehearsal 副本；副本迁移后守恒、幂等和 quick check 均通过，源库主文件哈希未变。
- [x] 用户授权后完成生产运行态 Feature Flag true/false 两态演练；真实 Companion 均正常启动并停止，恢复旧路径耗时 `4.189s`。
- [x] 使用 SQLite Backup API 从一致性备份实际恢复生产库，恢复耗时 `0.047860s`；`quick_check=ok`，关键表摘要一致，数据损失 0。
- [x] 2026-07-20 前置门禁修复后重新复验：Phase 3/API/Pipeline `67 passed`，Phase 0–3/API/Pipeline `141 passed`，完整 Python仍为 `353 passed`；未再次触碰生产库。
- [x] `rollback_ready=true`；Phase 03 回滚门禁已重新通过。
