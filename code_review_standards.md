# Aerie · 云栖 后端代码审查标准与流程

> 适用范围：Python 核心后端（`core/`、`main.py`、`config/`、`communication/` 等）。
> 目标：在承认「代码质量参差」的现状下，建立可落地的、分级的、可追溯的审查机制。

---

## 一、审查分级（每条评论必须带级别）

| 级别 | 含义 | 处理要求 |
|------|------|----------|
| 🔴 阻塞（Blocker） | 安全漏洞、数据丢失/损坏、竞态、破坏契约、关键路径无错误处理 | **必须修复后才可合并** |
| 🟡 建议（Should-fix） | 缺失输入校验、逻辑不清、缺测试、性能问题（N+1、重复分配）、明显重复 | 应修复；可协商 |
| 💭 提示（Nit） | 风格、命名、文档缺口、可选优化 | 可选，不阻塞 |

---

## 二、审查维度（逐条对照）

1. **正确性** — 是否实现预期行为？边界条件、返回值、异常分支是否完整？
2. **安全性** — 注入（SQL/命令/Prompt）、密钥处理、权限校验、隐私数据落盘。
3. **可维护性** — 命名、函数长度、模块边界、是否可复用；README/注释是否反映真实行为。
4. **性能** — 高频路径的 N+1、重复 IO、无必要的全局锁、大对象分配。
5. **测试** — 关键路径是否有测试；复杂逻辑是否有断言与回归覆盖。

---

## 三、本项目的高频风险点（已实测基线）

对 `core/` 全量扫描得到：

- **0 个** 项目内测试文件（仅在 `.venv` 有第三方库测试）→ 缺测试是首要治理项。
- **1093 处** `except Exception`；其中 **13 处** `except Exception: pass` 直接吞错（数据/错误被静默丢弃）。
- **178 处** `TODO / FIXME / HACK` 标记 → 技术债集中，需登记跟踪。
- **10 处** `print()` 散落核心逻辑（应使用 `logging`）。

**结论**：当前最大的质量风险不是单点 bug，而是「错误被静默吞掉 + 无测试兜底」，导致故障难被发现、难复现。

---

## 四、审查流程（三阶段）

### 阶段 1 · 提交前自检（作者）
- 运行 `ruff`/`flake8` 或 `pylint`（建议 `ruff`，快且可自动修）。
- 新增/修改的逻辑必须带单测：`pytest` + `pytest-asyncio`（本项目大量 `async`）。
- 自查清单：是否吞异常？是否拼 SQL？是否记录错误日志（`logger.exception`）？

### 阶段 2 · 同行评审（Reviewer）
- 使用本标准的分级清单逐文件过。
- 对 🔴 必须给出具体行号 + 修复示例；对 🟡 至少给出方向。
- 一次性给出完整反馈，不逐轮碎片化。

### 阶段 3 · 合并后守护（CI / 自动化）
- CI 门禁：`lint` 不过禁止合并；测试覆盖率低于阈值（建议 ≥ 60% 起步）标记 🟡。
- 定期（每周）跑一次「全局异味扫描」（异常吞掉、TODO、超大函数），生成报告。

---

## 五、针对本项目的专项审查重点

- **数据库层**（`database.py`）：禁止字符串拼接表名/列名；`execute()` 返回 cursor 前需先取数据。
- **Prompt 注入防御**（`prompt_injection.py`）：正则必须可单测；任何新增规则附带对抗样例。
- **密钥与隐私**（`windows_dpapi.py`、`ilink_credentials.py`）：密钥只走 DPAPI/环境变量，禁止明文落盘或进日志。
- **异步与并发**：SQLite 单锁串行化 + autocommit，确认高并发下不退化；`asyncio.create_task` 的协程泄漏排查。

---

## 六、落地建议（可执行）

1. 先把 `database.py`、`prompt_injection.py` 补齐单测（风险最高）。
2. 接入 pre-commit：`ruff --fix` + `pytest`。
3. 将本清单存入项目 `.workbuddy` 或 `docs/`，作为每次 PR 的固定模板。
4. 每周用「全局异味扫描」跟踪 1093→目标收敛。

---
*本标准为 CodeReviewExpert 依据当前代码库实测现状制定，可随演进迭代。*
