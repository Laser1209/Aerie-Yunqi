---
title: 全局验收清单
kind: control
tags: [aerie, acceptance]
---
# 全局验收清单
- [x] 数据回填守恒、幂等、可续跑
- [x] 桌面/QQ 短期隔离，Actor 长期记忆共享
- [x] 连续三条输入全部持久 queued；同 Conversation 串行、跨 Conversation 默认最多四路（Task 7 Worker 已验证同会话 active=1、跨会话 active=4 且第五等待；Task 9 API 已验证 Flag 开 202 queued 且不等待 Pipeline；Task 10 Renderer 已验证三次快速 send 发出三次 POST；Task 11 端到端与 Electron smoke 已验证连续三次真实 202、同会话顺序完成和四路/第五等待；Task 12 完成生产副本/回滚收口）
- [x] queued/running 真实取消；retry 新建 Request/Turn；重启与 lease 过期运行项转 failed 且不自动重排（Task 4/6/7 已验证 Repository retry/recovery、过期 lease 不复活、lease_lost 停止 execution、queued 不执行 Pipeline、running 真实 Task 取消、stop 与丢 Task 收口；Task 8 已验证 Pipeline 模型/legacy/canonical/event/QQ 边界和过晚取消 fail-closed；Task 11 已验证 cancel/retry/recovery 端到端副作用计数；Task 12 Flag 回滚矩阵通过）
- [x] Request/Turn/Message 状态守恒，纯附件内部中性指令不污染用户可见历史（Task 5 状态守恒、Task 6 Service 输入分离、Task 8 Pipeline visible/effective content 隔离与 canonical ID 复用已通过；Task 9 API 已验证纯附件 202 与脱敏 status DTO；Task 10 Renderer 保持纯附件 send 合同；Task 11 临时 DB smoke 验证 legacy/canonical 计数守恒；Task 12 A/B/C 生产副本关键表计数和脱敏摘要守恒，数据损失 0）
- [x] event_id 去重、request_id + sequence 有序、IPC/SSE/poll 与断线状态恢复不重复（Task 8 已验证 Pipeline 消息事件完整 ID 与 request-scoped sequence 单调；Task 10 已验证 Renderer event_id 去重、sequence 缓冲、IPC/SSE/poll 统一 ingest 与 SSE 断线后 GET status 恢复；Task 11 已验证 terminal event sequence 与事件失败后 GET status 恢复；Task 12 回归收口；Phase 05 已验证 SSE `id:`、bounded replay window、`Last-Event-ID` cursor reconnect、Flag-off legacy path 与 Electron cursor forwarding，完整 `tests` 收集 `477 passed`、Electron Node `16 passed`）
- [x] 完整 Turn Context、Token Budget、长期记忆与知识库注入可审计且可回滚（Phase 06 已验证 `context_budget_v1=true` 时 actor-scoped 长期记忆检索、知识库检索、assistant 多气泡合并为完整回复、Pipeline identity/audit 接线；Flag off 不传新 kwargs、不读取 audit；完整 `tests` 收集 `481 passed`、Electron Node `16 passed`）
- [x] Typing、首 delta、多气泡、打断、reduced-motion（Phase 07 已验证 Renderer typing bubble、request rebinding、reduced-motion fallback、`chat_stream_v1` 关闭路径；2026-07-21 TTS provider hardening 增补显式 `AERIE_TTS_*`/`OPENAI_TTS_*` OpenAI-compatible `/audio/speech` 调用，未显式配置时保持 stub 且不外呼；`EnhancedTTSEngine` 的 `TTSProvider.OPENAI` 可写本地音频文件且无显式 key 时不落到 Edge TTS；完整 Python `540 passed, 6 warnings`）
- [x] Cron、Desire、Idle、World Candidate 统一 Judge（Phase 01 已验证 Companion 绑定 ProactiveJudge、读取 `companion.desire`、idle/threshold hooks 统一走 `PushScheduler.trigger`，force 场景显式绕过 Judge；Phase 08 已验证反馈/延后策略持久化；Phase 14 已验证 World ImageCandidate 复用 ProactiveJudge，muted/expired/judge-rejected 终态 ACK 且无 workflow 副作用，approved 路径幂等执行 image workflow + ACK；本批 targeted regression `77 passed`）
- [x] 图片魔数、EXIF 清理、哈希、所有权、引用、审核、GC（Upload/Phase 10 已验证 `image_assets_v1` flag-off legacy、图片魔数拒绝、pixel bomb 限制、EXIF transpose 后重编码清理、`sha256` 去重、`owner_id`/audit 记录、安全路径引用、prompt/vision 审核与 provider 失败脱敏、引用保护和 orphan GC；2026-07-21 Provider hardening 增补显式 `AERIE_IMAGE_*`/`OPENAI_IMAGE_*` 与 `AERIE_VISION_*`/`OPENAI_VISION_*` OpenAI-compatible 调用，未显式配置时保持 stub 且不外呼，base64 图片字节可进入资产持久化链；本批 targeted regression `36 passed, 4 warnings`，完整 Python `536 passed, 6 warnings`）
- [x] 世界确定性重放、单库所有权、Sidecar 降级（Phase 12 已验证 deterministic seed/snapshot/replay、ActionRegistry、RelationshipEngine、SelfModel 与 Context audit 边界；Phase 13 已验证 world sidecar 独占 `world.db`、Outbox 幂等、ACK cursor、heartbeat/checkpoint 脱敏、RemoteWorldAdapter 降级和 Electron supervisor crash-loop 脱敏；本批 targeted regression `77 passed`）
- [x] Renderer 无通用 Sidecar 网络能力；疑似凭据零泄漏（Phase 11 已验证 WorldPort 只暴露五接口合同、CapabilityBroker 白名单拒绝 `qq.credential.read`/`shell.execute` 并只记录 metadata keys/digest；Phase 15 已验证 preload/main 仅暴露 `worldDashboard` 专用 IPC，无 `world-dashboard:raw` 通道，candidate approval 只上传 sanitized 字段，hidden/degraded 状态和 supervisor/backend 错误不泄漏 secret；本批 targeted regression `77 passed`）
