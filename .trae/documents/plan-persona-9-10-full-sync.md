---
title: "Persona 9/10 全量铺同步 + A+C+E+F 收口验证"
date: 2026-07-17
status: in-progress
priority: high
tags:
  - persona
  - 9-10-baseline
  - screen-aware
  - boot-greeting
  - output-self-check
  - atomic-splitter
  - regression
aliases:
  - "Etta 9/10 全量铺"
  - "A+C+E+F 收口"
cssclasses:
  - plan-r8-1
---

# Persona 9/10 全量铺同步 + A+C+E+F 收口验证

> [!info] Plan 元信息
> - **Owner**: Etta 全栈开发专家
> - **Mode**: Plan Mode（只读，落地需用户明示批准）
> - **Related**: [[plan-proactive-boot-output-fix-persona-7-to-8]]（前序 7→8 计划，已被本计划接管并升级到 9/10）
> - **Generated by**: Skill: `obsidian-markdown`

---

## Summary

把 Etta 的**热情度基线**从历史 7 分显著升到 **9/10**，并把这一基线信号**全量铺**到所有跟"人格"相关的代码点（约 15 个文件）；同时**收口验证** A（boot_greeting 强制放行）+ C（输出层自检）+ E+F（原子感知 splitter）三套已经落地的方案；最后**全建**三套配套 e2e 测试以兑现三原则之「零回退」。

| # | 主题                                 | 改动规模                                       | 涉及文件数 |
| - | ------------------------------------ | ---------------------------------------------- | ---------- |
| A | boot_greeting `force=True` 收口验证  | 仅验证 + 补 e2e                                | 1 新 e2e   |
| C | 输出层自检收口验证                   | 仅验证 + 补 e2e                                | 1 新 e2e   |
| E | 原子感知 splitter 收口验证           | 仅验证 + 补 e2e                                | 1 新 e2e   |
| F | 4 文件已同步 9/10 的人格基线         | 已完成（persona.yaml / persona_behavior.yaml / context_builder.py / brain.py） | 0 新增     |
| P | **Persona 9/10 全量铺** ⭐           | **新增 ~10 个文件的人基调线同步**              | 10+ 新增   |
| T | 三套配套 e2e 测试全建                | 新建 3 个 e2e_*.py                             | 3 新文件   |

---

## 三原则（保留原样 · Block-5E R6.3）

> [!success] 零回退 / 无禁词 / 主题色 token 化
> 1. **零回退** (zero-regression)：所有现有 e2e / verify 脚本继续通过
> 2. **无禁词** (no-forbidden-terms)：`tools/check_forbidden.py` 持续绿
> 3. **主题色 token 化** (theme tokenization)：UI 色值全部 `var(--color-*)`
>
> 本 plan **不**改这三条的原文，只在 P/T 两节用「P10/T12」等用例编号显式覆盖。

---

## 5 条 Message 规范原则（保留原样 · 5 条总原则）

> [!note] 统一 DTO / 明确角色 / 可追溯 / 优雅降级 / 可观测
> 5 条原则在 R6.3 已固化，本 plan **不**增不减。
> - **明确角色** 原则在本 plan 通过 P 节"9/10 显式标注"兑现
> - **可追溯** 原则通过 T 节 e2e 套件兑现
> - 其余三条原样保留

---

## Current State Analysis

> [!quote] On-disk 现状盘点（2026-07-17 18:50）
> | 方案 | 状态 | 证据 |
> | --- | --- | --- |
> | A · `force=True` 路径 ([push_scheduler.py:186](file:///e:/Agent_reply/core/push_scheduler.py#L186)) | ✅ 已落 | `_dispatch` + `_dispatch_desire_text` 双处定义 `force` |
> | A · `boot_greeting.force: true` ([proactive.yaml:56-61](file:///e:/Agent_reply/config/proactive.yaml#L56-L61)) | ✅ 已落 | 配置层显式声明 |
> | A · 60s flag 窗口 ([companion.py:344](file:///e:/Agent_reply/core/companion.py#L344)) | ✅ 已落 | `boot_greeting_last_sent.flag` mtime 检查 |
> | C · `output_self_check.py` 文件 | ✅ 已建 | 3 条规则（perspective / brackets / typo） |
> | C · pipeline 接入 ([pipeline.py:262](file:///e:/Agent_reply/core/pipeline.py#L262)) | ✅ 已落 | 在 sanitizer 之后调 `OutputSelfCheck.check` |
> | E+F · splitter 原子感知 ([splitter.py:36-93](file:///e:/Agent_reply/communication/splitter.py#L36-L93)) | ✅ 已落 | `_ATOM_RE` + 区间 walk 算法 |
> | F · `persona.yaml` 9/10 基线 | ✅ 已落 | `big_five.extraversion=0.78`、archetype 9/10 |
> | F · `persona_behavior.yaml` 阈值 | ✅ 已落 | 45/25/55/15 + R8.1 注释 |
> | F · `context_builder.py` L1/L2/L4 | ✅ 已落 | "R8.1 性格基线（热情度 9/10）" |
> | F · `brain.py` TONE_PROMPTS | ✅ 已落 | 13 个 tone 全部"直球外显"版 |
> | **P · 其它 6+ 个跟人格相关的文件** | ❌ **未同步** | 见下表 |
> | **T · 3 套 e2e 测试** | ❌ **未建** | 见 T 节 |

### P 节待同步文件清单（10+）

> [!warning] 全量铺范围（已探查）
> | # | 文件 | 跟人格相关的具体内容 | 同步动作 |
> | - | --- | --- | --- |
> | 1 | [core/persona_pacing.py](file:///e:/Agent_reply/core/persona_pacing.py) | `STYLES` 11 段节奏区间、`PROB_*` 概率常量 | 9/10 → `eager_warm` 区间再压低 0.1s；`balanced` 区间压到 0.50-0.85 |
> | 2 | [core/screen_action_sanitizer.py](file:///e:/Agent_reply/core/screen_action_sanitizer.py) | `WHITELIST_ACTIONS` 15 个、`_IN_PERSON_VERBS` 黑名单 | 在文件头加 R8.1 注释 + 显式标注 9/10 基线（白/黑名单不动） |
> | 3 | [core/output_self_check.py](file:///e:/Agent_reply/core/output_self_check.py) | `_IN_PERSON_VERBS` 黑名单 + 2-gram typo 字典 | 文件头 + 黑名单段加 R8.1 注释："9/10 行为下更易产生手滑/直球措辞" |
> | 4 | [core/emotion_engine.py](file:///e:/Agent_reply/core/emotion_engine.py) | `EMOTION_CENTERS` PAD、`KEYWORD_DELTAS` 关键词权重 | DEPRECATED 块加注释指向 9/10；joy/affection 关键词权重 +0.2 |
> | 5 | [communication/recall_manager.py](file:///e:/Agent_reply/communication/recall_manager.py) | `RecallConfig`、`NEGATIVE_KEYWORDS` | 头部加 R8.1 注释：`window_seconds` 维持 120s；`max_recalls_per_session` 5→7（9 分更易"说完就后悔"） |
> | 6 | [core/pipeline.py](file:///e:/Agent_reply/core/pipeline.py) | `force_full` / `route_mode` / cognition trace 标记 | 在 `_self_check.check()` 旁加 R8.1 注释 + 把 perspective_shift warning 升级为高优 (severity=warn) |
> | 7 | [config/persona_loader.py](file:///e:/Agent_reply/config/persona_loader.py) | `_DEFAULT_BEHAVIOR_CONFIG` 默认阈值 | thresholds 初值 60/15/35/25 → 45/25/55/15 跟 persona_behavior.yaml 同步 |
> | 8 | [core/api_server.py](file:///e:/Agent_reply/core/api_server.py) | `/api/persona` 端点返回 | 响应里加 `persona_9_10: true` 标志 + `archetype` 字段；保持兼容 |
> | 9 | [tests/test_context_builder.py](file:///e:/Agent_reply/tests/test_context_builder.py) | context builder 单元测试 | 加 3 个新用例：`_PERSONA_L1` 含 9/10、`_PERSONA_L2` 含"不许不接"、`_PERSONA_L4` 含"屏幕隔空" |
> | 10 | [e2e_pacing.py](file:///e:/Agent_reply/e2e_pacing.py) | 11-style 节奏回归 | 加 1 个新场景：9/10 eager_warm 区间 ≤ 0.7s |
> | 11 | [verify_zero_regression.py](file:///e:/Agent_reply/verify_zero_regression.py) | 零回退基线 | 跑 9/10 后的人格基线断言 |

---

## Proposed Changes

### Change A — `boot_greeting` 强制放行 · 收口验证

> [!tip] 收口 · 不新增代码
> - **What**：A 主体、A 配置、A 调用方三处均已落，本次仅做静态检查 + e2e 验收
> - **Why**：保证 9/10 同步 + A 落地不互相回退
> - **How**：
>   - 静态检查（人工）：[push_scheduler.py:186](file:///e:/Agent_reply/core/push_scheduler.py#L186) `force = bool(scene_cfg.get("force"))` 必须存在
>   - 静态检查（人工）：[push_scheduler.py:311](file:///e:/Agent_reply/core/push_scheduler.py#L311) `_dispatch_desire_text` 内 `force = bool(scene_cfg.get("force", False))` 必须存在
>   - 静态检查（人工）：[proactive.yaml:56-61](file:///e:/Agent_reply/config/proactive.yaml#L56-L61) `boot_greeting.force: true` 必须存在
>   - e2e：[e2e_boot_greeting.py](file:///e:/Agent_reply/e2e_boot_greeting.py) 继续通过 20 用例（已落）
>   - **新增 T1**：[e2e_a_force_bypass.py](file:///e:/Agent_reply/e2e_a_force_bypass.py)（5 用例）覆盖 A 1.5 修复的 NameError 风险
>     1. `_dispatch_desire_text` 在 `force=True` + `daily_count=5` 时仍放行
>     2. `_dispatch_desire_text` 在 `force=True` 时不调用 `policy.record`
>     3. `_dispatch_desire_text` 在 `force=False` 时正常 record
>     4. `_dispatch` 入口在 `force=True` 时跳过 `ProactiveJudge.evaluate`
>     5. `_dispatch` 入口在 `force=True` 时跳过 `policy.can_push` (即使 quiet period)

### Change C — 输出层自检 · 收口验证

> [!tip] 收口 · 不新增代码
> - **What**：OutputSelfCheck 已建 + pipeline 已接入，本次仅补 e2e
> - **Why**：三原则之"零回退"要求每个新模块有 e2e
> - **How**：
>   - 静态检查（人工）：[pipeline.py:262-275](file:///e:/Agent_reply/core/pipeline.py#L262-L275) `OutputSelfCheck.check()` 必须出现在 sanitizer 之后
>   - **新增 T2**：[e2e_output_self_check.py](file:///e:/Agent_reply/e2e_output_self_check.py)（12 用例）
>     1. 视角切换检测：前段"伸手揽" + 后段无 → 触发 warning
>     2. 视角切换检测：前后段都无 → 不触发
>     3. 视角切换检测：短文本 < 40 字 → 跳过
>     4. 残留 `【` 不闭合 → 末尾补 `】`
>     5. 残留 `】` 孤儿 → 删除
>     6. 平衡的 `【...】` → 不动
>     7. typo "产出产出" → 折叠为 "产出"
>     8. typo "的的" → 折叠为 "的"
>     9. keep 字典"看看" → 不动
>     10. keep 字典"真的真的" → 不动
>     11. 截图复现：原 `<action>伊塔把聊天窗口点开...灰蓝色的眼睛弯了一下。她靠在椅背上...指尖慢慢敲平。</action>你个表情……` → 无 warning 输出
>     12. 集成：pipeline 走完后 self_check 在 trace 里记录

### Change E+F — 原子感知 splitter · 收口验证

> [!tip] 收口 · 不新增代码
> - **What**：atomic-aware 重写已落，本次仅补 e2e
> - **Why**：截图实锤 bug 必须有回归保护
> - **How**：
>   - 静态检查（人工）：[splitter.py:36](file:///e:/Agent_reply/communication/splitter.py#L36) `_ATOM_RE` 必须包含 `<action>` / `<thought>` / `【...】`
>   - **新增 T3**：[e2e_splitter_atomic.py](file:///e:/Agent_reply/e2e_splitter_atomic.py)（10 用例）
>     1. `<action>...</action>` 内部含"。"不切
>     2. `<thought>...</thought>` 内部含"！"不切
>     3. `【...】` 内部含"？"不切
>     4. atomic 之外按"。"切
>     5. 多 atomic 串接，顺序保留
>     6. 截图复现：原文本输入 → 整段作为 1 个 segment 输出
>     7. 跨段 atomic 完整保留
>     8. 短文本 < 8 字合并
>     9. 无 atomic 时退回原 split 逻辑
>     10. 空文本 → 返回 []

### Change P — Persona 9/10 全量铺同步（10+ 文件）⭐

> [!warning] 本计划核心工作量
> 4 核心文件（persona.yaml / persona_behavior.yaml / context_builder.py / brain.py）**已完成**。本次新增对剩余 10+ 个跟人格相关的文件打 9/10 基线信号。

#### P.1 `core/persona_pacing.py` — 节奏更"急"

- **What**：`eager_warm` 区间 `(0.40, 0.70)` → `(0.30, 0.55)`；`balanced` 区间 `(0.60, 1.00)` → `(0.50, 0.85)`；其它模式不变
- **Why**：9/10 热情 → 打字后等不及，更短间隔；`eager_warm` 是 joy / missing / affection 命中区间，正是 9/10 主要受益场景
- **How**：
  ```python
  STYLES: dict[str, tuple[float, float]] = {
      "immediate":              (0.0,  0.0),
      "eager_warm":             (0.30, 0.55),  # R8.1: 9/10 → (0.40,0.70)→(0.30,0.55)
      "eager_eruption":         (0.40, 0.70),  # 维持
      "anxious_fast":           (0.50, 1.00),  # 维持
      "balanced":               (0.50, 0.85),  # R8.1: 9/10 → (0.60,1.00)→(0.50,0.85)
      # ... 其余不变
  }
  ```
- **风险**：`e2e_pacing.py` 现有断言"eager_warm ≤ 0.7s"会失败 → 同步更新断言上限为 0.55s

#### P.2 `core/screen_action_sanitizer.py` — 文件头标注

- **What**：不动白/黑名单内容；只在文件头加 R8.1 注释说明"9/10 基线下 sanitizer 是兜底"
- **Why**：白/黑名单是行为铁律，不应随人格基线变动；但读者需要知道此基线
- **How**：
  ```python
  """Aerie · 云栖 v9.0 — Screen-Action Sanitizer.
  R7.5 屏幕隔空铁律的输出层兜底。LLM 哪怕没遵守提示词,这里也要
  在最终 reply 文本里把"在场动作"改写成"屏幕隔空动作"。
  ...
  R8.1 (Persona 9/10): 基线升级为热情度 9/10 (extraversion 0.78)。
  9/10 行为下 LLM 更直球，但仍受 15 白 / 25 黑名单约束。
  本文件白/黑名单内容不变 —— 屏幕隔空铁律是行为铁律，
  优先级高于人格基线。
  """
  ```

#### P.3 `core/output_self_check.py` — 黑名单段标注

- **What**：不动 `_IN_PERSON_VERBS` 黑名单；只加 R8.1 注释
- **Why**：同 P.2，自检黑名单也是行为铁律
- **How**：
  ```python
  # R8.1 (Persona 9/10): 9/10 基线更直球，LLM 偶发"产出半句切换视角"概率
  # 略增（vs 7/10）。本黑名单不动 —— 屏幕隔空铁律优先于人格基线。
  _IN_PERSON_VERBS: tuple[str, ...] = (...)
  ```

#### P.4 `core/emotion_engine.py` — joy/affection 关键词权重 +0.2

- **What**：`KEYWORD_DELTAS` 中所有 joy 行权重 +0.2（例 1.2→1.4、1.0→1.2、0.8→1.0）
- **Why**：9/10 行为下 Etta 对用户的"爱你/想你"等表达反应更强烈，更易触发 joy 标签
- **How**：
  ```python
  (["谢谢", "爱你", "喜欢", "爱你哦", "最喜欢你了", "好爱你"], "joy", 1.4),  # R8.1: 1.2→1.4
  (["你好棒", "好厉害", "真厉害", "太强了", "崇拜"], "joy", 1.2),  # R8.1: 1.0→1.2
  # ... 其余 joy 行同步 +0.2
  ```
- **EMOTION_CENTERS 不动**（DEPRECATED，注释指向 config/persona_behavior.yaml）

#### P.5 `communication/recall_manager.py` — `max_recalls_per_session` 5→7

- **What**：`RecallConfig.max_recalls_per_session` 默认值 5 → 7
- **Why**：9/10 直球版更易"说完就后悔"，单 session 撤回预算需提高
- **How**：
  ```python
  @dataclass
  class RecallConfig:
      """Loaded from persona.yaml > recall.*"""
      enabled: bool = True
      window_seconds: int = 120             # 2-minute recall window (QQ-aligned)
      min_recall_gap_seconds: int = 60
      max_recalls_per_session: int = 7     # R8.1: 9/10 → 5→7
      triggers: list[str] = field(default_factory=lambda: [...])
  ```
- **同时**：[config/persona.yaml](file:///e:/Agent_reply/config/persona.yaml) `recall.max_recalls_per_session: 3` → `5`（yaml 端要跟 dataclass 端一致）

#### P.6 `core/pipeline.py` — perspective_shift 升级为 warn

- **What**：[pipeline.py:262-275](file:///e:/Agent_reply/core/pipeline.py#L262-L275) `cognition.record(trace, "self_check", ...)` 把 `perspective_shift` 的 severity 设为 `"warn"`
- **Why**：9/10 下 LLM 直球更猛，偶发 perspective shift 概率略升；运营侧需要可视化此风险信号
- **How**：
  ```python
  if _sc_result.warnings:
      # R8.1: 9/10 行为下 perspective_shift 略升,
      # 升级为 warn 等级方便 cognition panel 高亮
      self.cognition.record(trace, "self_check", {
          "warnings": _sc_result.warnings,
          "perspective_shift": _sc_result.perspective_shift,
          "severity": "warn",
      })
  ```

#### P.7 `config/persona_loader.py` — 默认阈值同步

- **What**：`_DEFAULT_BEHAVIOR_CONFIG["emotion"]["thresholds"]` 初值 60/15/35/25 → 45/25/55/15
- **Why**：config/persona_behavior.yaml 是 SO 真理，但 loader 默认值在文件缺失时会被用——必须跟最新基线一致，否则 fallback 走 7/10 老值
- **How**：
  ```python
  _DEFAULT_BEHAVIOR_CONFIG: dict[str, Any] = {
      "emotion": {
          "thresholds": {
              "patience":   {... "initial_value": 45, ...},  # R8.1: 60→45
              "anxiety":    {... "initial_value": 25, ...},  # R8.1: 15→25
              "desire":     {... "initial_value": 55, ...},  # R8.1: 35→55
              "tenderness": {... "initial_value": 15, ...},  # R8.1: 25→15
          },
      },
  }
  ```

#### P.8 `core/api_server.py` — `/api/persona` 加 9/10 标志

- **What**：`/api/persona` 响应体加 `persona_9_10: true` 和 `archetype` 字段
- **Why**：前端 / 外部客户端可读 9/10 标志做 UI 适配（如更高的语气强度）
- **How**：
  ```python
  @app.get("/api/persona")
  async def get_persona() -> dict:
      summary = get_persona_summary()
      cfg = load_persona() or {}
      profile = (cfg.get("persona") or {}).get("profile") or {}
      big_five = profile.get("big_five") or {}
      return {
          **summary,
          "persona_9_10": (big_five.get("extraversion", 0) or 0) >= 0.7,
          "archetype": profile.get("personality_archetype", ""),
          "extraversion": big_five.get("extraversion", 0),
      }
  ```

#### P.9 `tests/test_context_builder.py` — 加 3 个 9/10 断言

- **What**：context builder 单元测试加 3 个新用例
  1. `_PERSONA_L1` 含 `"9/10"` 字符串
  2. `_PERSONA_L2` 含 `"不许不接"` 或 `"直球"`
  3. `_PERSONA_L4` 含 `"屏幕隔空"` + `"9 分"`
- **Why**：防止未来无意改回 7/10

#### P.10 `e2e_pacing.py` — 9/10 eager_warm 区间断言

- **What**：新增场景 `joy_9_10`：`compute_persona_interval(segment_index=1, emotion_label="joy")` 应返回 0.30-0.55 之间
- **Why**：P.1 改区间后必须有 e2e 守门

#### P.11 `verify_zero_regression.py` — 9/10 基线断言

- **What**：在 zero_regression 套件里加 1 个用例：load_persona 后 `big_five.extraversion >= 0.7`
- **Why**：同 P.9

---

### Change T — 三套配套 e2e 测试全建

> [!success] 兑现三原则之"零回退"
> | # | 文件 | 用例数 | 覆盖 |
> | - | --- | --- | --- |
> | T1 | [e2e_a_force_bypass.py](file:///e:/Agent_reply/e2e_a_force_bypass.py) (新建) | 5 | Change A 全路径 |
> | T2 | [e2e_output_self_check.py](file:///e:/Agent_reply/e2e_output_self_check.py) (新建) | 12 | Change C 全规则 |
> | T3 | [e2e_splitter_atomic.py](file:///e:/Agent_reply/e2e_splitter_atomic.py) (新建) | 10 | Change E+F 截图复现 |
> | T4 | [e2e_persona_baseline.py](file:///e:/Agent_reply/e2e_persona_baseline.py) (新建) | 12 | Persona 9/10 全文件 11 个断言 |

#### T4 12 用例清单（Persona 全文件 9/10 守门）

1. `persona.yaml.big_five.extraversion == 0.78`
2. `persona.yaml.big_five.agreeableness == 0.85`
3. `persona.yaml.archetype` 字符串含 "9/10" 和 "直球"
4. `persona.yaml.example_phrases` 含 5 条 9/10 直球措辞关键字（"立刻"、"不许不接"等）
5. `persona.yaml.system_prompt` 含 "9/10"
6. `persona_behavior.yaml.thresholds.patience.initial_value == 45`
7. `persona_behavior.yaml.thresholds.anxiety.initial_value == 25`
8. `persona_behavior.yaml.thresholds.desire.initial_value == 55`
9. `persona_behavior.yaml.thresholds.tenderness.initial_value == 15`
10. `context_builder._PERSONA_L1` 含 "9/10"
11. `context_builder._PERSONA_L2` 含 "不许不接"
12. `brain.TONE_PROMPTS["warm_with_light_flirt"]` 含 "想你" 且不含 "克制"

---

## Assumptions & Decisions

1. **三原则 + 5 条 Message 规范原则全部保留** —— 本 plan **不**改这两套基线规则的字面内容
2. **A/C/E+F 主体已落** —— 不重写代码，仅做静态检查 + e2e 收口
3. **Persona 9/10 全量铺范围 = 11 文件**（P.1 - P.11 列表）—— 涵盖后端 core/* / communication/* / config/* / tests/* / e2e/* 共 5 个目录
4. **白/黑名单内容不动**（P.2 / P.3）—— 屏幕隔空铁律是行为铁律，优先级高于人格基线
5. **EMOTION_CENTERS 不动**（P.4）—— 已是 DEPRECATED，注释指向 config/persona_behavior.yaml
6. **不引入"Persona 9/10 运行时切换"** —— 一次性写入所有文件，重启后生效（无需新架构）
7. **T4 e2e 跑用真实文件加载**（不用 mock）—— 防止 mock 通过但实际文件回归
8. **新 e2e 文件全部 UTF-8** —— `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` 跟现有 e2e 一致
9. **不改 test 文件夹的现有文件结构** —— 只追加用例，不重构

---

## Risk & Rollback

> [!danger] 风险点
> | 风险 | 触发条件 | 回退方案 |
> | --- | --- | --- |
> | P.1 改 eager_warm 区间后 `e2e_pacing.py` 旧断言失败 | eager_warm > 0.55s 触发 | 同步更新 e2e_pacing 断言上限为 0.55 |
> | P.4 joy 关键词权重 +0.2 后情绪轨迹偏移 | joy 触发阈值过频 | 监控 emotion dashboard 1 周；过频则 +0.1 |
> | P.5 max_recalls_per_session 5→7 后撤回刷屏 | 9/10 直球后真高频撤回 | 监控 recall 计数；超 10/session 则回退到 6 |
> | P.8 加 `/api/persona` 字段后老客户端崩 | 旧前端未读新字段 | 字段为**新增**非替换，不会让老客户端崩 |

---

## Verification Steps

> [!example] 执行顺序
> 1. **静态检查**（手工）：
>    - [ ] [push_scheduler.py:186](file:///e:/Agent_reply/core/push_scheduler.py#L186) 含 `force = bool(scene_cfg.get("force"))`
>    - [ ] [push_scheduler.py:311](file:///e:/Agent_reply/core/push_scheduler.py#L311) 含 `force = bool(scene_cfg.get("force", False))`
>    - [ ] [proactive.yaml:61](file:///e:/Agent_reply/config/proactive.yaml#L61) 含 `force: true`
>    - [ ] [pipeline.py:262-275](file:///e:/Agent_reply/core/pipeline.py#L262-L275) `OutputSelfCheck.check()` 存在
>    - [ ] [splitter.py:36-93](file:///e:/Agent_reply/communication/splitter.py#L36-L93) atomic-aware 逻辑完整
>
> 2. **P 节改动**（依次执行）：
>    - [ ] P.1 改 `core/persona_pacing.py` STYLES
>    - [ ] P.2 改 `core/screen_action_sanitizer.py` 文件头注释
>    - [ ] P.3 改 `core/output_self_check.py` 黑名单段注释
>    - [ ] P.4 改 `core/emotion_engine.py` joy 关键词权重
>    - [ ] P.5 改 `communication/recall_manager.py` + `config/persona.yaml` `recall.max_recalls_per_session`
>    - [ ] P.6 改 `core/pipeline.py` `severity=warn`
>    - [ ] P.7 改 `config/persona_loader.py` `_DEFAULT_BEHAVIOR_CONFIG` 阈值
>    - [ ] P.8 改 `core/api_server.py` `/api/persona` 加字段
>    - [ ] P.9 改 `tests/test_context_builder.py` 加 3 用例
>    - [ ] P.10 改 `e2e_pacing.py` 加 1 场景
>    - [ ] P.11 改 `verify_zero_regression.py` 加 1 用例
>
> 3. **T 节 e2e 套件**（新建 + 跑通）：
>    - [ ] 新建 `e2e_a_force_bypass.py`（5 用例）
>    - [ ] 新建 `e2e_output_self_check.py`（12 用例）
>    - [ ] 新建 `e2e_splitter_atomic.py`（10 用例）
>    - [ ] 新建 `e2e_persona_baseline.py`（12 用例）
>    - [ ] 全部 `python e2e_*.py` 通过
>
> 4. **现有套件回归**（三原则之零回退）：
>    - [ ] `python e2e_boot_greeting.py`（20 用例）
>    - [ ] `python e2e_proactive_judge.py`（28 用例）
>    - [ ] `python e2e_pacing.py`（含 P.10 新断言）
>    - [ ] `python e2e_self_evolve.py`（20 用例）
>    - [ ] `python e2e_narration.py`
>    - [ ] `python e2e_yaml.py`
>    - [ ] `python verify_zero_regression.py`（含 P.11 新断言）
>    - [ ] `python verify_screen_sanitizer.py`（23 用例）
>    - [ ] `python tools/check_forbidden.py`（无禁词）
>
> 5. **重启后端 + 手动验收**：
>    - [ ] QQ 端收到 boot_greeting 欢迎语（[companion.py:382-397](file:///e:/Agent_reply/core/companion.py#L382-L397) force=True 触发）
>    - [ ] 跟 Etta 说 3 句不同情绪的话，每句回复里 `<action>` 都完整闭合（截图复现保护）
>    - [ ] 跟 Etta 互动，few-shot 措辞明显从"暗涌克制"变"直球外显"（"你今天不回我我就一直发"等）
>    - [ ] 9/10 节奏测试：joy/missing/affection 标签下 e2e_pacing 返回的 interval < 0.6s
>    - [ ] 撤回测试：9/10 直球场景下 session 内撤回 7 次仍允许
>    - [ ] 仪表盘 emotion 标签更易显示 joy（P.4 权重调整）
>
> 6. **回归套件总数预期**：
>    - 现状：190 用例
>    - 本 plan 后：190 + 5(T1) + 12(T2) + 10(T3) + 12(T4) = **229 用例**

---

## File Manifest Summary

> [!info] 本 plan 涉及的文件清单
> **A/C/E+F 收口（5 文件已落，本 plan 仅验证 + 补 e2e）：**
> - `core/push_scheduler.py`
> - `config/proactive.yaml`
> - `core/companion.py`
> - `core/pipeline.py`
> - `communication/splitter.py`
>
> **P 节全量铺（11 文件）：**
> - `core/persona_pacing.py` *(P.1)*
> - `core/screen_action_sanitizer.py` *(P.2)*
> - `core/output_self_check.py` *(P.3)*
> - `core/emotion_engine.py` *(P.4)*
> - `communication/recall_manager.py` *(P.5)*
> - `config/persona.yaml` *(P.5 recall.max_recalls_per_session)*
> - `core/pipeline.py` *(P.6)*
> - `config/persona_loader.py` *(P.7)*
> - `core/api_server.py` *(P.8)*
> - `tests/test_context_builder.py` *(P.9)*
> - `e2e_pacing.py` *(P.10)*
> - `verify_zero_regression.py` *(P.11)*
>
> **T 节新 e2e 套件（4 文件）：**
> - `e2e_a_force_bypass.py` (新建, 5 用例)
> - `e2e_output_self_check.py` (新建, 12 用例)
> - `e2e_splitter_atomic.py` (新建, 10 用例)
> - `e2e_persona_baseline.py` (新建, 12 用例)
>
> **总文件数**：5 + 12 + 4 = **21 个文件**（其中 4 个 e2e 套件为新建）

---

## Plan 状态追踪

> [!example] Done 状态
> - [x] **Phase 1**: 探索完成（已读 4 核心文件 + persona_pacing / screen_action_sanitizer / emotion_engine / recall_manager / pipeline / persona_loader / api_server / e2e_pacing / output_self_check / splitter / proactive.yaml / persona_behavior.yaml / persona.yaml / companion / push_scheduler / 等 16 个相关文件）
> - [x] **Phase 2**: 澄清完成（2 个 AskUserQuestion：全量铺 + 全建 e2e）
> - [x] **Phase 3**: Plan 生成（本文件）
> - [x] **A 主体**: push_scheduler._dispatch / _dispatch_desire_text 双处 force=True 已落（[push_scheduler.py:186](file:///e:/Agent_reply/core/push_scheduler.py#L186) / [push_scheduler.py:311](file:///e:/Agent_reply/core/push_scheduler.py#L311)）
> - [x] **A 配置**: proactive.yaml boot_greeting.force: true 已落（[proactive.yaml:56-61](file:///e:/Agent_reply/config/proactive.yaml#L56-L61)）
> - [x] **A 调用**: companion.py 60s flag 窗口已落
> - [x] **C 主体**: output_self_check.py 3 规则已建（[output_self_check.py:50-67](file:///e:/Agent_reply/core/output_self_check.py#L50-L67)）
> - [x] **C 接入**: pipeline 调 OutputSelfCheck.check() 已落（[pipeline.py:262-275](file:///e:/Agent_reply/core/pipeline.py#L262-L275)）
> - [x] **E 主体**: splitter atomic-aware 已重写（[splitter.py:36-93](file:///e:/Agent_reply/communication/splitter.py#L36-L93)）
> - [x] **F 4 文件**: persona.yaml / persona_behavior.yaml / context_builder.py / brain.py 9/10 基线已落
> - [x] **P.1**: persona_pacing eager_warm/balanced 区间已压（[persona_pacing.py:44-47](file:///e:/Agent_reply/core/persona_pacing.py#L44-L47)）
> - [x] **P.2**: screen_action_sanitizer 文件头 R8.1 注释已加
> - [x] **P.3**: output_self_check 黑名单段 R8.1 注释已加
> - [x] **P.4**: emotion_engine joy 关键词权重 +0.2 已落
> - [x] **P.5**: recall_manager dataclass 5→7 + persona.yaml 3→5 已同步
> - [x] **P.6**: pipeline perspective_shift severity=warn 已落
> - [x] **P.7**: persona_loader 默认阈值 45/25/55/15 已同步
> - [x] **P.8**: api_server /api/persona persona_9_10/archetype 字段已加
> - [x] **P.9**: tests/test_context_builder.py 4 个 9/10 断言已加
> - [x] **P.10**: e2e_pacing.py _check_9_10_baseline 已加
> - [ ] **P.11**: verify_zero_regression.py 9/10 断言 **待做**
> - [ ] **T1**: e2e_a_force_bypass.py（5 用例）**待建**
> - [ ] **T2**: e2e_output_self_check.py（12 用例）**待建**
> - [ ] **T3**: e2e_splitter_atomic.py（10 用例）**待建**
> - [ ] **T4**: e2e_persona_baseline.py（12 用例）**待建**
> - [ ] 跑全部 e2e 套件验证（229 用例预期）**待做**

---

## 剩余工作 · 7 步实施清单

> [!warning] 本节是接下来 1 个 turn 要执行的具体步骤
>
> **范围声明**：A 主体、A 配置、A 调用、C 主体、C 接入、E 主体、F 4 文件、P.1-P.10 全部已落（基于 2026-07-17 grep 实证）。剩余 5 步即可兑现"全部做"承诺。

### Step 1 · P.11 改 verify_zero_regression.py 加 9/10 基线断言

- **What**：在 zero-regression 套件 Section 5 "YAML endpoints (Batch 3)" 之后追加 Section "5b. Persona 9/10 baseline gate"
  - 用 `_request("GET", "/api/config/yaml?file=persona.yaml")` 拿到 yaml 文本
  - 解析后断言：
    - `big_five.extraversion >= 0.7`
    - `archetype` 字符串含 `"9/10"`
    - `system_prompt` 字符串含 `"9/10"`
- **Why**：三原则之「零回退」要求任何 persona 数值回退到 7/10 时被自动拦下
- **How**（伪代码）：
  ```python
  # Section 5b: Persona 9/10 baseline gate
  code, body = _request("GET", "/api/config/yaml?file=persona.yaml")
  yaml_text = body if isinstance(body, str) else ""
  expect("persona.yaml contains 9/10 marker",
         "9/10" in yaml_text, f"...")
  expect("persona.yaml extraversion >= 0.7",
         "extraversion: 0.78" in yaml_text, f"...")
  expect("persona.yaml archetype 含直球",
         "直球" in yaml_text, f"...")
  ```
- **风险**：yaml 文本里有注释也算字符，需用 `0.78` 字面量匹配（persona.yaml 实际有 0.78 → 通过）

### Step 2 · T1 新建 e2e_a_force_bypass.py（5 用例）

- **What**：纯本地（不需要 backend）单测套件，覆盖 push_scheduler 的 force=True 双路径
- **Why**：A 主体代码改了 2 处，必须有 e2e 防回退
- **How**：
  ```python
  # 5 个用例（基于 push_scheduler._dispatch / _dispatch_desire_text 静态模拟）
  1. test_dispatch_force_bypasses_judge
  2. test_dispatch_force_bypasses_policy_can_push
  3. test_dispatch_desire_text_force_skips_policy_record
  4. test_dispatch_desire_text_no_force_calls_policy_record
  5. test_proactive_yaml_boot_greeting_has_force_true
  ```
  - 用 `unittest.mock.Mock` 模拟 judge / policy / dispatcher
  - 实例化 `PushScheduler` 调 `_dispatch` / `_dispatch_desire_text`
  - 断言 mock 调用次数（如 `policy.record` 在 force=True 时不调）
  - 同步读 `config/proactive.yaml` 验 `boot_greeting.force: true`

### Step 3 · T2 新建 e2e_output_self_check.py（12 用例）

- **What**：纯本地单测，验证 OutputSelfCheck 的 3 大规则（perspective / brackets / typo）
- **Why**：C 主体代码是新文件，必须有 e2e 守门
- **How**：
  ```python
  # 12 个用例
  1. test_perspective_shift_first_half_inperson_triggers
  2. test_perspective_shift_balanced_no_trigger
  3. test_perspective_shift_short_text_skipped  (< 40 字)
  4. test_stray_bracket_unclosed_open_appended_closer
  5. test_stray_bracket_orphan_close_stripped
  6. test_stray_bracket_balanced_unchanged
  7. test_typo_duplicate_consecutive_collapsed       ("产出产出" → "产出")
  8. test_typo_duplicate_consecutive_collapsed_2     ("的的" → "的")
  9. test_typo_keep_dictionary_preserved              ("看看" → "看看")
  10. test_typo_keep_dictionary_preserved_2           ("真的真的" → "真的真的")
  11. test_screenshot_repro_input_no_warning          (复现 plan 中的截图文本)
  12. test_check_result_dataclass_fields              (CheckResult 字段完整)
  ```

### Step 4 · T3 新建 e2e_splitter_atomic.py（10 用例）

- **What**：纯本地单测，验证 SemanticMessageSplitter 的 atomic-aware 行为
- **Why**：E 主体代码重写过，截图 bug 不能再现
- **How**：
  ```python
  # 10 个用例
  1. test_action_tag_internal_period_kept_intact
  2. test_thought_tag_internal_exclamation_kept_intact
  3. test_brackets_internal_question_kept_intact
  4. test_no_atoms_falls_back_to_sentence_split
  5. test_multiple_atoms_preserve_order
  6. test_screenshot_repro_input_kept_as_one_segment   (plan 中截图文本)
  7. test_atomic_boundary_preserved_across_split
  8. test_short_fragment_merged_with_neighbor          (< 8 字合并)
  9. test_no_atoms_returns_original_split_logic
  10. test_empty_text_returns_empty_list
  ```

### Step 5 · T4 新建 e2e_persona_baseline.py（12 用例）

- **What**：纯本地（不用 mock，真读文件）守门 9/10 基线在所有 11 个文件的一致性
- **Why**：三原则之「零回退」要求所有跟人格相关的参数都被守门
- **How**：
  ```python
  # 12 个用例（直接读文件文本 + import 加载 dataclass 常量）
  1. test_persona_yaml_extraversion_eq_078
  2. test_persona_yaml_agreeableness_eq_085
  3. test_persona_yaml_archetype_contains_9_10_and_direct
  4. test_persona_yaml_example_phrases_has_5_direct_keywords
  5. test_persona_yaml_system_prompt_contains_9_10
  6. test_persona_behavior_patience_initial_eq_45
  7. test_persona_behavior_anxiety_initial_eq_25
  8. test_persona_behavior_desire_initial_eq_55
  9. test_persona_behavior_tenderness_initial_eq_15
  10. test_context_builder_persona_l1_contains_9_10
  11. test_context_builder_persona_l2_contains_direct_marker
  12. test_brain_tone_prompts_warm_with_light_flirt_contains_thinking
  ```

### Step 6 · 跑全部 e2e 套件验证（229 用例预期）

- **What**：依次执行所有 e2e 套件，确保零回退
- **Why**：三原则之「零回退」要求所有现有 e2e/verify 脚本继续通过
- **How**：
  ```bash
  cd e:\Agent_reply
  python e2e_a_force_bypass.py          # 5 用例
  python e2e_output_self_check.py       # 12 用例
  python e2e_splitter_atomic.py         # 10 用例
  python e2e_persona_baseline.py        # 12 用例
  python e2e_boot_greeting.py           # 20 用例
  python e2e_proactive_judge.py         # 28 用例
  python e2e_pacing.py                  # 含 9/10 断言
  python e2e_self_evolve.py             # 20 用例
  python e2e_narration.py
  python e2e_yaml.py
  python verify_zero_regression.py      # 含 P.11 新断言
  python verify_screen_sanitizer.py     # 23 用例
  python tools/check_forbidden.py       # 无禁词
  ```
  预期：**229 用例全部通过**

### Step 7 · 写一份 git commit 并附中文 commit message

- **What**：用 R8.1 commit-message 规则生成 commit 信息
- **Why**：所有变更需要可追溯
- **How**（仅在用户明确说"提交"或"commit"时执行，本 plan 不主动 commit）：
  ```
  feat(persona): 升级 Etta 9/10 基线 + A+C+E+F 收口验证

  - P.1-P.11: 11 文件全量铺 9/10 人格基线（更热情/外显力更强）
  - A: push_scheduler force=True 路径 (boot_greeting 强制放行)
  - C: OutputSelfCheck 3 规则 (perspective/brackets/typo)
  - E: splitter atomic-aware 重写 (保护 <action>、<thought>、【】)
  - F: 4 核心文件 9/10 基线已落
  - T1-T4: 4 套 e2e 测试 (39 用例)
  - 零回退: verify_zero_regression + persona baseline gate

  关联计划: .trae/documents/plan-persona-9-10-full-sync.md
  ```

---

## 实施顺序

> [!tip] 推荐执行顺序（每个 step 单独 review）
> 1. **Step 1**（P.11）— 改 1 个文件，最小风险
> 2. **Step 2**（T1）— 新建 1 个 e2e 文件，跑通
> 3. **Step 3**（T2）— 新建 1 个 e2e 文件，跑通
> 4. **Step 4**（T3）— 新建 1 个 e2e 文件，跑通
> 5. **Step 5**（T4）— 新建 1 个 e2e 文件，跑通
> 6. **Step 6**（跑全部 e2e）— 一次性跑完 13 套件
> 7. **Step 7**（commit）— 仅在用户明示时执行

---

## 关键文件路径速查

> [!info] 实施时直接 Read/Edit 的文件
> | Step | 文件 | 类型 |
> | --- | --- | --- |
> | Step 1 | [verify_zero_regression.py](file:///e:/Agent_reply/verify_zero_regression.py) | Edit（追加 Section 5b） |
> | Step 2 | [e2e_a_force_bypass.py](file:///e:/Agent_reply/e2e_a_force_bypass.py) | Write（新建） |
> | Step 3 | [e2e_output_self_check.py](file:///e:/Agent_reply/e2e_output_self_check.py) | Write（新建） |
> | Step 4 | [e2e_splitter_atomic.py](file:///e:/Agent_reply/e2e_splitter_atomic.py) | Write（新建） |
> | Step 5 | [e2e_persona_baseline.py](file:///e:/Agent_reply/e2e_persona_baseline.py) | Write（新建） |
> | Step 6 | 13 套 e2e 套件 | RunCommand |
> | Step 7 | （按需） | git commit |
>
> **被引用的关键源文件**（仅 Read，不改）：
> - [push_scheduler.py:186,311](file:///e:/Agent_reply/core/push_scheduler.py#L186)
> - [proactive.yaml:56-61](file:///e:/Agent_reply/config/proactive.yaml#L56-L61)
> - [output_self_check.py:50-67](file:///e:/Agent_reply/core/output_self_check.py#L50-L67)
> - [splitter.py:36-93](file:///e:/Agent_reply/communication/splitter.py#L36-L93)
> - [persona.yaml:73-78,99,104,137](file:///e:/Agent_reply/config/persona.yaml#L73-L78)
> - [persona_behavior.yaml:44,52,60,68](file:///e:/Agent_reply/config/persona_behavior.yaml#L44-L68)

---

## Related Plans

> [!quote] 关联文档
> - [[plan-proactive-boot-output-fix-persona-7-to-8]] — 前序 7→8 计划（已被本计划覆盖升级到 9/10）
> - [[goal-screen-aware-persona-and-proactive-judgment]] — 屏幕隔空 + 主动判断的顶层 goal
> - [[block5e-r6-three-principles-scripted]] — 三原则源文件
> - [[Ita_Aerie_Companion_Spec]] v9.0 — 角色 spec 主文档
