---
title: 历史凭据处置方案
kind: control
status: completed
tags: [aerie, release, security, history]
---
# 历史凭据处置方案
> [!success] 处置完成
> 2026-07-21 用户确认旧 SiliconFlow/DeepSeek key 已吊销、本地 `.env` 已换新，并明确授权按审计范围重写本地 Git 历史。目标 GitHub 仓库 `Laser1209/Aerie-Yunqi` 为公开空仓库，首次上传采用普通 push，不需要 force push。

## 最终结论
- 当前工作区扫描通过：`python tools\scan_provider_key_patterns.py` → `PROVIDER_KEY_SCAN_OK`。
- Git history 扫描通过：`python tools\scan_provider_key_patterns.py --history` → `PROVIDER_KEY_SCAN_OK`，`history_findings=0`。
- GitHub `>=100,000,000` bytes blob 数量为 0，高风险历史路径数量为 0。
- [[94_发布与安全检查]] 中“疑似凭据未进入文档、提交、历史或日志”已具备关闭证据。

## 清洗前范围
| 类别 | 原提交 | 路径 | 处置 |
|---|---|---|---|
| 旧规划/示例 key 形态 | `90e8a0466dae`、`68b295a39519` | `.trae/documents/OpenCloud_Companion_Development_Plan.md`、`OpenCloud_Companion/.env.example` | 旧 key 已吊销；历史值已替换为脱敏文本 |
| 测试 fixture 假 key 形态 | `54037a9b995f`、`6211dd03ba33` | `electron/tests/world-dashboard-renderer.test.js` | 历史假值已替换为非 provider-key 形态文本 |
| NapCat 私人运行物 | 最早可达于 `90e8a0466dae` | `NapCat/NapCat.Shell/config/`、`NapCat/NapCat.Shell/cache/` | 二维码和账号配置从所有 refs 移除，本地副本恢复后由 `.gitignore` 隔离 |
| 数据与日志 | 多个历史提交 | DB/WAL/SHM、日志、`uploads/`、`data/backups/`、`data/audit/` | 从所有 refs 移除，本地运行数据不进入仓库 |
| 构建产物 | `41a4cee`、`be9bb2e` 等 | `electron/dist*`、`Spotlight/public/*.exe` | 7 个 GitHub 超限 blob 从历史移除；安装包改用 GitHub Releases |

## 误报裁决
- Git pickaxe 候选 `be9bb2e`、`f73948a`、`40ce82b` 的宽松命中来自 SVG 内嵌 data URI 或被更长标识包裹的子串，严格 provider-key 边界命中为 0，未修改对应资源内容。
- 扩展 Gitleaks 清洗后剩余 7 条人工确认误报：5 条来自 vendored NapCat 固定代码标识，2 条来自 `YOUR_API_KEY` 文档占位符；未发现额外可用凭据。

## 实际执行
1. 在 Provider 后台吊销旧 key，并确认新 key 仅保存在 Git 忽略的根目录 `.env`。
2. 在仓库外创建完整 Git bundle、refs/HEAD 记录、未提交文档补丁，以及 `.env`、数据库、NapCat 私人配置和本机构建产物备份；备份未进入仓库。
3. 安装隔离的 `git-filter-repo 2.47.0`，在独立 mirror 中使用 `--sensitive-data-removal` 预演，再从清洗 mirror 创建普通 clone 验证。
4. 使用与受控扫描器一致的严格规则替换已确认的 `sk-` 形态值，并从三分支全部历史移除公开发布不应包含的运行数据和构建产物。
5. 预演通过后对本地仓库执行同一命令；`git filter-repo` 未删除 `.git`，未执行 filter-branch，未执行 force push。
6. 本地 `.env`、数据库、NapCat 配置和四套构建目录从校验备份恢复，内容一致且均被 Git 忽略。

## 分支映射
- 旧 `Aerie-Model-X` HEAD：`4f6cb00`。
- 新 `Aerie` HEAD：`32b4c53`。
- 新 `Aerie-Model-X` HEAD：`1b87408`。
- 新 `main` HEAD：`7324fec`。

## 验证证据
- `python tools\scan_provider_key_patterns.py --history` → `PROVIDER_KEY_SCAN_OK`。
- `python tools\history_remediation_preflight.py --json` → `history_findings=0`、`can_close_credential_history_gate=true`。
- `python -m pytest tests -q` → `557 passed, 6 warnings`。
- `node --test electron/tests/*.test.js` → `25 passed`。
- `npm run check:all` → emoji/forbidden/token 三项通过。
- `git fsck --full --strict` → 通过。
- 当前源码树除明确清理路径外无新增、删除或内容变化。
- Git pack 由约 `589.48 MiB` 降至约 `108.37 MiB`。

## 回滚与备份
- 清洗前 bundle、未提交文档补丁和本地私人文件备份位于仓库外，不得上传 GitHub。
- 若本地验证失败，可从 bundle 恢复旧 refs；旧 bundle 含已吊销 key 和私人历史，只能本地受控使用。
- 公开仓库一旦完成首次推送，不应把旧 bundle 推回远程；后续问题使用清洗历史上的修复提交处理。

## 发布裁决
- 已完成 `清洗并轮换`，凭据历史门禁关闭。
- 目标仓库为空，不包含旧历史；只向远程 `main` 普通推送清洗后的当前分支。
- 本地历史处置已完成，公开上传仅剩 GitHub CLI/凭据认证与首次 push。
