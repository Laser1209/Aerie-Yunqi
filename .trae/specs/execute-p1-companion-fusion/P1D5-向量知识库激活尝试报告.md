---
title: P1-D.5 向量知识库激活尝试报告
date: 2026-08-09T00:21:43
change-id: execute-p1-companion-fusion
doc-type: audit-record
audit-type: capability-attempt
phase: P1-D.5
status: blocked-with-evidence
tags:
  - Aerie
  - P1 计划
  - 向量知识库
---

# P1-D.5 激活专用向量知识库——尝试报告

## 尝试时间

2026-08-09T00:21:43

## 结论

**阻塞（blocked-with-evidence）**。与 P0 Task 3.6 结论一致，四项硬阻塞均未落地修复。

## 能力探测逐项现状

| 探测项 | 现状 | 阻塞点 | 可激活 |
| --- | --- | --- | --- |
| requirements.txt 中 chromadb | 第 118 行 `# chromadb>=1.5.9` 已注释 | 无 | ✅ 取消注释+安装 |
| chromadb 是否已安装 | 未安装（`ModuleNotFoundError`） | **硬阻塞** | ❌ |
| embedding 环境变量 | `AERIE_EMBEDDING_*` 全部未配置 | **硬阻塞** | ❌ |
| .env.example 含 embedding | 无任何 embedding 变量模板 | 硬阻塞（无模板） | ❌ |
| 生产代码接线 LayeredMemory | `core/brain.py` 未实例化 `LayeredMemory`，embedding_fn 未传入 | **硬阻塞（接线缺失）** | ❌ |
| data/chroma 目录 | 不存在（初始化时自动创建） | 无 | ✅ |

## 代码就绪点

- `memory/layers/long_permanent.py`：`LongTermMemoryLayer` 已完整实现 ChromaDB 初始化、`_get_embedding`、向量 store/retrieve/update/delete；chromadb 未装时自动降级纯 SQLite。
- `memory/layers/layered_memory.py`：`LayeredMemory.__init__` 接受 `embedding_fn` 并传给长期记忆层。
- `core/brain.py`：`_brain_bge_embed` 已实现 OpenAI 兼容 embedding 调用，绑定为 `Brain.bge_embed`。

## 激活前置（四项）

1. 取消 `requirements.txt` 第 118 行注释并 `pip install chromadb`
2. 在 `.env` 与 `.env.example` 配置 `AERIE_EMBEDDING_*`（或 fallback 到 `OPENAI_EMBEDDING_*`）
3. 生产代码从 `memory_store` 切换到 `LayeredMemory`
4. 将 `Brain.bge_embed` 作为 `embedding_fn` 传入

## 安全边界

安装依赖、配置 API Key、切换生产存储为外部状态变更，需用户显式授权后方可执行；本轮仅完成只读能力探测与证据记录。
