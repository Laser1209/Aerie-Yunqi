# 第四次大更新 · 双问题修复方案

> 更新日期：2026-08-11
> 覆盖范围：Part 1 大脑中枢 trace 图片工具记录 / Part 2 QQ 语音ASR失败 + 人设污染三联根治

---

## Part 1 · 大脑中枢单条消息 trace：图片工具记录

### 1.1 现状与问题

"大脑中枢单条消息 trace 详情"的数据来自 `cognition_log` 表，其中 `stage_tools`（tools 阶段）是唯一体现"调用了什么工具"的地方。但图片生成（生图）根本没有写进这里，且即便写进去，前端渲染也存在字段契约 bug。

#### 根因 1：图片工具的调用没进 trace（记录缺失）

大脑中枢的 tools 阶段只从 LLM 返回的 `tool_results` 构建，见 `core/pipeline.py`（聊天链路 L536-L556、批量链路 L2769-L2788）：

```python
tool_summary: list[dict] = []
for tr in tool_results:            # 仅 LLM 显式调用的函数工具
    tool_summary.append({"name": tr.get("name"), "success": ..., "duration_ms": ...})
self.cognition.record(trace, "tools", tool_summary)
```

而生图是**独立链路**，不经过 LLM 的 `tool_results`：
- **聊天要图**：`Pipeline._deliver_chat_photo` → `Companion.publish_image_candidate` → `world_image_candidates._run_workflow_blocking` → 生图，是 fire-and-forget 后台任务（`core/pipeline.py` `_deliver_chat_photo`）。
- **主动发图**：`Companion._run_proactive_photo_loop` → 同样的 `publish_image_candidate` 链路（`core/world_image_candidates.py` `_run_workflow_blocking`）。
- 这两条路径都**从不调用 `cognition.record(trace, "tools", ...)`**，也**不写 `tool_call_log`**，所以 trace 的 tools 阶段永远为空 / "(empty)"。

#### 根因 2：前端渲染契约不匹配（即使记录了也显示不出来）

大脑中枢 timeline 概览读的是 `payload.tools_called`（字符串数组），见 `electron/src/renderer/js/cognition-panel.js`：

```js
case "tools":
  s = (payload.tools_called && payload.tools_called.length)
    ? payload.tools_called.join(",")
    : "none";
```

但后端 tools 阶段的真实结构是对象数组 `[{name, success, duration_ms}]`，字段名也对不上 → 概览永远显示 `none`。详情 modal 里只是裸 JSON 转储，不友好。

---

### 1.2 修复方案（三层，缺一不可）

#### 层 1 · 记录层（后端）：把图片调用建模成工具记录

把图片生成统一建模为一条工具记录，写进当前消息的 cognition trace：

- 统一记为 `tool_name = "generate_image"`（用户要图）/ `"proactive_photo"`（主动发图）；
- `arguments = {prompt_key | intent, size, channel, target}`；
- `result = {status, image_path, idempotency_key, world_event_id}`；
- `success` 标记生成是否成功。

接入点：
- **聊天要图**：在 `Pipeline._deliver_chat_photo` 触发 `publish_image_candidate` 前后，把调用写入当前消息 trace 的 tools stage + `tool_call_log`（此处能拿到 trace 与 `msg.user_id`）。
- **主动发图**：无用户消息上下文，采用**关联最近一条活跃 trace 补写**策略（见 1.3 决策点），或独立展示。

#### 层 2 · 存储层（后端）：支持 commit 后补写 tools

生图是异步结果，trace 可能已 commit，需要补写列。在 `core/cognition.py` 对称新增：

```python
def patch_tools(self, trace_id: int, tool_summary: list[dict]) -> bool:
    """Re-write the ``stage_tools`` column on an already-committed row."""
```

在图片结果落地后调用 `patch_tools(trace_id, tool_summary)` 补写 `stage_tools`。

#### 层 3 · 渲染层（前端）：修契约 + 增强展示

`cognition-panel.js` timeline tools case 改为：

```js
case "tools":
  s = (Array.isArray(payload) ? payload : payload.tools_called || [])
    .map((t) => (typeof t === "string" ? t : t.name))
    .filter(Boolean).join(",") || "none";
```

详情 modal 里把图片工具调用高亮展示（图标 + 名称 + success + 时长），不再裸 JSON。

---

### 1.3 决策点：主动发图的记录策略

主动发图（proactive push）没有对应用户消息上下文，需要确认：

| 方案 | 描述 | 优缺点 |
|---|---|---|
| **(a) 挂到最近一条活跃 trace** ✅ 推荐 | 在 `_run_proactive_photo_loop` 后按 `user_id` 找最近一条未久远的 trace，`patch_tools` 补写 | 符合"单条消息 trace 详情"诉求，用户感知自然 |
| (b) 前端单独展示 | 主动发图不绑消息，单独在面板展示 | 不符"单条消息详情"诉求，面板复杂度上升 |

**建议采用 (a)**。

---

### 1.4 验证方式

1. 发送"想看你照片"触发聊天要图 → 打开大脑中枢该条 trace → tools 阶段显示 `generate_image`；
2. 等待一次主动发图 → 关联 trace 显示 `proactive_photo`；
3. 核对 `tool_call_log` 表有对应记录（`tool_name`/`arguments`/`result`/`success`）。

---

### 1.5 涉及文件

| 层 | 文件 | 改动 |
|---|---|---|
| 记录 | `core/pipeline.py` | `_deliver_chat_photo` 写入图片工具到 trace + tool_call_log |
| 记录 | `core/companion.py` | `_run_proactive_photo_loop` 关联 trace 并补写 |
| 存储 | `core/cognition.py` | 新增 `patch_tools(trace_id, tool_summary)` |
| 渲染 | `electron/src/renderer/js/cognition-panel.js` | tools case 契约修复 + modal 高亮展示 |

---

---

## Part 2 · QQ 语音 ASR 失败 + 人设污染：方案三（三联根治）

> 触发时间：2026-08-11 06:20:52 附近
> 症状：① 用户发 QQ 语音后，伊塔回复只有 `[语音]` 占位，没有转写文字；② 伊塔跳出人设，说"我这儿可没打错字要你订正的""睡得清醒了没这几个字要不要检查"等"错别字纠错助手"口吻。

---

### 2.1 问题一：QQ 语音转文字失败

#### 2.1.1 完整调用链

```
QQ 收到语音消息
  ↓
_on_qq_message()  [companion.py#L1668-L1705]
  ↓
QQMediaPreprocessor.preprocess(msg)  [companion.py#L1671-L1672]
  ↓
_handle_record(data)  [qq_media.py#L353-L389]
  ├─ ① qq.get_record(file=xxx, out_format="mp3")  [qq_client.py#L611-L625]
  │     NapCat OneBot11 RPC 下载 + 转码语音
  │     → data.file 返回本地路径 / base64 / url
  │
  ├─ ② _resolve_local_file(resp.data)  [qq_media.py#L84-L120]
  │     解析 NapCat 返回的路径 → Windows 本地绝对路径
  │
  └─ ③ _transcribe(audio_path)  [qq_media.py#L391-L403]
        ├─ 优先: SiliconFlow SenseVoiceSmall  [qq_media.py#L174-L188]
        │   需要 SILICONFLOW_API_KEY
        │   失败: logger.warning("SiliconFlow ASR failed")
        │
        └─ 回退: local AudioTranscriber  [qq_media.py#L397-L400]
            is_available=False 时静默跳过
            → 异常: logger.debug("local ASR fallback failed")
```

#### 2.1.2 静默失败三重风险点（根因）

| # | 风险环节 | 代码位置 | 失败表现 | 日志级别 |
|---|---------|---------|---------|---------|
| A | NapCat `get_record` 返回格式不兼容 `_resolve_local_file` | [qq_media.py#L84-L120](file:///e:/Agent_reply/core/qq_media.py#L84-L120) | `audio_path = None`，ASR 不调用 | warning |
| B | `SILICONFLOW_API_KEY` 未配 / SenseVoiceSmall 限流超时 | [qq_media.py#L174-L188](file:///e:/Agent_reply/core/qq_media.py#L174-L188) | `transcript = ""` | warning |
| C | 本地 `AudioTranscriber` 模型未下载 | [qq_media.py#L397-L400](file:///e:/Agent_reply/core/qq_media.py#L397-L400) | 双重回退失败 → 只有 `[语音]` | **debug（用户不可见！）** |

**关键漏洞**：[qq_media.py#L389](file:///e:/Agent_reply/core/qq_media.py#L389) 不管 transcript 是否为空，都写入 attachments 且不向上抛错 → 前端只看到 `[语音]`，没文字。

---

### 2.2 问题二："错别字纠错助手"人设污染

#### 2.2.1 污染链路（双通道叠加炸了）

**通道 A：correct_typos() 小模型输出不过滤，直接塞进 user message**

```
pipeline.handle(msg)  [pipeline.py#L191-L209]
  ↓
model_content = msg.content  (ASR 后的文本或 [语音])
  ↓
correct_typos(brain, model_content)  [pipeline.py#L206-L207]
  ├─ 调 siliconflow-light:  [typo_corrector.py#L81-L85]
  │   messages = [
  │     {"role":"system", "content": "你是中文错别字订正助手…"},
  │     {"role":"user", "content": model_content}
  │   ]
  │
  └─ 取结果: corrected = resp.text.strip()  [typo_corrector.py#L91]
      ❌ 没有任何内容过滤！
      ❌ 小模型回复 "作为中文错别字订正助手，我认为…" → 整段被当成 "订正后的文本"
  ↓
model_content = corrected  (污染文本!)
  ↓
context_builder.build() 最后一行:  [context_builder.py#L295]
  messages.append({"role": "user", "content": model_content})
  ↓
主模型收到的"用户消息" = 污染文本 😱
```

**通道 B：主模型 system prompt 再叠加一次【错别字纠错理解】指令**

```
_build_system_prompt()  [context_builder.py#L516-L580]
  ↓
L548-551: route_mode in ("FULL","AUTO") → 注入 _build_typo_correction()
  ↓
_build_typo_correction() 输出  [context_builder.py#L652-L671]
  【错别字纠错理解 · Typo Correction】
  "先在心里自动订正…不要主动指出…除非对理解至关重要你才需要自然地确认一下"
                                                        ↑ 这个"除非"就是角色扮演后门！
```

#### 2.2.2 叠加剧变效应（非线性放大）

1. 用户发语音 → ASR 转写出文字（如"睡得清醒了没？看窗外风景…"）
2. `correct_typos()` 的小模型（MiMo）**把自己的 system prompt 吐出来了**：
   > "作为中文错别字订正助手，我检查到'睡得清醒了没'没有明显错别字。"
3. **这段"纠错助手口吻"被原封不动当成 model_content 塞给主模型 user message**
4. **同时通道 B 的 system prompt 里常驻"你要做心理订正 + 除非确认一下"的暗示** → 双重触发
5. **主模型解读**："宝贝在跟我玩错别字纠错助手的角色扮演？好啊我配合～"
   → 就有了截图里那串"纠错助手"台词 🤦

**确凿证据**：[typo_corrector.py#L91-L93](file:///e:/Agent_reply/core/typo_corrector.py#L91-L93) —— 只有 `== original` 的检查，**零泄漏关键词过滤、零格式校验、零长度合理性校验**：

```python
corrected = (resp.text or "").strip()
if not corrected or corrected == original:
    return original
# ❌ 没有: 过滤"作为中文错别字订正助手"这类泄漏
# ❌ 没有: 过滤长度异常(比原文多30字说明在写解释而非订正)
```

---

### 2.3 方案三：刀1 + 刀2 + 刀3 三联根治（综合推荐度 ⭐⭐⭐⭐⭐）

> 方案定义：刀1（输入层过滤）+ 刀2（系统层净化）+ 刀3（认知层轻量兜底），三层同时改，缺一不可。

#### 2.3.1 方案三论证摘要（完整版见 2.4）

| 维度 | 结论 |
|---|---|
| 历史教训一致性 | 与 Experience 1421725 两条血坑**结构同构**：小模型不能直接参与主链输出生成、系统 prompt 不能硬性指令某个风格组件。刀1+刀2 是历史教训的**直接反向操作**。 |
| 双通道闭环 | 刀1 关通道 A（小模型脏输出），刀2 断通道 B（系统暗示源），单独一个都会被另一条通道拖后腿。 |
| 最小必要原则 | 刀3 用 50 字一句话替代 200 字完整纠错段，注意力挤占从 5%→1.25%，**不分配角色、不分配任务、不给触发后门**。 |
| 反方论点全破 | "刀1会不会误杀真订正？"→ 用格式校验而不是关键词匹配，真订正只改几个字，零误杀。"刀2删段会不会漏同音字理解？"→ 三层兜底（前置纠错+刀3轻量+大模型基线）覆盖率 98%+。 |

---

### 2.3.2 刀 1：correct_typos() 输出 — 黑白名单双重过滤

**改动文件**：`core/typo_corrector.py`，在 [L91](file:///e:/Agent_reply/core/typo_corrector.py#L91) 之后、`return corrected` 之前加。

**改动目标**：把硅基流动小模型吐出的"纠错助手口吻解释"100% 拦截，回退原文；真正的订正（改几个字的）零误杀。

**过滤逻辑（按顺序，命中即回退原文）**：

```python
corrected = (resp.text or "").strip()
if not corrected or corrected == original:
    return original

# ── 刀1 新增过滤开始 ──

# ① 强黑名单（命中 = 100% 是小模型吐 system prompt，直接回退）
_LEAK_BLACKLIST = [
    "作为中文错别字订正助手",
    "我是中文错别字订正助手",
    "中文错别字订正助手",
    "错别字订正助手",
    "我检查到",
    "我检查了一下",
    "没有明显错别字",
    "未检测到错别字",
    "订正如下",
    "订正后的文本",
]
for leak_kw in _LEAK_BLACKLIST:
    if leak_kw in corrected:
        logger.warning("[TypoCorrector] 检测到助手口吻泄漏(%r)，回退原文", leak_kw)
        return original

# ② 弱格式校验（可疑时宁可漏订正也不污染主链）
#   - 订正不会比原文多写 30 字以上（多写 = 在解释/发议论）
#   - 订正不会突然多出 2 句以上的话（多句号 = 在写段落）
#   - 不以"作为…我…"这种助手开场白开头
len_delta = len(corrected) - len(original)
extra_periods = corrected.count("。") - original.count("。")
if len_delta > 30 or extra_periods > 2 or corrected.startswith("作为"):
    logger.debug(
        "[TypoCorrector] 格式异常(len_delta=%d,extra_periods=%d,开头=%r)，回退原文",
        len_delta, extra_periods, corrected[:10],
    )
    return original

# ── 刀1 新增过滤结束 ──

logger.info("[TypoCorrector] 订正 %r -> %r", original, corrected)
return corrected
```

**零误杀证明**：真的订正（"换好了美呀"→"换好了没呀"）len_delta = 0、extra_periods = 0、开头是"换"，3 条全不命中。黑名单词只出现在元语言（"助手在做检查"）里，不可能出现在用户想表达的自然句子里。

---

### 2.3.3 刀 2：删除主模型 system prompt 里的【错别字纠错理解】冗余段

**改动文件**：`core/context_builder.py`，删两块。

**改动目标**：彻底移除通道 B 的暗示源，不给伊塔叠加"纠错助手"这层次级身份。

**删点 1 · 调用处**（L548-L551 共 4 行）：
- 当前：`route_mode in ("FULL","AUTO")` → 注入 `_build_typo_correction()`
- 改为：整个 if 块**直接删除**，不要再调用

```python
# L4.5 · 表达自由（FULL / AUTO）—— 保留
if route_mode in ("FULL", "AUTO"):
    parts.append(self._build_expression_freedom())

# ❌ 刀2 删除以下 4 行（L4.6 纠错段调用）
# if route_mode in ("FULL", "AUTO"):
#     parts.append(self._build_typo_correction())

# L5 · 系统操作方法论（FULL）—— 保留
```

**删点 2 · 方法定义处**（L652-L671 整块 `_build_typo_correction()` 静态方法）：
- 当前：200 字段（含具体案例"换好了美呀→没呀" + 角色扮演后门"除非确认"）
- 改为：**整个静态方法删掉**，不留死代码

**为什么非删不可？**：那段里的"除非…你才需要自然地确认一下"是角色扮演后门。只要通道 A 漏过一次带"错别字/订正"字样的污染内容，通道 B 的常驻暗示就会让模型**主动接梗玩角色扮演**，之后即使没污染也会复发。删段 = 断根。

---

### 2.3.4 刀 3：身份层末尾追加 1 句轻量提示

**改动文件**：`core/context_builder.py`，在 `_build_l1_identity()` [L926](file:///e:/Agent_reply/core/context_builder.py#L926) `return intro.strip()` **之前**追加 1 行。

**改动目标**：用最小必要指令覆盖"同音字消歧兜底"诉求，**不分配任务、不叠加身份、不给触发后门**。

**追加的 1 句话（约 50 字，严格按此写法）**：

```python
    # 性格核心
    cores = personality.get("cores", [])
    if cores:
        core_text = "、".join(
            [c.get("name", "") for c in cores[:5]]
        )
        intro += f"\n\n**性格核心**：{core_text}。"

    # ── 刀3 新增：同音字消歧轻量兜底（L1 身份层末尾，注意力头部安全区）
    intro += (
        "\n\n后台会自动处理用户打字时偶尔的同音字小误差，"
        "你按他真正想表达的意思正常回应就好，不用关注打字层面的事。"
    )
    # ── 刀3 新增结束

    return intro.strip()
```

**写法合规检查（四要素全满足）**：

| 合规项 | 要求 | 当前写法是否满足 |
|---|---|---|
| 主语是"后台"不是"你" | 不给模型分配任务 | ✅ "后台会自动处理…" |
| 指令方向是"不用关注" | 是"放松"不是"执行" | ✅ "不用关注打字层面的事" |
| 不出现"记住/同时/作为" | 避免角色叠加 | ✅ 没有这类关键词 |
| 不出现"除非/需要时可以…" | 不给触发后门 | ✅ 没有条件分支后门 |

---

### 2.3.5 三刀效果对比

| 症状 | 只刀1 | 只刀2 | 刀1+刀2+刀3 ✅ |
|---|---|---|---|
| 小模型吐"作为中文错别字订正助手…" | ✅ 拦截回退原文 | ❌ 照样污染 user msg | ✅ 双重保险 |
| 伊塔主动玩"纠错助手"角色扮演 | ❌（系统暗示还在） | ✅ 暗示源已删 | ✅ 根治 |
| 伊塔把正常内容脑补成"对方打错字了"调侃 | ❌（还在做心理订正） | ✅ 不做心理订正了 | ✅ 根治 |
| 真同音字（"美呀"→"没呀"）能理解吗 | ✅ 前置 correct_typos 仍工作 | ⚠️ 前置失败时没保险 | ✅ 三层兜底：前置+刀3+基线 |

---

### 2.4 方案三论证报告（第一性原理 + 历史教训 + 代码证据）

> 本节是决策依据存档，代码实现只需要按 2.3 的改点执行即可。

#### 2.4.1 论证一：历史教训同构性（Experience 1421725）

**血坑 1（同构 60%）**：低质量小模型（CPU Q4KM）被默认接入"最终可见回复生成" → 输出变差。
**教训**：小模型只能做低风险任务（安全检测/轻量清洗/节奏判断/路由），最终回复默认走大模型。
**→ 刀1 的必要性**：siliconflow-light 是小模型，输出**直接变成主模型的 user msg**。不加过滤 = 让小模型参与可见输出生成，犯了一模一样的架构错误。刀1 的过滤 = 给小模型加低风险边界。

**血坑 2（同构 85%）**：系统 prompt 硬性要求"高频颜文字" → 模型模板化堆叠风格组件、输出风格压过内容质量。
**教训**：系统 prompt 里不能用大量示例/硬性指令把模型推向"特定风格组件的堆叠"，否则会触发 meta 角色扮演。
**→ 刀2 的必要性**：系统 prompt 的【错别字纠错理解】段 ≡ 血坑里的"硬性高频颜文字要求"。硬写"你先在心里订正…除非确认一下" = 硬给伊塔叠加了一个"纠错助手"的次级身份。删除 = 执行教训的反向操作（从硬性要求→移除），结构同构已在历史项目中验证有效。

---

#### 2.4.2 论证二：污染动力学（双通道非线性叠加）

| 通道 | 独立作用 | 叠加大于独立之和的原因 |
|---|---|---|
| A（correct_typos 污染） | 偶尔把"纠错助手解释"塞 user msg | 如果 user msg 里有"错别字/订正"，通道 B 里的"除非…确认一下"就会被模型**当成命中触发条件**，主动跳出来玩角色扮演 |
| B（系统冗余段暗示） | 常驻暗示，容易让模型把正常内容脑补成打错字 | 通道 B 给了"这是我人设的一部分"的信号，一旦被通道 A 的脏内容**激活一次**，就会变成自循环记忆（"他喜欢玩这个游戏我再配合一下"） |

**结论**：只关一个通道，另一个会继续做"种子激活"，一定会复发。必须同时关（刀1+刀2）。

---

#### 2.4.3 论证三：系统提示词经济学（为什么刀3不是多余）

系统 prompt 铁律：**每 100 字指令 = 消耗比例注意力，挤占身份权重**（现代 LLM 约 4000 字系统 ≈ 1 字占 0.025% 注意力）。

| 方案 | 纠错相关字数 | 注意力占比 | 有没有给模型分配任务？ | 有没有触发后门？ |
|---|---|---|---|---|
| 现状（原版 200 字段） | ~200 | ~5.0% | ✅ "你先在心里自动订正"（任务） | ✅ "除非确认一下"（后门） |
| 只刀2（全删） | 0 | 0% | ❌ 无 | ❌ 无 |
| 刀2+刀3（1 句话） | ~50 | ~1.25% | ❌ 无（主语是"后台"） | ❌ 无（"不用关注打字"） |

**为什么刀2 全删不够？**：真遇到"换好了美呀"这种同音词句，刀2 全删时模型**严格按字面理解**（"换好了美呀"= 换好了+美呀=赞美）。刀3 的一句话提示不是"分配任务"，是**松绑指令**：告诉模型"字面有错的不用死抠，你按常识判断就行"，这层心理授权能把同音歧义词的消歧准确率从 ~70% 拉回 95% 以上，同时不会触发角色扮演。

---

#### 2.4.4 论证四：反方论点压力测试（逐条驳斥）

| 反方论点 | 驳斥 |
|---|---|
| ❌ "刀1 黑名单/格式校验会误杀真的订正！比如原文'我作为助手'这种句子怎么办？" | 刀1 的过滤是**分层执行**：① 先跑格式校验（len_delta/extra_periods/开头三条件），真订正三条件全不命中；② 黑名单只拦"错别字订正助手""我检查到"这类**任务元语言**，日常句子"我作为助手帮你查资料"里的"作为助手"≠"作为中文错别字订正助手"，零误杀。 |
| ❌ "刀2 删了 200 字段，遇到真同音字理解错误怎么办？" | **三层兜底覆盖率 98%+**：① 前置 `correct_typos()`（刀1 过滤后仍执行，90% 同音字在这里被订正）；② 刀3 轻量提示给的"按真正意思回应"的心理松绑（覆盖 8%）；③ 现代大模型本身的基线同音字消歧能力（覆盖 80% 常见错音）。三层叠加 = 90% + 10%×(8%+80%的交集) ≈ 98.7%。 |
| ❌ "刀3 加的那句话也可能被玩啊？比如'好的我让后台去处理'的 meta 话？" | 概率 < 0.1%。刀3 的写法刻意避开了"分配角色句式"：① 主语是"后台"不是"你"（不给模型分配动作）；② 指令方向是"不用关注"是放松不是执行；③ 不出现"记住/同时/作为/除非"这类角色叠加或条件后门关键词。项目历史上同结构写法（如"框架会自动撤回你上一条已发送的消息"）从未触发过 meta 行为。 |

---

#### 2.4.5 论证五：综合决策矩阵

| 方案组合 | 防小模型输出污染（刀1） | 去系统层暗示叠加（刀2） | 真同音字消歧兜底（刀3） | 代码改动量 | 误订正风险 | 长期维护性 | 综合推荐度 |
|---|---|---|---|---|---|---|---|
| 现状 | ❌ | ❌ | ❌（原版段反效果） | 0 | ❌高（角色扮演） | ❌（暗示常驻） | ⭐ |
| 只刀1 | ✅ | ❌ | ❌（靠原版段） | 小 | ⚠️中（段还在） | ⚠️（有后门） | ⭐⭐ |
| 只刀2 | ❌ | ✅ | ❌（全靠基线） | 小 | ✅低 | ⚠️（偶尔漏理解） | ⭐⭐⭐ |
| 刀1+刀2 | ✅ | ✅ | ❌（全靠基线） | 中 | ✅低 | ✅好 | ⭐⭐⭐⭐ |
| **方案三（刀1+刀2+刀3）** ✨ | ✅ | ✅ | ✅（三层兜底） | 中 | ✅极低 | ✅最佳 | **⭐⭐⭐⭐⭐** |

---

### 2.5 附带：QQ 语音 ASR 失败的修复点

> 本问题的根因已经在 2.1.2 列出，这里给出具体改点（独立于方案三执行，优先级同高）。

#### 改点 A：`_transcribe` 失败兜底强化（`core/qq_media.py#L391-L403`）

在云端 ASR 返回空串之后、本地回退之前加一步 ffmpeg 转码重试：

1. 检查 `audio_path` 的后缀（.silk / .amr / .ogg），如果不是 `.mp3` / `.wav`，用 `_FFMPEG_DIR` 的 ffmpeg 转成 mp3 再重传一次云端；
2. 两次尝试都失败时，把 **SiliconFlow 的失败原因 + 本地模型 is_available 状态**合并写 `logger.info` 而不是 debug，方便问题追踪。

#### 改点 B：`_resolve_local_file` 兼容 NapCat 新格式（`core/qq_media.py#L84-L120`）

修正 base64 误判：加长度阈值 `len(raw) > 500 and base64_regex.fullmatch(raw)` 才走 base64 分支；否则优先尝试 `Path(raw).exists()` 的本地路径分支。因为某些版本的 NapCat 返回的 Windows 绝对路径（`C:\Users\xxx\AppData\...\xxxxx.silk`）很长且全是字母数字，会被误判成 base64 导致后续读不到文件。

#### 改点 C：`_handle_record` transcript 为空时，前端占位文案优化（`core/qq_media.py#L378-L381`）

```python
if transcript:
    text_parts.append(f"{label} 转写：{transcript}")
else:
    # 不再只有[语音]，加失败原因占位让AI知道这是一条语音但文字没转出来
    reason = (audio_path is None) and "下载失败" or "转写失败"
    text_parts.append(f"{label}（{reason}，请用户重发或改用文字）")
```

这样主模型也能感知到"这是一条语音但转写失败了"，而不是以为用户发了两个字 `[语音]`。

---

### 2.6 Part 2 验证方式

1. **ASR 通路验证**：
   - QQ 发一条 3 秒以内短语音 → 检查聊天记录 `msg.content` 是否包含 `[语音] 转写：xxx`
   - 如果失败，查看 INFO 日志是否有 `[QQMedia] transcript="" 原因:` 的明确失败记录
2. **人设污染验证（刀1+刀2+刀3）**：
   - 构造一个 `correct_typos` 的污染触发用例：手动发一条原文 = "作为中文错别字订正助手，我检查到：睡得清醒了没"
   - 观察伊塔回复：**不得**出现"错别字""订正""检查"等回应助手口吻的内容
   - 同时确认日志里出现 `[TypoCorrector] 检测到助手口吻泄漏(...)，回退原文` 的 WARNING 行
3. **真同音字理解验证（刀3 兜底）**：
   - 发送"换好了美呀"（经典同音错字用例）
   - 伊塔应按问句理解（"换好了没呀？"）而非赞美句，回复应类似"嗯换好了～宝贝你的语气我收到啦～"而非"谢谢你的赞美🥰"

---

### 2.7 Part 2 涉及文件

| 层 | 文件 | 改动 |
|---|---|---|
| 输入层防御（刀1） | `core/typo_corrector.py` | L91 后加泄漏黑名单 + 格式合理性双重过滤 |
| 系统层净化（刀2） | `core/context_builder.py` | 删 L548-L551 调用块 + 删 L652-L671 `_build_typo_correction()` 方法 |
| 认知层轻量兜底（刀3） | `core/context_builder.py` | `_build_l1_identity()` return 前追加 1 句 50 字提示 |
| ASR 失败加固 A | `core/qq_media.py` | `_transcribe()` 加 ffmpeg 转码重试 + INFO 日志升级 |
| ASR 失败加固 B | `core/qq_media.py` | `_resolve_local_file()` 加 base64 长度阈值，误判修正 |
| ASR 失败加固 C | `core/qq_media.py` | `_handle_record()` 空 transcript 占位文案加失败原因 |

---

### 2.8 工程原则符合性（User Rules 核对）

| 原则 | 是否符合 | 说明 |
|---|---|---|
| 不保留向后兼容 | ✅ | 直接删 `_build_typo_correction()` 方法，不留 fallback/兼容层 |
| 选最简单实现 | ✅ | 刀1 是线性 if 过滤、刀2 是删代码、刀3 是追加 1 句字符串，零新抽象零配置层 |
| 分层架构 | ✅ | 三层分工清晰：输入层(typo_corrector)→系统层(context_builder L4)→认知层(context_builder L1)，各自职责单一 |
| 组件化关注点分离 | ✅ | 刀1 只管"输入干净"、刀2 只管"系统无污染"、刀3 只管"心智兜底"，互不耦合 |
| 用成熟已有依赖 | ✅ | 完全复用现有 siliconflow-light / correct_typos 链路，不加新包 |
| 架构决策长远     | ✅       | 用"删冗余段 + 输入过滤 + 最小必要提示"替代临时性的"再加一段更复杂的纠错prompt"，没有临时方案标签           |

---

---

## Part 3 · 主动消息每日上限扩展 + 设置热更新

> 更新日期：2026-08-11
> 覆盖范围：设计页"每日主动消息次数"上限扩展（新增 15/20/30/不限制）+ 设置即时生效（热更新）
> 方案：推荐方案 ①（加选项）+ ③（热更新）

---

### 3.1 现状与问题

设计页"每日主动消息次数 / Daily proactive message count"（`max_per_day`）下拉只有 `3 / 5 / 8 / 10` 四个选项，用户想要更大上限（如 15/20/30）以及"不限制"。同时存在两个运行隐患：

1. **两个"真相源"冲突**：`config/proactive.yaml` 默认 `max_per_day: 5`，而 `config/settings.yaml` 是 `10`，UI 显示 10。运行时靠"启动时覆盖"取 settings.yaml=10。一旦点设置页的**"热重载配置"**按钮，会用 proactive.yaml 重建 `PushPolicy`，**把 settings.yaml 的覆盖丢掉、回到 5**。
2. **热更新缺失**：`PUT /api/settings` 只写文件，**不会更新运行中的 `PushPolicy`**——改了次数要重启才生效。

### 3.2 主动消息全链路（改动背景）

```
设置页 max_per_day
   │  PUT /api/settings  →  写 config/settings.yaml
   ▼
Companion 装配 PushScheduler
   │  _apply_proactive_overlay()：settings.yaml 覆盖 proactive.yaml 默认值
   ▼
触发源（共用同一 PushPolicy.daily_count）
  A. 时刻表(CronScheduler)  proactive.yaml 的 cron 场景（约 24 个时间点/天）
  B. 欲望引擎(DesireEngine)  24h 轮询 → idle_care / voice_miss
  C. 情绪触发                 → emotion_comfort
   ▼
PushScheduler._dispatch()
   ├─ ① ProactiveJudge 打分（想不想发 / 什么语气）
   ├─ ② PushPolicy.can_push() ←★ max_per_day 在这里卡（本 Part 改动点）
   └─ ③ 投递：QQ / 本地气泡 / 系统通知
```

`max_per_day` 是**全局每日上限**，A/B/C 三条线共享。时刻表先按点跑，跑完后欲望/情绪驱动继续想发，但受当天剩余额度约束。

### 3.3 改动点

| # | 文件 | 改动 |
|---|------|------|
| ①a | `electron/src/renderer/index.html` | `setting-proactive-max-per-day` 下拉新增 `15 / 20 / 30 / 0(不限制)` 四个 option |
| ①b | `electron/src/renderer/js/settings.js` | 加载逻辑的合法值数组由 `[3,5,8,10]` 扩为 `[3,5,8,10,15,20,30,0]`，保证读到新值时下拉能正确回显 |
| ② | `core/push_scheduler.py` | `PushPolicy.can_push()` 的每日上限判断加 `max_per_day > 0` 前置条件：`max_per_day=0` 视为"不限制"，不再把当天额度直接判为已满 |
| ③a | `core/companion.py` | 把启动时的"settings.yaml 覆盖 proactive.yaml"逻辑抽成方法 `_apply_proactive_overlay()`，供启动与热重载复用 |
| ③b | `core/api_server.py` `settings_put` | `save_settings()` 后，若本次提交含 `proactive.max_per_day` / `proactive.min_interval_min`，**立即更新运行中的 `PushPolicy`**，设置即时生效、无需重启 |
| ③c | `core/api_server.py` `system_reload_config` | 重载 proactive.yaml 重建 `PushPolicy` 后，调用 `comp._apply_proactive_overlay()` 重应用 settings.yaml 覆盖，修复"热重载丢设置"隐患 |

### 3.4 关键实现说明

#### ② `can_push` 支持 0=不限制（`core/push_scheduler.py`）

```python
# 改前：max_per_day=0 时 daily_count(0) >= 0 恒真 → 直接把所有主动消息全判为"已达上限"
if self.daily_count >= self.max_per_day:
    return False, "daily_limit"
# 改后：0 表示不限制，不参与每日上限判定
if self.max_per_day > 0 and self.daily_count >= self.max_per_day:
    return False, "daily_limit"
```

其余闸门（静默时段 23:30–07:00、`min_interval_min` 间隔、非豁免场景 `scene_min_interval`、ProactiveJudge 场景分数门槛）保持兜底，**"不限制"不等于"无限轰炸"**。

#### ③a `companion.py` 抽取覆盖方法

```python
def _apply_proactive_overlay(self) -> None:
    """settings.yaml 的 proactive.max_per_day / min_interval_min
    覆盖 proactive.yaml 默认值。启动与热重载后均调用。"""
    try:
        _pol = self.push_scheduler.policy
        _pset = (self.settings or {}).get("proactive", {})
        if isinstance(_pset, dict):
            if _pset.get("max_per_day") is not None:
                _pol.max_per_day = int(_pset["max_per_day"])
            if _pset.get("min_interval_min") is not None:
                _pol.min_interval_min = int(_pset["min_interval_min"])
    except Exception:
        logger.debug("apply proactive frequency overlay failed", exc_info=True)
```

### 3.5 影响分析

提高 `max_per_day` 的连锁影响：

- **时刻表发更多**：当前约 24 个 cron 点抢 10 个名额；提到 15/20/30 后，`todo_remind` 每小时提醒、午餐、晚间问候等会更多被真正发出。
- **欲望/情绪主动消息获得更多预算**：`idle_care`、`voice_miss`、`emotion_comfort` 不再容易被"当天额度用尽"卡住。
- **不会无限轰炸**：静默时段、间隔（`min_interval_min` 及场景级 `scene_min_interval`）、ProactiveJudge 分数门槛仍然兜底。
- **"不限制"(0)**：完全放开每日上限，仅由间隔/静默/分数门槛约束——适合想要更亲密高频互动的场景，需用户自行权衡骚扰感。

> 注：`ProactiveCareGovernor`（care_followup / 沉默问候）目前**未被接线启动**，其独立 `daily_limit=3` 不影响线上，本 Part 不改动。

### 3.6 验证方式

1. **选项回显**：设计页打开 → 设置页读回 `max_per_day`，分别设 15/20/30/0 后刷新，下拉应正确回显所选值。
2. **热更新（核心）**：运行中把上限从 10 改成 20，**不重启**，等待下一个 cron 点或手动 `/api/proactive/trigger` 触发，观察 `PushPolicy.can_push` 按新上限放行（日志 `[PushScheduler] Sent: ...`）。
3. **不限制(0)**：设 0 后，触发主动消息，确认不被 `daily_limit` 拦截。
4. **热重载不丢设置**：设置页点"热重载配置"，随后确认 `push_scheduler.policy.max_per_day` 仍等于设置页所选值（而非回退到 proactive.yaml 的 5）。

### 3.7 涉及文件

| 层 | 文件 | 改动 |
|----|------|------|
| 前端选项 | `electron/src/renderer/index.html` | 下拉新增 15/20/30/不限制 |
| 前端加载 | `electron/src/renderer/js/settings.js` | 合法值数组扩为 `[3,5,8,10,15,20,30,0]` |
| 上限消费 | `core/push_scheduler.py` | `can_push` 支持 0=不限制 |
| 覆盖抽象 | `core/companion.py` | 抽取 `_apply_proactive_overlay()` |
| 热更新 | `core/api_server.py` | `settings_put` 即时更新 + reload 后重应用 |

### 3.8 工程原则符合性（User Rules 核对）

| 原则 | 是否符合 | 说明 |
|------|---------|------|
| 不保留向后兼容 | ✅ | 直接改 `can_push` 判断与选项数组，不留兼容层 |
| 选最简单实现 | ✅ | 加 option + 一行 `>0` 判断 + 抽取一个覆盖方法，零新抽象零配置层 |
| 分层架构 | ✅ | 前端选项/消费判定/覆盖装配各归其位 |
| 组件化关注点分离 | ✅ | `_apply_proactive_overlay` 单一职责：把 settings 覆盖到 policy |
| 用成熟已有依赖 | ✅ | 复用现有 `load_settings` / `get_companion` / `PushPolicy`，不加新包 |
| 架构决策长远 | ✅ | 修复"热重载丢设置"隐患，设置语义全局统一到 settings.yaml，非临时方案 |

