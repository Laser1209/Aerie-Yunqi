---
title: P1-D.5 向量知识库激活成功审计
date: 2026-08-09T00:51:40
change-id: execute-p1-companion-fusion
doc-type: audit-record
audit-type: capability-activation
phase: P1-D.5
status: activated-with-evidence
tags:
  - Aerie
  - P1 计划
  - 向量知识库
---

# P1-D.5 激活专用向量知识库——成功验收审计

## 验收时间

2026-08-09T00:50:53 ~ 2026-08-09T00:51:40

## 结论

**激活成功（activated-with-evidence）**。ChromaDB 专用向量知识库已安装、配置并落地语义检索，
上一轮 `P1D5-向量知识库激活尝试报告.md`（blocked-with-evidence）记录的四项前置均已解决。

## 前置落实情况（对照尝试报告）

| 前置项 | 之前阻塞 | 本轮落地 |
| --- | --- | --- |
| requirements.txt 中 chromadb | 注释未启用 | `chromadb>=1.5.9` 已启用 |
| chromadb 是否已安装 | 未安装 | `.venv` 内 `import chromadb` → **v1.5.9** ✅ |
| embedding 环境变量 | 未配置 | `.env` 新增 `AERIE_EMBEDDING_PROVIDER/API_KEY/BASE_URL/MODEL/TIMEOUT`、`AERIE_CHROMA_DIR`、`AERIE_KNOWLEDGE_COLLECTION` ✅ |
| .env.example 含 embedding | 无模板 | 已含完整模板 ✅ |

## 环境变量配置（.env）

```ini
AERIE_EMBEDDING_PROVIDER=chromadb_local
AERIE_EMBEDDING_API_KEY=
AERIE_EMBEDDING_BASE_URL=https://api.openai.com/v1
AERIE_EMBEDDING_MODEL=text-embedding-3-small
AERIE_EMBEDDING_TIMEOUT_SECONDS=60
AERIE_CHROMA_DIR=data/chroma
AERIE_KNOWLEDGE_COLLECTION=aerie_knowledge
```

> 安全边界：`AERIE_EMBEDDING_API_KEY` 保持为空，未擅自接入任何远程 embedding provider；
> 自动回退到 ChromaDB 本地 ONNX MiniLM 离线模型，符合"不配置 API Key 不对外请求"的边界约定。

## embedding 解析链路

`resolve_embedding_fn()` 按优先级：远程（Key 为空跳过）→ ChromaDB 本地 ONNX（**命中**）→ 确定性哈希兜底。
本轮实测走 ChromaDB 本地 ONNX（all-MiniLM-L6-v2，384 维）。

## 激活执行证据

运行 `scripts/p1d5_activate_knowledge.py`（2026-08-09 00:50）：

```
chromadb available: True
index result: {'indexed': 6, 'available': True, 'deduped': 0}   # 首轮写入 6 块
query='主动陪伴与情绪价值'  -> hits=['kb_companion', 'kb_world']
query='向量语义检索底座'    -> hits=['kb_empathy', 'kb_world']
query='角色世界状态快照'    -> hits=['kb_world', 'kb_empathy']
query='语音表情包通道表达'  -> hits=['kb_channels', 'kb_world']
query='共情与情绪回应'      -> hits=['kb_empathy', 'kb_world']
retrieved distinct topics: 4   # channels/companion/empathy/world
>=3 fusion concepts retrievable: True
```

二次运行（2026-08-09 00:50，验证幂等去重与持久化）：

```
index result: {'indexed': 0, 'available': True, 'deduped': 6}
```

- 已索引知识块：`kb_companion / kb_vector / kb_world / kb_channels / kb_empathy / kb_persona`
- 语义检索命中 ≥3 个融合概念：**4 个**（channels/companion/empathy/world）✅
- 幂等去重与 `data/chroma` 持久化均验证通过 ✅

## TDD GREEN 证据

`tests/test_p1_d5_knowledge_indexer.py`（6 用例：可用性、写入、去重、语义检索、失败降级）：

```
pytest tests/test_p1_d5_knowledge_indexer.py -q
6 passed, 1 warning in 1.86s
```

## 过程中修复

- `core/knowledge_indexer.py`：ChromaDB 本地 ONNX embedding 返回的 `np.float32` 标量列表不被
  Chroma 1.5.9 接受（`ValueError: Expected embeddings to be a list of floats...`），
  已改为 `[float(x) for x in vec]` 强制转换为 Python float，解决后 upsert/query 正常。

## 遗留

- SubTask P1-D.5.3「切换生产代码到 LayeredMemory 并传入 embedding_fn」为本轮范围外的生产接线，
  尚未执行，保持 open。
