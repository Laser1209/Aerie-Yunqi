---
title: 09-当前状态
aliases:
  - 当前状态
  - 系统状态摘要
  - 四份参考文档摘要
tags:
  - vault/status
  - project/current-state
status: active
created: 2026-07-27
updated: 2026-08-13
---

# 09-当前状态

本页汇总 Aerie 第三次修正计划 P0 融合闭环的四份参考文档核心信息，并沉淀当前已修复、遗留问题、技术债、测试副作用与禁止触碰边界。它是 [[00_首页]] 的状态详情页，也为 [[05_审计报告]]、[[06_验证索引]] 和 [[07_证据目录]] 提供事实入口。

> [!warning] 状态口径
> 本页只归纳已读取文档中的证据，不把计划项误写成已验收结果；涉及真实模型、QQ 扫码、外部向量库、Pyisland/eIsland 直接迁移等事项，仍需后续门禁确认和独立验证。

## 四份参考文档

| 来源 | 核心信息 | 当前结论 | 关联任务 |
| --- | --- | --- | --- |
| [Aerie 桌面端完整能力修复与全量审计计划.md](../documents/Aerie%20桌面端完整能力修复与全量审计计划.md) | 桌面窗口能启动但不能验收为全正常；重点修复启动健康、统一身份、PAD、无限历史、附件全链、World 生命周期、Electron 全元素审计与 QQ/NapCat 连接测试 | 提供本轮 P0 修复与审计验收的主边界；强调真实窗口、截图、接口时间戳、控制台、数据库快照与附件哨兵证据优先 | Task 1.1.1 |
| [修复_History.md](../documents/修复_History.md) | 记录隔离工作树、启动契约、统一身份、PAD、游标历史、附件 API、World lifecycle、二维码桥接、数据目录隔离、QQ 连接测试模式等推进过程 | 多项底层修复已在历史中描述为完成或测试通过，但仍需在当前主线重新归档证据，不能只用聊天记录替代验收 | Task 1.1.2 |
| [Echo情绪价值调研与Aerie结合方案.md](../documents/Echo情绪价值调研与Aerie结合方案.md) | Echo 的价值来自角色主体、关系/记忆可视化、主动触达、图片生活流、表情包、语音与低压力陪伴文案；Aerie 已有 PAD、四层记忆、上下文检索、Agent 循环和 QQ/移动通道 | 上层陪伴体验应接入现有 Agent 底座；最先修 UTF-8、附件 artifact、ImageObservation 和主动图片路由，不重写情绪引擎 | Task 1.1.3 |
| [Echo-Pyisland-Aerie统一融合实施方案.md](../documents/Echo-Pyisland-Aerie统一融合实施方案.md) | 将 Echo 陪伴、Pyisland/eIsland 桌面触达和 Aerie 附件/视觉底座合并为共享事件、状态、附件和工具架构 | 明确 P0 优先级：乱码修复、AttachmentEnvelope、ImageObservation、VisualIntentRouter、DesktopSurfaceAdapter 与安全边界 | Task 1.1.4 |

## 当前系统状态

| 领域 | 当前状态 | 证据来源 | 后续入口 |
| --- | --- | --- | --- |
| 桌面启动与健康 | 已形成修复方向：隔离端口、实例 ID、真实路径归一化、backend-ready、统一主身份解析；身份锚定机制已完善，人设记忆稳定性增强 | 桌面修复计划、修复历史、提交 `00a45c7` | [[06_验证索引#验证矩阵]] |
| PAD 与情绪面板 | 历史记录显示双冷启动约 65 秒产生当前身份快照，并修复健康应用名乱码与采样时间字段；新增决策赛马实际执行结果展示；仍需当前证据归档 | 修复历史、提交 `5c82e2c` | [[07_证据目录#文档证据]] |
| 长对话与历史 | 目标为无限累计历史、游标分页、滚动摘要、相关记忆/附件片段；消息渲染已收敛为单一数据源 Store（chat-store.js），并修复重复渲染、typing 残留、请求状态挂错气泡三重 bug，以渲染契约测试锁定 | 提交 `2a2a804`、`ec1b8c3`、`7d24cb1` | [[02_技术总览#架构机制]] |
| 附件与多模态 | 旧/新附件路径分裂是 P0 阻断；目标统一为 AttachmentEnvelope → Artifact → AI context parts → 前端按类型预览；多端交互与附件体验已升级，前端按类型预览仍待归档验证 | 四份参考文档共同指向、提交 `2861aad` | [[03_依赖清单#依赖总表]] |
| 图片理解 | Aerie 有 image_service、multimodal_input、brain 视觉入口；已新增图片表达层次认知（图片/表情包层级，图片权重更高）；结构化 ImageObservation、OCR/VLM/检测器编排与记忆准入仍需落地 | Echo 调研、统一融合方案、提交 `657df2d` | [[05_审计报告#发现列表]] |
| 主动图片 | 已实现聊天主动触发生图、AI 回复语义触发与意图判断层级（role_selfie/role_in_scene/couple_photo/environment_object 等），并对同视觉主题做持久化去重 + LLM 提示词接力；已修复世界数据接力异常导致空提示词（empty_prompt）秒拒问题，两层兜底保证任何异常下生图仍可完成、异常体落盘可复盘；仍需前端验收归档 | 提交 `199ae6b`、`d88b5bd`、`d41ff4f`、`d11d9df`、空提示词兜底（未提交） | [[04_决策记录#决策索引]] |
| World 生命周期 | 目标区分 enable/disable、start/stop、pause/resume、restart、show/hide；Sidecar 端口和令牌不得暴露给 Renderer；已修复时钟时区错位（UTC+8 本地时段），并整合百度地图、迁移世界模拟到重庆；生命周期 IPC/UI 受控仍有待归档 | 桌面修复计划、修复历史、提交 `b440b14`、`f916a87` | [[06_验证索引#覆盖缺口]] |
| QQ/NapCat | 已接入语音/表情包多媒体管道：语音 ASR 转写+语音条、表情视觉解析、出站收藏表情（fetch_custom_face）；每层独立降级，不阻塞主回复 | [[modules/QQMediaPipeline]]、提交 `f442931` | [[07_证据目录#审计证据]] |
| 身份/记忆/知识 | 身份锚定机制已完善；已重构主动发起逻辑与伊塔人设并接入对话知识库；新增知识库写入工具并启用全局工具支持；专用向量库独立能力探测仍待补齐 | 提交 `00a45c7`、`13bc637`、`5bac342` | [[07_证据目录#文档证据]] |
| 聊天体验与消息 | 修复消息撤回异常并优化时间戳过滤；新增错别字纠错、主动发图间隔配置与智能滚动；待办与日历双向同步并修复时间格式 bug | 提交 `d9c2807`、`1cce2d3`、`07fd2b2`、`532e8e4` | [[06_验证索引#验证矩阵]] |
| Pyisland/eIsland | 用户说明已获许可，可参考或迁移，但仍需来源、版本、NOTICE、权限隔离和审计记录 | 统一融合方案 | [[03_依赖清单#外部服务]] |
| 人设 AI 智能生成器 | 进行中：人设中心一句话/一段话角色描述 → 后端 5 阶段管线自动生成完整人设并套合成熟骨架（LLM 失败确定性兜底），结果只保存不激活，前端 AI 向导进度条实时反馈 | [[modules/PersonaGenerator]] | [[modules/PersonaGenerator#使用流程]] |
| 角色级对话与记忆隔离 | 已实施（门户 A-F 完成，G 集成验收中）：对话历史、长期记忆、上下文检索、摘要分桶、跨端时间线新增 `persona_id` 角色维度，切换人设后历史/记忆/召回全部跟随激活角色；存量数据共享兼容（NULL=共享）；迁移 013/014、写链/读链/记忆链/前端全链隔离；主动消息/生图消息已归入角色对话（`persist_proactive_message` 补 `messages` 层，管理平台可见 + 级联删除覆盖）；全量回归无新增失败，剩余 G-3 双角色 GUI 实测待用户验收 | [[plans/角色级对话与记忆隔离-实施计划]] | [[plans/角色级对话与记忆隔离技术方案#6 分阶段实施]] |

## 已修复

- 启动链路已有隔离端口、实例验证和 backend-ready 的修复记录，目标是避免 Electron 误连旧后端。
- 统一身份链路已有 `AERIE_PRIMARY_USER_ID`、`SELF_QQ`、本机配置与 YAML 的解析顺序设计，避免静默退回 `user_id=0`。
- PAD 冷启动、实时状态与系统统计已有修复历史，包含真实网络速率、采样时间、应用名乱码修复和当前身份快照验证。
- 长对话连续性已有滚动摘要、分页历史和上下文预算默认启用的记录，且未修改 `core/brain.py`。
- 附件 API、状态查询、重试/删除、专用 Python 3.12 环境、聊天气泡与历史库共享附件渲染已有接线记录。
- World lifecycle 已从单纯 show/hide 扩展到受控生命周期 IPC/UI，并补充关闭门禁回归修复记录。
- QQ 连接测试模式已有安全边界记录：丢弃私聊事件，拒绝发送、戳一戳、撤回，并只终止本批启动的 NapCat 进程树。
- 数据目录隔离已修复 desire state 固定写仓库 data 的缺口，并扩展关注桌面附件默认目录和启动问候标记。
- QQ 语音/表情包多媒体管道已实现：语音 CQ 码 ASR 转写（SenseVoice 云端 + whisper 本地回退）与前端语音条+转写文字；表情视觉解析与 face ID 映射；出站经 fetch_custom_face 拉取收藏表情、轻量 LLM 决策、规则按情绪挑图、send_image 直发 URL。详见 [[modules/QQMediaPipeline]]。

- 聊天渲染架构：消息渲染收敛为单一数据源 Store（`chat-store.js`，`chat.js` 只翻译成 DOM），修复同条消息重复渲染、typing 气泡残留、请求状态挂错 user 气泡三重 bug，并用渲染契约测试（47 项全绿）锁定（提交 `2a2a804`、`ec1b8c3`、`7d24cb1`）。
- 主动图片链路：实现聊天主动触发生图、AI 回复语义触发生图与意图判断层级（image 层级优先于 sticker 层级），并对同视觉主题做持久化去重（跨重启有效）+ LLM 提示词两步接力（提交 `199ae6b`、`d88b5bd`、`d41ff4f`）。
- 世界模拟时区修复：时钟改为本地时区（UTC+8）判定时段，生图时间注入统一归一本地时区，修复凌晨发"白天照"的 8 小时错位（提交 `b440b14`）。
- 世界地理与数据：整合百度地图服务，世界模拟迁移到重庆，并优化图像生成与意图识别（提交 `f916a87`）。
- 身份锚定与记忆：完善身份锚定机制、强化伊塔人设记忆稳定性；重构主动发起逻辑并接入对话知识库；新增知识库写入工具并启用全局工具支持（提交 `00a45c7`、`13bc637`、`5bac342`）。
- 图片表达层次认知：新增图片表达层级认知，区分"图片（虚拟存在锚点）"与"表情包/语气词（调味剂）"，图片权重更高（提交 `657df2d`）。
- 聊天体验与消息：修复消息撤回异常并优化时间戳过滤；新增错别字纠错、主动发图间隔配置与智能滚动（提交 `d9c2807`、`1cce2d3`、`07fd2b2`、`d11d9df`）。
- 待办与日历：实现待办与日历双向同步，并修复时间格式 bug（提交 `532e8e4`）。
- 决策赛马：新增决策赛马实际执行结果展示（提交 `5c82e2c`）。
- 生图提示词空串兜底：修复 empty_prompt 导致生图秒拒——`_image_prompt_for` 世界数据接力套 try 退回基础提示词（恒非空），`_resolve_prompt` 异常/空结果返回非空占位提示词，吞错日志由 `logger.debug` 升为 `logger.warning` 强制落盘；契约测试覆盖 resolver/context 双异常（`tests/test_phase14_world_image_candidates.py` 17/17 通过）。
- 主动消息/生图消息归入角色对话：修复主动消息与生图消息只写 `chat_log`、未写 normalized `messages` 层导致管理平台聊天记录看不到、删除遗漏的问题——新增 `ConversationRepository.persist_proactive_message()` 把主动推送/生图同步补进 `messages` 层（归入对应角色桌面/QQ 会话），admin 回收站/恢复/purge 级联兜底软删历史孤儿 `route_mode='PROACTIVE'` 行，并新增 `tools/cleanup_orphan_proactive.py` 手动物理清理历史孤儿行。

## 遗留问题

- 四份参考文档中的已修复项需要重新落入当前 Vault 的 [[06_验证索引]] 与 [[07_证据目录]]，不能只停留在历史对话摘要。
- Electron UTF-8 JSON/SSE 分块解码、附件单一事实源（旧 `attachment_handler` / 新 `attachment_worker_runtime` 收敛）、结构化 ImageObservation 与前端附件按类型预览仍是 P0 实现与验收重点（主动图片路由已实现，需前端验收归档）。
- 图片理解仍需沉淀 OCR、VLM、检测器、置信度与记忆准入的完整结构化编排，避免仅靠自然语言描述。
- Echo 登录后真实聊天语气、情绪分类、主动消息策略、音效、支付、训练机制、微信 ClawBot 细节仍标记为待验证。
- 对话知识库已接入并有写入工具，但专用向量库的 embedding 检索能力探测、写入与语义检索证据（Task 3.6）仍需补齐，不能以已接入对话知识库替代独立向量库验证。
- Pyisland/eIsland helper 虽已说明授权不阻塞，但真实代码迁移前仍缺版本、来源、NOTICE、权限边界和安全审计记录。
- 真实 Electron 全元素、全字符审计仍需要可复查的窗口截图、控制台、网络、DOM、布局、焦点和交互结果。

## 技术债

- 附件路径曾存在旧 `attachment_handler` 与新 `desktop_attachments/attachment_worker_runtime` 分裂，必须收敛到一个 Artifact 事实源。
- Office/PDF 解析仍有粗暴 Markdown 化风险；Markdown 只能作为展示投影，不能替代结构化页码、sheet、slide、range 与 parser warning。
- AI 上下文边界弱，附件内容必须以非可信用户内容传递，并带 attachment id、part id、页码/sheet/slide/range 与不确定性。
- 图片理解不能只靠自然语言描述，应沉淀 OCR、VLM、检测器、置信度、不确定性、模型元数据与删除联动。
- 主动消息与 World 仍需从固定模板/force 路径转为 world snapshot、关系状态、频控/Judge 与用户偏好共同决策。
- Renderer 暴露面需要持续收窄，Sidecar endpoint/token、本机绝对路径、二维码本机路径和扫描令牌不得进入 DOM、payload、截图或日志。
- 测试隔离曾暴露仓库 data 写入副作用，后续新增状态文件必须默认接入统一运行数据根。

## 测试副作用

- 修复历史记录过隔离运行时误写 `data/desire_state.json`，已定位为旧路径问题并恢复，后续必须验证所有状态文件服从 `AERIE_DATA_DIR` 或等价隔离根。
- 桌面附件默认目录与启动问候标记被列为同类旧路径风险，后续测试前需要先确认不会回落到仓库相对 `data`。
- QQ 测试只允许进程级临时开关，结束后必须恢复初始状态并检查残留端口、PID 和外部 NapCat 实例不受影响。
- 外部 QA 证据目录、截图、日志与接口输出不得进入 Git；Vault 只记录证据路径、摘要与脱敏结论。

## 禁止触碰边界

- 禁止保存密钥、令牌、QQ 正文、私人聊天内容、用户私有截图文本、邮箱、上传素材或真实文件正文。
- 禁止发送 QQ 消息、撤回消息、戳一戳、读取私人消息或触发真实启动问候；QQ 本批只测连接、二维码、心跳、重连和退出。
- 禁止污染正式数据库、正式运行数据目录和用户未提交改动；测试只能使用合成数据与隔离数据根。
- 禁止修改或纳入未授权边界文件，桌面修复计划中特别保留 Spotlight 暂存项、`core/brain.py` 和 `data/desire_state.json` 不被本批无关提交触碰。
- 禁止让危险文件执行；RAR/7Z、EXE/APK 等只能做安全清单、哈希和元数据分析。
- 禁止把 `environment_object` 环境图挂载角色参考图；禁止在低置信度时猜测角色本人入镜。
- 禁止把图片识别结果默认写入长期记忆；长期记忆需要用户确认或独立准入。
- 禁止把 Sidecar 动态端口、短期令牌、本机路径、扫描令牌暴露给 Renderer 或证据截图。

## 同步状态

| 检查项 | 状态 | 证据 |
| --- | --- | --- |
| Task 1.1 | 已完成文档研读与当前状态摘要 | 本页“四份参考文档”“当前系统状态” |
| C1.1 | 已覆盖四份输入文档核心信息 | [[09_当前状态#四份参考文档]]、[[00_首页#四份参考摘要]] |
| C1.2 | 已明确当前系统状态 | [[09_当前状态#当前系统状态]]、[[09_当前状态#已修复]]、[[09_当前状态#遗留问题]]、[[09_当前状态#技术债]]、[[09_当前状态#禁止触碰边界]] |

## 互链

- 回到首页：[[00_首页]]
- 模块入口：[[01_模块总览]]
- 技术入口：[[02_技术总览]]
- 审计入口：[[05_审计报告]]
- 验证入口：[[06_验证索引]]
- 证据入口：[[07_证据目录]]
- 经验复用：[[故障经验库-Troubleshooting-Playbook]]
