---
title: Aerie 二期 G0 基线与环境冻结
kind: baseline
status: done
version: 1.0
updated_at: 2026-07-21
work_package: P2-00
batch_id: P2-00-B01
gate: G0
baseline_source_commit: b8c9397bba935219885adb8c1c73512f66ae1694
baseline_source_tree: 873f0fe5208ef263189a5406318d8e95ed59fc83
evidence_commit: 71b8815537d750569c49c1c32ed5973933d2cb40
owner: ARCH
reviewer: QA
approval_status: accepted_with_deviation
deviation_id: DEV-P2-00-001
reviewed_at: 2026-07-21
---

# Aerie 二期 G0 基线与环境冻结

## 1. 批次合同

```yaml
batch_id: P2-00-B01
work_package: P2-00
week: W1
gate: G0
objective: 冻结实际代码 HEAD、工作区归属、仓库边界、工具链和隔离验证目录
owner: ARCH
reviewer: QA
owned_modules:
  - documents/二期升级/00_二期基线与环境冻结.md
  - documents/二期升级/07_二期任务编排.md
  - documents/二期升级/Aerie_项目二期升级更新计划.md
dependencies: []
feature_flag: none
default_state: not_applicable
data_impact: none
compatibility_path: 仅新增和更新治理文档，不修改任何运行路径
rollback: 仅通过新增文档纠正提交回滚 B01 治理记录；禁止直接 revert mixed commit 71b8815；并行功能、配置和测试由其所属任务独立回滚
tests:
  - 文档结构与字段检查
  - Git 变更范围检查
  - Provider key 形态扫描
evidence_types: [BAS]
risk_ids: [R-11, R-13, R-14]
non_goals:
  - 不运行完整测试、E2E、Electron smoke 或打包
  - 不修改任何功能代码、配置或运行数据
  - 不关闭 G0，不开始 P2-01
status: Done
```

## 2. 冻结结论

本批冻结的是 **已提交代码源基线**，不是当前共享工作区的全部内容。后续 P2-00-B02 必须在独立、干净、detached 的临时 worktree 中检出该提交重采证据，不能把当前持续变化的主工作区当作可复现基线。

| 项目 | 冻结值 |
|---|---|
| 主应用仓库 | `Laser1209/Aerie-Yunqi` |
| 本地分支 | `Aerie-Model-X` |
| 源代码基线 HEAD | `b8c9397bba935219885adb8c1c73512f66ae1694` |
| 源代码 Tree | `873f0fe5208ef263189a5406318d8e95ed59fc83` |
| 远端默认分支 | `origin/main` |
| 冻结时远端 main | `e90b84c383fdd98d8772a27b23f437d8b98ec0ba` |
| 本地相对远端 | ahead 3 / behind 0 |
| 本地领先提交 | `3666ee6`、`8f6e8bd`、`b8c9397` |
| 二期上传策略 | 显式推送远端 `Aerie-Model-X`，不得用普通 `git push` 误推 `origin/main` |
| 隔离源目录 | `%TEMP%\Aerie-P2-G0-b8c9397\source` |
| 隔离源状态 | detached HEAD=`b8c9397...`，创建后 clean |
| G0 当前状态 | Open；本批不构成 G0 通过 |

### 2.1 为什么同时记录 commit 与 tree

- `baseline_source_commit` 用于追踪历史、作者和父提交。
- `baseline_source_tree` 用于证明被测试的文件集合不因后续补写 Evidence 文档而变化。
- 本文件所在提交是治理证据提交，不反向替换 `baseline_source_commit`，避免“写基线文档导致 HEAD 改变，再次重写基线”的循环。

## 3. 两个 Git 发布单元

用户已要求大目录中的两个项目在 Git 上传和 GitHub 同步时单独处理。本项目冻结为以下两个发布单元：

| 发布单元 | 本地位置 | GitHub 远端 | 当前状态 | 强制规则 |
|---|---|---|---|---|
| Aerie 主应用 | `<repo-root>` | `Laser1209/Aerie-Yunqi` | 本批所属仓库 | 只暂存主应用本批明确文件；推送远端二期分支 |
| Spotlight 官网发布仓库 | `<repo-root>/.codex-deploy-aerie-spotlight` | `Laser1209/Aerie_Spotlight` | 独立仓库 HEAD `aff510c`，与其 `origin/main` 一致 | 使用 `git -C` 独立检查、提交和推送；不得由主仓库宽范围暂存 |

补充边界：

- `Spotlight/` 是当前主应用仓库中跟踪的官网源代码目录；网站发布仍必须在 Spotlight 发布仓库中形成独立提交和推送证据。
- 父仓库把 `.codex-deploy-aerie-spotlight` 记录为 gitlink，索引期望 `e78b735`，本机目录当前为 `aff510c`，因此父仓库显示 modified。
- 本批不更新该 gitlink，不提交 `Spotlight/` 源码，也不触发网站部署。
- 后续任何同时涉及主应用和官网的工作，必须拆成两个批次或至少两个独立仓库提交，并分别运行各自门禁。

## 4. 冻结时工作区归属

以下是 B01 冻结时的共享工作区快照。所有条目均早于本批文档编辑出现或由并行任务持续写入，本批原样保护并排除。

| 路径 | 状态 | 归属分类 | 本批处理 |
|---|---|---|---|
| `.codex-deploy-aerie-spotlight` | modified gitlink | Spotlight 独立发布仓库 | 排除；禁止暂存 |
| `config/settings.yaml` | modified | 批次外并行/用户拥有，具体归属由对应任务确认 | 排除；禁止读取秘密或覆盖 |
| `data/boot_greeting_last_sent.flag` | modified | Aerie 正在运行产生的状态 | 排除；禁止暂存/回退 |
| `data/desire_state.json` | modified | Aerie 正在运行产生的状态 | 排除；禁止暂存/回退 |
| `main.py` | modified | 批次外并行/用户拥有，具体归属由对应任务确认 | 排除；禁止覆盖 |
| `tests/test_v139_batch2.py` | modified | 批次外并行/用户拥有，具体归属由对应任务确认 | 排除；禁止暂存 |
| `core/mobile_gateway.py` | untracked | 批次外并行/用户拥有，具体归属由对应任务确认 | 排除；禁止暂存 |
| `documents/Android/Aerie_Android_Companion_Master_Plan.md` | untracked | 批次外并行/用户拥有，具体归属由对应任务确认 | 排除；禁止暂存 |
| `tests/test_mobile_gateway.py` | untracked | 批次外并行/用户拥有，具体归属由对应任务确认 | 排除；禁止暂存 |

本表只确认“本批不拥有这些文件”。是否纳入主应用或另一个子项目，必须由对应任务独立验收、提交和推送，不能通过 P2-00 顺带合并。

### 4.1 已忽略的秘密、运行数据与生成物

普通 `git status` 不显示被 ignore 的内容。以下路径已通过 `git check-ignore -v` 验证规则来源；本批不读取其内容，也禁止任何后续批次使用宽范围暂存或把它们打入安装包。

| 路径模式 | `.gitignore` 规则 | 分类 | 处理 |
|---|---|---|---|
| `.env` | `.gitignore:55` | 本机秘密 | 禁止读取、暂存、提交、Evidence 回显和打包 |
| `.manual-userdata*/` | `.gitignore:41` | 手动测试用户数据 | 禁止暂存、提交和打包 |
| `data/**/*.db` | `.gitignore:24` | 运行数据库 | 禁止暂存、提交和打包；测试只用隔离副本 |
| `data/backups/` | `.gitignore:29` | 数据备份 | 禁止暂存、提交和打包 |
| `data/audit/` | `.gitignore:30` | 运行审计 | 禁止暂存、提交和公开 Evidence |
| `/logs/` | `.gitignore:33` | 运行日志 | 禁止暂存、提交和打包；Evidence 只保存脱敏摘要 |
| `tmp/` | `.gitignore:37` | 临时测试/构建产物 | 禁止暂存、提交和打包 |
| `electron/dist*/` | `.gitignore:45` | Electron 构建产物 | 禁止暂存；发布产物走独立 Release 流程 |
| `uploads/` | `.gitignore:52` | 用户上传与图片资产 | 禁止暂存、提交、公开 Evidence 和打包 |

以上是最低排除清单，不代表未列出的文件可自动进入提交。每批仍必须先查看完整 status，再用显式文件路径暂存。

## 5. 运行态与隔离风险

冻结时检测到真实 Aerie 实例正在运行，`127.0.0.1:7890` 已被 Python 后端监听。因此：

1. 不在共享工作区启动第二个 Electron/Core 实例。
2. 不对现有 7890 端口发送写请求或把其结果记作 G0 基线。
3. 不在共享工作区执行固定端口 verify、旧 E2E、Electron smoke 或打包。
4. `AERIE_DATA_DIR` 与 `AERIE_USER_DATA_DIR` 不能单独证明完全隔离：当前仍有 `desire_state.json`、uploads 等相对项目路径。
5. P2-00-B02 在 `%TEMP%\Aerie-P2-G0-b8c9397\source` 中运行；相对路径写入只会污染临时 worktree，不会写当前主项目。
6. Electron smoke 如不能使用动态端口，必须等待真实实例停止或先增加经过评审的端口隔离合同；不得自动终止用户正在运行的实例。

### 5.1 B02 安全环境模板

```powershell
$BaselineRoot = Join-Path $env:TEMP "Aerie-P2-G0-b8c9397"
$SourceRoot = Join-Path $BaselineRoot "source"
$env:AERIE_DATA_DIR = Join-Path $BaselineRoot "data"
$env:AERIE_DB_PATH = Join-Path $env:AERIE_DATA_DIR "aerie.db"
$env:LOG_DIR = Join-Path $BaselineRoot "logs"
$env:AERIE_USER_DATA_DIR = Join-Path $BaselineRoot "electron-user-data"
$env:AERIE_DISABLE_QQ = "true"
$env:AERIE_DISABLE_MODEL_CALLS = "true"
```

限制：以上变量只覆盖已接入这些环境变量的路径和 QQ/Brain 外呼。天气、Feed、Edge TTS、工具、浏览器及其他适配器仍需测试级 Mock/poison 或断网策略；不得据此宣称“完整应用绝对无外呼”。

## 6. 工具链冻结

| 工具 | 版本/状态 | 批次规则 |
|---|---|---|
| Windows | Windows 11 x64，build 26200 | 当前 G0 只代表该环境，不代表 Win10 矩阵 |
| PowerShell | 5.1.26100.8655 Desktop | 证据命令使用 PowerShell 语义 |
| Python | CPython 3.14.3 | B02 显式使用主仓库 `.venv\Scripts\python.exe`，不使用未声明的系统解释器 |
| pip | 26.1.2 | `pip check` 当前为 `No broken requirements found` |
| pytest | 9.1.1 | B02 重新采集测试结果 |
| Node.js | 24.14.1 | 未形成仓库级 runtime pin，登记为 G0 事实 |
| npm | 11.11.0 | 使用 lockfile，不在共享工作区重装依赖 |
| Electron | 28.3.3 | 来自当前安装态 |
| electron-builder | 24.13.3 | B02 不在共享工作区直接打包 |
| Git | 2.54.0.windows.1 | 提交使用显式路径暂存 |
| GitHub CLI | 2.96.0，账户 `Laser1209` 已登录 | 主应用与 Spotlight 分别指定工作目录和远端 |

已知工具链缺口：

- Python 只有直接依赖 `requirements.txt`，不是完整传递依赖锁。
- 仓库没有统一 Python/Node runtime version 文件。
- 主仓库当前没有 `.github` CI 工作流。
- Electron 依赖许可证字段为 `UNLICENSED`，依赖/许可证审计留给 B02。

## 7. 依赖输入哈希

### 7.1 主应用

| 文件 | SHA-256 |
|---|---|
| `requirements.txt` | `FABA9F2771C8BD92E7C4A8AE081C446EAA8759D5E4DA8B00E57BC9C1DD8D0FF5` |
| `electron/package.json` | `598F980A7513992535A3CF767698F131C73213A567DDCAE825092C598E631779` |
| `electron/package-lock.json` | `F364DB9702BB34FBAC33ED358AE7A8DEFF329420ADF5B516A1B3214D0282B2F7` |
| `electron/electron-builder.yml` | `D9D308D7591AB644097D465D5DA8BE13CD5FA5D51F6461B61EF600F983B73751` |

### 7.2 Spotlight 源码输入（独立发布）

| 文件 | SHA-256 |
|---|---|
| `Spotlight/package.json` | `85F5D8B8DC567AC774BA1E8F103440358D8689EC074EB12CB4BBF535F6F832F4` |
| `Spotlight/package-lock.json` | `352B7538DCEDA8DBD61F6BDEFBB07D3522778BC7A02B8E1F3A2EE5AE05768075` |
| `Spotlight/remotion/package.json` | `B31FBF060DE0B4046802CA1266B2003921F0D11B8A21DC214DBFB0BA5DAB0407` |
| `Spotlight/remotion/package-lock.json` | `9C31422BF80C31720E39537258E46BC3F6E389301A72D339EFC3CA356B4095F5` |

哈希只冻结输入文件，不代表依赖许可证、安全或可复现安装已经通过；这些结论属于 P2-00-B02。

## 8. Git 上传规则

### 8.1 主应用

```powershell
git diff --check -- "documents/二期升级/00_二期基线与环境冻结.md" "documents/二期升级/07_二期任务编排.md" "documents/二期升级/Aerie_项目二期升级更新计划.md"
git commit --only -m "Document P2-00 B01 scope deviation" -- "documents/二期升级/00_二期基线与环境冻结.md" "documents/二期升级/07_二期任务编排.md" "documents/二期升级/Aerie_项目二期升级更新计划.md"
git push -u origin HEAD:Aerie-Model-X
```

禁止使用 `git add -A`、`git add .`、直接 `git revert 71b8815` 或不带 refspec 的首次 `git push`。

### 8.2 Spotlight 官网发布仓库

Spotlight 只有在对应网站批次拥有变更时才执行独立流程：

```powershell
git -C .codex-deploy-aerie-spotlight status --short --branch
git -C .codex-deploy-aerie-spotlight add -- <website-owned-paths>
git -C .codex-deploy-aerie-spotlight commit -m "<website batch>"
git -C .codex-deploy-aerie-spotlight push origin <website-branch>
```

本批 Spotlight 仓库无变更，不执行提交或推送。

### 8.3 并行提交范围偏差记录

`DEV-P2-00-001`：在 B01 三份治理文档显式暂存后，并行 Mobile/Android 任务创建了提交 `71b8815537d750569c49c1c32ed5973933d2cb40`，共享暂存区中的 B01 文档因此进入同一提交。该提交共包含以下 9 个路径：

- B01 拥有：`documents/二期升级/00_二期基线与环境冻结.md`、`documents/二期升级/07_二期任务编排.md`、`documents/二期升级/Aerie_项目二期升级更新计划.md`。
- 并行 Mobile/Android 任务拥有：`config/settings.yaml`、`core/mobile_gateway.py`、`documents/Android/Aerie_Android_Companion_Master_Plan.md`、`main.py`、`tests/test_mobile_gateway.py`、`tests/test_v139_batch2.py`。

事件发生时 `71b8815` 尚未推送远端。该事件不改变本批冻结的源代码基线 `b8c9397...` / `873f0fe...`，也不把 Mobile/Android 文件重新归属为 B01；后续审计必须按路径和本偏差编号追踪，不能把 `71b8815` 表述为纯文档提交。

作者于 2026-07-21 确认保留该提交并按当前分支正常推送，因此不改写历史、不回退并行任务内容。本纠偏提交只允许包含三份二期治理文档；后续并行批次必须使用独立 worktree/索引，避免共享暂存区被其他任务提交。残余事实是 `71b8815` 仍为 mixed commit，`DEV-P2-00-001` 只恢复治理可审计性，不重写既有历史。

## 9. B01 验收结果

| 验收项 | 结果 | 证据 |
|---|---|---|
| 实际 HEAD/Tree 已冻结 | Pass | `b8c9397...` / `873f0fe...` |
| 主应用与 Spotlight Git 边界已冻结 | Pass | 两个远端、两个独立上传命令 |
| 既有 dirty 文件已分类并排除 | Pass | 第 4 节归属表 |
| 已忽略秘密、运行数据和生成物已排除 | Pass | 第 4.1 节及 `git check-ignore -v` |
| 工具版本和依赖输入已记录 | Pass | 第 6、7 节 |
| 隔离 worktree 已创建且 clean | Pass | `%TEMP%\Aerie-P2-G0-b8c9397\source` |
| 冻结源基线未变化 | Pass | 源 commit/tree 继续为 `b8c9397...` / `873f0fe...`，隔离 worktree 未改用 `71b8815` |
| 运行数据与秘密仍排除 | Pass | `.env`、运行 DB、日志、上传和当前运行态文件未进入 `71b8815` |
| 提交原子性 | Accepted deviation | `71b8815` 是 B01 与并行 Mobile/Android 的 mixed commit，按 `DEV-P2-00-001` 追踪 |
| 独立 QA 与范围例外 | Pass with `DEV-P2-00-001` | QA 已复验冻结事实、工具链、哈希、隔离 worktree、端口、Provider-key 和文档内容；提交范围偏差由作者明确接受并要求正常推送 |

## 10. 尚未完成与下一步

本批不证明以下事项，相关 G0 条目保持未完成：

- 当前基线 Python、Node、E2E、性能、安全和包内容门禁是否通过。
- 架构守护工具能否发现平行主链、跨库所有权、Persona 和 Renderer 违规。
- 13 个工作包的原子需求追踪率是否达到 100%。
- 二期 ADR 是否覆盖所有新合同与过渡例外。
- R-01 至 R-17 的触发阈值、状态和责任确认是否完整。
- G0 是否可关闭，以及是否允许进入 P2-01。

下一允许批次是 `P2-00-B02`：在冻结 worktree 中重采 TST/PERF/SEC/包内容证据，并建立 `04_二期测试与证据索引.md`。B01 独立复核完成后，B02 进入 Ready；G0 仍保持 Open。
