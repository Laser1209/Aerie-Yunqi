---
title: 历史凭据处置方案
kind: control
status: completed
tags: [aerie, release, security, history]
---
# 历史凭据处置方案
> [!success] 处置完成
> 2026-07-21 用户确认旧 SiliconFlow/DeepSeek key 已吊销、本地 `.env` 已换新，并明确授权按审计范围重写本地 Git 历史。`Laser1209/Aerie-Yunqi` 首次上传采用普通 push；发布后递归树复核发现根 `logs/` 路径遗漏，用户随后明确授权二次清洗并使用带租约保护的强推完成远端修正。

## 最终结论
- 当前工作区扫描通过：`python tools\scan_provider_key_patterns.py` → `PROVIDER_KEY_SCAN_OK`。
- Git history 扫描通过：`python tools\scan_provider_key_patterns.py --history` → `PROVIDER_KEY_SCAN_OK`，`history_findings=0`。
- 增强历史预检通过：`history_high_risk_paths=0`、`can_close_credential_history_gate=true`。
- GitHub 公开仓库默认分支为 `main`；强推核验点 `78497a0` 的递归树完整，共 2084 项，根 `logs/`、`.env`、私人运行路径和 `>=100,000,000` bytes blob 数量均为 0，`.env.example` 恰有 1 份。
- [[94_发布与安全检查]] 中“疑似凭据未进入文档、提交、历史或日志”已具备关闭证据。

## 清洗前范围
| 类别 | 原提交 | 路径 | 处置 |
|---|---|---|---|
| 旧规划/示例 key 形态 | `90e8a0466dae`、`68b295a39519` | `.trae/documents/OpenCloud_Companion_Development_Plan.md`、`OpenCloud_Companion/.env.example` | 旧 key 已吊销；历史值已替换为脱敏文本 |
| 测试 fixture 假 key 形态 | `54037a9b995f`、`6211dd03ba33` | `electron/tests/world-dashboard-renderer.test.js` | 历史假值已替换为非 provider-key 形态文本 |
| NapCat 私人运行物 | 最早可达于 `90e8a0466dae` | `NapCat/NapCat.Shell/config/`、`NapCat/NapCat.Shell/cache/` | 二维码和账号配置从所有 refs 移除，本地副本恢复后由 `.gitignore` 隔离 |
| 数据与日志 | 多个历史提交 | DB/WAL/SHM、根 `logs/`、`uploads/`、`data/backups/`、`data/audit/` | 首轮移除数据库等运行数据；发布后发现根 `logs/` 中非 `.log` 后缀文件遗漏，二次从所有 refs 清除 38 个历史路径，并改为整目录忽略 |
| 构建产物 | `41a4cee`、`be9bb2e` 等 | `electron/dist*`、`Spotlight/public/*.exe` | 7 个 GitHub 超限 blob 从历史移除；安装包改用 GitHub Releases |

## 误报裁决
- Git pickaxe 候选 `be9bb2e`、`f73948a`、`40ce82b` 的宽松命中来自 SVG 内嵌 data URI 或被更长标识包裹的子串，严格 provider-key 边界命中为 0，未修改对应资源内容。
- 扩展 Gitleaks 清洗后剩余 7 条人工确认误报：5 条来自 vendored NapCat 固定代码标识，2 条来自 `YOUR_API_KEY` 文档占位符；未发现额外可用凭据。

## 实际执行
1. 在 Provider 后台吊销旧 key，并确认新 key 仅保存在 Git 忽略的根目录 `.env`。
2. 在仓库外创建完整 Git bundle、refs/HEAD 记录、未提交文档补丁，以及 `.env`、数据库、NapCat 私人配置和本机构建产物备份；备份未进入仓库。
3. 安装隔离的 `git-filter-repo 2.47.0`，在独立 mirror 中使用 `--sensitive-data-removal` 预演，再从清洗 mirror 创建普通 clone 验证。
4. 使用与受控扫描器一致的严格规则替换已确认的 `sk-` 形态值，并从三分支全部历史移除公开发布不应包含的运行数据和构建产物。
5. 预演通过后对本地仓库执行同一命令；`git filter-repo` 未删除 `.git`，未执行 filter-branch，未执行 force push。
6. 本地 `.env`、数据库、NapCat 配置和四套构建目录从校验备份恢复，内容一致且均被 Git 忽略。
7. 将清洗后的 `Aerie-Model-X` 以普通 push 首次发布为远端 `main`，远端初始 SHA 为 `2f44e28`。
8. 首次发布后的 GitHub 递归树复核发现根 `logs/` 仍跟踪 26 个诊断文件；进一步历史审计确认 38 个路径中含 QQ `user_id`、本机绝对路径和测试聊天内容，但没有新增 API key。用户明确授权清理 `logs` 并执行 `force-with-lease`。
9. 在受限备份目录保存首次发布状态的完整 bundle、101 个本地日志文件及用户未提交文档副本；新增 `/logs/` 整目录忽略和高风险历史路径预检。隔离 mirror 预演确认三个分支排除 `logs/` 后的树指纹完全一致，历史与对象库中的 `logs/` 均为 0。
10. 正式二次清洗后，使用显式租约 `refs/heads/main:2f44e284...` 将 `Aerie-Model-X` 更新为远端 `main`；GitHub 返回 forced update 成功，随后通过远端引用、REST 仓库元数据和完整递归树交叉复核。

## 分支映射
- 首轮清洗前 `Aerie-Model-X` HEAD：`4f6cb00`；首轮清洗与安全记录完成后首次发布 SHA：`2f44e28`。
- 根 `logs/` 二次清洗后 `Aerie` HEAD：`6fb5658`。
- 根 `logs/` 二次清洗后的防复发提交：`9cb03ba`；刷新精确 Gitleaks 误报指纹后的远端核验点：`78497a0`。
- 本地 `main` 未包含根 `logs/` 历史，HEAD 保持 `7324fec`；该分支未上传。

## 验证证据
- `python tools\scan_provider_key_patterns.py --history` → `PROVIDER_KEY_SCAN_OK`。
- `python tools\history_remediation_preflight.py --json` → `history_findings=0`、`history_high_risk_paths=0`、`can_close_credential_history_gate=true`。
- `python -m pytest tests -q` → `559 passed, 6 warnings`。
- `node --test electron/tests/*.test.js` → `25 passed`。
- `npm run check:all` → emoji/forbidden/token 三项通过。
- Gitleaks 8.30.1 → `99 commits scanned`、`no leaks found`；历史重写导致 2 条 `YOUR_API_KEY` 占位符误报指纹变化，复核后仅更新对应精确指纹，其余 5 条 NapCat 误报指纹不变。
- `git fsck --full --strict` → 通过；GitHub `>=100,000,000` bytes blob 数量为 0。
- 三分支排除 `logs/` 后的树条目数和 SHA-256 指纹在预演前后完全一致，业务源码未改变。
- GitHub `main` 在核验点 `78497a0` 与本地一致；递归树 `truncated=false`，根 `logs/`、`.env`、DB/WAL/SHM、NapCat 私人配置、构建目录、上传/备份/审计和 pytest 产物均为 0。
- Git pack 由约 `589.48 MiB` 降至约 `108.37 MiB`。

## 回滚与备份
- 清洗前 bundle、未提交文档补丁和本地私人文件备份位于仓库外，不得上传 GitHub。
- 二次清洗前 bundle、101 个本地日志和用户文档副本位于受限目录 `E:\Aerie-history-remediation-backup-20260721\post-publish-logs-gap`，同样不得上传。
- 若本地验证失败，可从 bundle 恢复旧 refs；旧 bundle 含已吊销 key 和私人历史，只能本地受控使用。
- 不得把任何旧 bundle、旧分支或已清除的提交重新推回远程；若再次发现历史敏感数据，必须先关闭发布门禁并取得新的明确远端改写授权。

## 发布裁决
- 已完成 `清洗并轮换`，凭据历史门禁关闭。
- 公开仓库 `Laser1209/Aerie-Yunqi` 已发布，默认分支为 `main`；只上传清洗后的 `Aerie-Model-X`，本地 `Aerie` 与 `main` 未上传。
- 首次普通 push 后仅因根 `logs/` 隐私路径遗漏执行了一次用户明确授权的 `force-with-lease`，租约固定旧远端 SHA，未使用无条件 `--force`。
- 不创建初始 PR，不使用 Git LFS，不上传安装包；安装包仍应单独通过 GitHub Releases 发布。
