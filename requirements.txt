# Aerie · 云栖 v9.0 — Python Backend Dependencies
# Code-level elements (package names) must use pure English
#
# Install: pip install -r requirements.txt
# Important: psutil / fastapi / uvicorn are REQUIRED; if missing,
# the Electron app will show "后端启动失败" in the UI.

# HTTP API server (FastAPI app)
fastapi>=0.110
uvicorn[standard]>=0.27

# Core async & HTTP
aiohttp>=3.9
websockets>=12.0

# Process / system monitoring
psutil>=5.9

# Logging & utilities
loguru>=0.7
pyyaml>=6.0
python-dotenv>=1.0

# Scheduler (auto-wake core)
apscheduler>=3.10

# AI / LLM (multi-provider)
openai>=1.10

# HTTP client
requests>=2.31
httpx>=0.25

# Windows integration (UAC + Task Scheduler + screenshot)
pywin32>=306 ; sys_platform == "win32"
pyautogui>=0.9.54 ; sys_platform == "win32"

# Optional: testing
pytest>=8.0
pytest-asyncio>=0.23
