
Aerie 全面升级只读深度探索报告
已完成对 E:\Agent_reply 的全面只读探索：六份升级方案均已覆盖，实际代码已从 Electron、FastAPI、Pipeline、Context Builder、Brain、Database、Companion、Persona、主动消息、图片上传和世界模拟等方向交叉核验。以下结论基于当前工作区真实文件，而不是仅依据方案文档推断；本次未修改或创建任何项目文件，未启动应用，未运行测试或破坏性命令。

行号范围依据当前文件内容整理；后续文件发生改动后可能产生偏移。

1. 执行摘要
   Aerie 当前不是空白项目。它已经具备较完整的本地 AI 伴侣基础设施：

Electron 桌面端与 Python 后端生命周期
FastAPI API
QQ/NapCat 通信
多模型降级与 ReAct 工具调用
PAD 情绪系统、DesireEngine、认知轨迹
SQLite 聊天、记忆、知识、情绪、推送、工具调用等数据
Persona Hub
基础附件上传和文档 Markdown 提取
Cron 主动消息、主动判断器和事件引擎代码
stderr、轮询与 SSE 多种实时消息通道
但六份方案的核心升级能力大多仍处于“设计完整、实现不完整”状态，主要缺口集中在：

对话仍以消息行而非会话/轮次为中心
前端 _loading 锁阻止连续输入，没有请求队列、取消和真实流式输出
长期记忆和知识库存在，但未真正接入主 Context Builder
主动消息组件已经存在，却有多处 P0 级接口和生命周期断点
图片目前只是通用附件，不是完整的图片资产、理解和生成系统
Persona Hub 与旧 Persona 配置并存，身份真源未统一
24 小时世界模拟、关系引擎、图片决策和 Sidecar 插件体系尚未实现
数据库迁移仍是内联补列，不适合承载后续大规模模型升级
推荐不要按六份文档分别开发。应先合并为一个主路线，并按以下主链实施：

2. 六份升级方案逐份复核
   2.1 对话系统全面升级方案
   文件：

E:\Agent_reply\documents\Level_up\Aerie_v14_对话系统全面升级方案.md，约第 26–1261 行。

目标
将当前同步、消息行驱动的聊天系统升级为：

会话与轮次驱动
支持连续输入、排队、取消和重试
支持流式输出和拟人化多气泡节奏
支持主题跟踪、滚动摘要和 Token 预算
接入长期记忆和知识检索
修复主动消息主链
建立用户反馈与策略自适应机制
核心功能
Conversation、Turn、Request、Response Group 实体
请求状态机
流式事件
中断和合并输入
Token 预算上下文
多气泡语义切分和延时投递
主动消息三通道投递
用户反馈和自适应频率
数据模型
主要新增字段：

当前 chat_log 不具备这些实体关系，因此不是简单补 UI 字段即可完成。

API
方案建议：

实施阶段
迁移准备、Feature Flag、测试基线
主动消息 P0
会话模型和异步队列
流式输出与拟人节奏
反馈闭环和高级主动行为
兼容性、性能和回滚验证
依赖
正式数据库迁移机制
稳定的事件传输协议
统一用户与会话身份
Pipeline 增量改造，而非重写
Renderer 聊天状态机改造
与其他方案的关系和冲突
大量覆盖“不受限制对话方案”和“拟人化对话方案”
主动消息部分与独立主动消息方案重复
早期世界相关假设已被最新 Sidecar 世界方案替代
文档倾向引入 ContextBuilder v2，但从实际代码看，更安全的方式是保留现有 Context Builder 并逐步扩展
验收重点
即使助手上一轮被拆成十段，下一轮仍能读到完整用户—助手轮次
中断响应不超过 500 ms
首 Token 不超过 1 秒
Typing 状态在 100 ms 内出现
主动消息可同时到达 QQ、本地气泡和系统通知
用户可配置主动频率及拟人节奏
2.2 Agent 主动发消息方案
文件：

E:\Agent_reply\documents\Level_up\Aerie_Agent主动发消息方案.md，约第 22–643 行。

目标
修复现有主动消息链路，并扩展为：

Cron 定时触发
空闲、情绪、天气、日历、Todo、纪念日事件触发
QQ、本地气泡和系统通知三通道投递
按场景限流
用户反馈驱动策略调整
数据与接口
建议新增主动反馈记录，例如：

反馈动作包括：

实施阶段
P0：修复投递、启动事件引擎、修复重复配置
P1：接入空闲、情绪、天气、日程和记忆事件
P2：反馈闭环和自适应限流
P3：节奏、打扰等级和设置 UI
当前代码对应结论
方案对现状的判断基本准确，而且代码中还存在更直接的 P0 故障：

PushEventEngine 没有接入 Companion
多处调用不存在的 PushScheduler.trigger_scene()
Companion.desire 与 ProactiveJudge.desire_engine 命名不一致
idle_care 在 YAML 中重复定义
主动 QQ 消息未统一进入本地聊天事件合同
无持久反馈闭环
验收重点
同一主动消息可抵达三通道
QQ 离线时暂停或合理降级
安静时段不发送非豁免消息
不重复发送早安等场景
长时间空闲后自动关怀
用户负反馈可降低对应场景频率
2.3 图片上传与管理完整解决方案
文件：

E:\Agent_reply\documents\Level_up\Aerie_图片上传与管理完整解决方案.md，约第 18–1333 行。

目标
把当前通用附件上传扩展为完整图片子系统：

图片上传、校验、压缩和存储
图片理解、OCR 和视觉问答
AI 图片生成
Emoji 管理和回复
主动图片分享
图片历史、检索、去重和清理
数据模型
方案建议新增：

核心元数据包括：

文件哈希
MIME
宽高
大小
来源
所有者
创建时间
安全检查状态
缩略图
生成模型和 Prompt
关联消息或会话
接口和组件
建议主要组件：

实施阶段
基础图片接收、发送和理解
AI 图片生成
Emoji 支持
主动图片分享
历史、搜索、优化和清理
当前代码对应结论
当前只实现了“通用附件”的一部分：

已有 20 MB 限制
已有 MIME 白名单
已有 UUID 文件名
已有静态上传目录
已有拖放、粘贴和文件选择
已有文档 Markdown 提取与缓存
已有附件 JSON 持久化
尚未实现：

图片魔数和完整格式校验
宽高、EXIF 清理、重压缩、缩略图
图片元数据表和所有权
图片资产去重、搜索和垃圾回收
视觉理解
实际图片生成
Emoji 资产库
主动图片决策
与最新世界方案的冲突
图片方案倾向于让一个 ImageHandler 同时负责决策和副作用。最新世界方案则要求：

后者应作为最终架构：图片资产和生成能力属于 Core，世界状态驱动的图片选择属于 aerie.world。

2.4 拟人化对话模式研究与优化方案
文件：

E:\Agent_reply\documents\Level_up\Aerie_拟人化对话模式研究与优化方案.md，约第 21–548 行。

目标
让回复从“模型一次返回文本”升级为更接近真人聊天的交互：

根据内容切分气泡
控制气泡节奏
稳定 Typing 指示
支持连续输入、打断和消息合并
适配用户情绪和对话能量
识别话题延续和自然结束点
核心组件
MessageChunker
PacingEngine
TopicTracker
EndingDetector
Request/Turn 状态机
Conversational Energy Matching
状态建议：

当前代码对应结论
方案中“当前系统只有单气泡”的描述不准确。

实际 Pipeline 已进行语义切分，并把多个助手片段分别持久化。真正缺少的是：

轮次级分组
逐段可感知投递
首 Token 流式事件
可取消请求
稳定 Typing 状态
用户连续输入合并
节奏与打断状态机
因此应复用现有切分逻辑，不应重新实现一套互不兼容的 MessageChunker。

验收重点
“好的”得到短回复
情绪支持先共情再建议
三条快速连续输入能合并或有序处理
生成中允许打断
多气泡逐步出现而非瞬间全部渲染
Typing 状态不闪烁
首 Token 不超过 1 秒
2.5 不受限制对话模式二次开发方案
文件：

E:\Agent_reply\documents\Level_up\Aerie_不受限制对话模式二次开发方案.md，约第 17–286 行。

目标定义
这里的“不受限制”不是绕过安全限制，而是：

不受单轮问答限制
不因 UI 分段造成上下文丢失
允许连续输入
在模型有限窗口内使用摘要、检索和 Token 预算保持长期连续性
根因判断
文档指出的根因与实际代码一致：

Renderer 全局 _loading 锁
/api/chat/send 同步请求
历史按消息行截断，而不是按轮次截断
多个助手片段会挤掉用户消息
缺少 Conversation、Turn、Request
Memory 和 Knowledge 没有真正注入
用户身份存在漂移风险
System Prompt 合并合同不够清晰
推荐最小路线
与 v14 的关系
该文档可视为 v14 对话升级中的“数据正确性和连续输入专项”。不宜作为独立平行项目，否则会重复修改：

chat_log
Pipeline 历史读取
/api/chat/send
Renderer 状态
Context Builder
2.6 24 小时世界模拟与人格图片系统实施计划
文件：

E:\Agent_reply\documents\Level_up\2026-07-20_Agent_24小时世界模拟与人格图片系统实施计划.md，约第 28–2488 行。

目标
建立独立、确定性、可恢复的 24 小时角色世界：

不依赖 LLM Tick 的确定性模拟
有限动作注册表
双向关系状态
计算型 SelfModel
人格图片候选决策
独立 world.db
可靠事件 Outbox
WorldPort 抽象
先 In-process Adapter，后 Remote Sidecar
Electron 负责插件进程监管和权限代理
核心接口
目标模块
当前代码对应结论
以下能力均未找到实际实现：

WorldSimulation
RelationshipEngine
SelfModel
ActionRegistry
ImageDecision
WorldPort
RemoteWorldAdapter
world_state_snapshot
relationship_state
image_decision_log
PluginSupervisor
CapabilityBroker
aerie.world
因此这是全新的子系统，不是对现有世界模块的升级。

架构优先级
该文件日期最新，并明确推翻了旧设想。下列旧路线不应再采用：

把世界表写入 aerie.db
把世界引擎直接长期挂到 Companion
Renderer 直接请求 Sidecar
世界逻辑直接执行图片生成或主动消息副作用
应以以下路线为准：

3. 实际代码架构
   3.1 启动链
   主要文件：

E:\Agent_reply\main.py
E:\Agent_reply\electron\src\main.js
E:\Agent_reply\core\companion.py
启动流程：

Companion 是后端组合根，负责构造和维护：

Database
Brain
EmotionEngine
LongTermMemory
KnowledgeBase
CognitionEngine
ToolRegistry
QQClient
Router
SendQueue
Pipeline
PushScheduler
ProactiveJudge
DesireEngine
3.2 消息通道
当前存在三条相关通道：

现有 SSE 更适合作为 Brain Center 等遥测通道，尚不能直接作为可靠会话流或 Sidecar 事件总线，因为它缺少：

持久序号
断线续传
ACK
Outbox
重放
At-least-once 语义
3.3 Pipeline
关键文件：

E:\Agent_reply\core\pipeline.py

实际流程已经包含：

因此升级时必须增量封装，而不能用简化版聊天 Pipeline 整体替换，否则容易破坏工具、认知、Office Mode、屏幕控制、输出校验、QQ 和自我进化链路。

3.4 Context Builder 与 Brain
关键文件：

E:\Agent_reply\core\context_builder.py
E:\Agent_reply\core\brain.py
Context Builder 已注入：

核心身份
关系深度
语言规则
表达自由度
当前时间
引用内容
附件 Markdown
PAD 情绪
情绪爆发模式
有限近期历史
但构造器虽然接收 LongTermMemory 和 KnowledgeBase，当前主 build() 路径没有把检索结果真正写入模型上下文。

Brain 已支持：

多 Provider 降级
OpenAI-compatible completion
ReAct 工具调用
最多六轮工具推理
主动内容生成
图像、TTS、Vision 等接口表面
图片生成和视觉理解仍返回 status: "stub"，不属于可用实现。

4. 能力实现状态矩阵
   能力	状态	主要依据
   Electron 启动 Python 后端	已实现	E:\Agent_reply\electron\src\main.js、E:\Agent_reply\main.py
   同步聊天	已实现	E:\Agent_reply\core\api_server.py、E:\Agent_reply\core\pipeline.py
   助手语义分段	已实现	E:\Agent_reply\core\pipeline.py
   Conversation/Turn/Request 模型	未实现	chat_log 仍为消息行模型
   连续输入和请求队列	未实现	E:\Agent_reply\electron\src\renderer\js\chat.js 中全局 _loading
   取消、重试、打断	未实现	无 Request 状态和取消 API
   Token 流式输出	未实现	当前聊天主要为完整结果事件和轮询
   SSE 基础设施	部分实现	E:\Agent_reply\core\event_stream.py
   可靠事件重放	未实现	无序号、ACK、Outbox
   多气泡拟人节奏	部分实现	已切分，但缺真实逐段流式状态机
   Typing 状态机	未实现或极弱	无完整 Request/Turn 状态
   TopicTracker	未实现	未找到实际模块
   滚动摘要	未实现	无会话摘要实体和更新链
   长期记忆持久化	已实现	E:\Agent_reply\core\database.py
   长期记忆主上下文注入	部分实现	接口存在，主 build 路径未完整接入
   知识库存储	已实现	E:\Agent_reply\core\database.py
   知识检索主上下文注入	部分实现	同上
   多模型降级和 ReAct	已实现	E:\Agent_reply\core\brain.py
   Persona Hub CRUD/导入导出	已实现	E:\Agent_reply\core\persona_hub\persona_manager.py
   Persona 单一真源	未实现	Persona Hub 与 config\persona.yaml 并存
   通用文件上传	已实现	E:\Agent_reply\core\api_server.py
   文档转 Markdown	已实现	E:\Agent_reply\core\attachment_handler.py
   纯附件消息	部分实现且合同冲突	UI 允许空文本，后端聊天接口不接受
   图片资产管理	未实现	无 images 元数据与生命周期
   图片理解	Stub	E:\Agent_reply\core\brain.py
   图片生成	Stub	E:\Agent_reply\core\brain.py
   Emoji 资产库	未实现	未找到对应表和服务
   Cron 主动调度	已实现	E:\Agent_reply\core\push_scheduler.py
   安静时段和全局限流	已实现	E:\Agent_reply\config\proactive.yaml
   ProactiveJudge	部分实现	存在，但 DesireEngine 引用名错误
   DesireEngine	部分实现	存在，但调用不存在的调度方法
   PushEventEngine	代码存在但未启用	未接入 Companion 生命周期
   主动消息三通道投递	未实现	QQ、本地和系统通知未统一
   主动反馈与自适应	未实现	无反馈 API 和持久策略
   确定性世界模拟	未实现	无对应代码
   关系引擎	未实现	无对应代码
   SelfModel	未实现	无对应代码
   图片候选决策	未实现	无对应代码
   WorldPort	未实现	无对应代码
   World Sidecar	未实现	无对应代码
   插件监管和权限代理	未实现	无 PluginSupervisor/CapabilityBroker
   世界仪表盘	未实现	无对应 Renderer 模块
   Creative Workshop	未实现	无对应实现
5. 已确认的 P0 缺陷
   5.1 PushScheduler 公共方法不匹配
   E:\Agent_reply\core\push_scheduler.py 约第 446–505 行只暴露：

但以下调用方使用不存在的高层方法 trigger_scene()：

E:\Agent_reply\core\api_server.py:2166
E:\Agent_reply\core\desire_engine.py:230
E:\Agent_reply\core\push_event_engine.py:299
E:\Agent_reply\core\companion.py:524
这不是单一事件引擎问题，而是主动消息多个入口共同失效的公共接口问题。

5.2 DesireEngine 属性名不一致
E:\Agent_reply\core\companion.py 约第 215 行：

self.desire = DesireEngine(self, self.behavior_cfg)
E:\Agent_reply\core\proactive_judge.py 约第 196–197 行却查询：

getattr(self.companion, "desire_engine", None)
self.companion.desire_engine.get_state()
结果是 ProactiveJudge 很可能无法使用真实 DesireEngine 状态，只能退回默认值。

5.3 PushEventEngine 未接入生命周期
E:\Agent_reply\core\companion.py 中未看到：

因此事件引擎即使已有实现，也不会正常运行。

5.4 idle_care 配置重复
E:\Agent_reply\config\proactive.yaml 约第 1–118 行中，idle_care 定义两次。

后一个 YAML 映射覆盖前一个，导致前一处的：

丢失。

5.5 上传前后端合同不一致
E:\Agent_reply\electron\src\renderer\js\chat-uploader.js：

前端允许 WEBP、压缩包、音频、视频、EXE、APK 等
后端 /api/upload 白名单并不完整接受这些类型
注释称应走 IPC，实际直接请求固定地址 127.0.0.1:7890
无进度、取消和后端限制同步
E:\Agent_reply\electron\src\renderer\js\chat.js：

UI 允许纯附件消息
后端聊天接口仍要求文本
附件 URL 可能出现 /uploads/uploads/...
根相对路径在 file:// 页面下存在兼容风险
6. 方案重复、冲突与裁决
6.1 重复内容
v14 与不受限制对话方案
共同覆盖：

Conversation/Turn
连续输入
Token 预算
历史轮次修复
滚动摘要
Memory/Knowledge 接入
队列、取消和流式输出
裁决：以 v14 为主控，不受限制方案作为“会话正确性专项验收”。

v14 与拟人化对话方案
共同覆盖：

多气泡
Pacing
Typing
打断
快速连续输入
用户对话能量适配
裁决：拟人化方案作为 v14 的 Renderer/Pacing 子计划，不独立重建后端模型。

v14 与主动消息方案
共同覆盖主动消息修复与反馈闭环。

裁决：主动消息方案承担 P0/P1 详细任务，v14 只保留里程碑和集成验收。

图片方案与世界图片系统
共同覆盖“角色主动发图片”，但职责不同。

裁决：

Core Image Service：
上传、验证、压缩、元数据、生成、发送、安全、清理

aerie.world ImageDecision：
根据世界状态生成候选，不执行真实副作用
6.2 明确冲突
世界部署方式冲突
旧路线：

WorldEngine 直接挂 Companion
世界表写入 aerie.db
Renderer 通过通用 API 访问
新路线：

裁决：以 2026-07-20 最新方案为准。

Context Builder 重建还是演进
部分文档倾向建立 ContextBuilder v2。实际 Context Builder 已承载 Persona、情绪、引用、时间、附件和系统操作约束。

裁决：保留现有入口，内部增加结构化 Context Sections、Token Budget、Retriever；避免双 Builder 长期并存。

消息分段是否重做
拟人化文档暗示当前没有分段，但 Pipeline 已实现语义切分。

裁决：复用切分规则，新增 response_group_id、sequence 和 Pacing，不重造重复算法。

7. 迁移风险
   7.1 Chat 行模型迁移
   从 chat_log 消息行升级到 Conversation/Turn 后，需要处理：

旧消息没有 conversation_id
UI 与 QQ 短期历史是否隔离
多个助手分段如何归并为同一 Turn
旧附件 JSON 如何迁移
旧消息顺序是否稳定
中断请求如何标记部分完成
是否保留原 chat_log 兼容视图
建议采用“新增实体和列、双读、回填、双写、切读、停止旧写”的渐进方式，避免一次性破坏历史数据。

7.2 双投递和重复消息
当前聊天可同时通过：

stderr IPC
/api/chat/poll
SSE
新流式协议上线后若不先定义事件 ID 和去重规则，容易出现：

同一消息重复渲染
完成事件早于最后一个片段
取消后仍收到旧结果
Renderer 重连后乱序
7.3 Persona 真源分裂
当前至少存在：

E:\Agent_reply\config\persona.yaml
E:\Agent_reply\data\personas\*.json
E:\Agent_reply\data\personas\_active.json
风险是 UI 显示一个人格，而 Context Builder 使用另一个人格。

应确认 Persona Hub 为唯一真源；旧 Persona API 仅作为兼容投影。

7.4 用户 ID 漂移
DesireEngine 和 ProactiveJudge 使用 emotion.get_state(0)，而真实 Master 用户可能不是 0。

引入关系、世界和主动反馈前必须统一：

否则情绪、关系、记忆、主动消息可能归属错误。

7.5 图片资产迁移
当前上传文件只是目录文件和消息附件 JSON。转为正式图片资产后需解决：

旧文件如何补元数据
SHA 去重是否改变现有 URL
文件删除时如何检查消息引用
缩略图和原图路径
EXIF 清理是否重写原文件
上传目录是否迁移到 AERIE_DATA_DIR
7.6 数据库迁移机制不足
E:\Agent_reply\core\database.py 使用建表和内联补列，没有正式：

schema version
migration ledger
rollback 标记
数据回填状态
校验和
失败恢复
在 Conversation、Image、Plugin 和 World 数据升级前，应先建立版本化迁移器。

7.7 aerie.db 与 world.db
Core 不应跨库直接联表或读取插件私有表，否则会破坏 Sidecar 隔离。

建议：

aerie.db：聊天、记忆、知识、主动反馈、Core 图片资产
world.db：世界状态、关系状态、动作日志、SelfModel、Outbox、图片候选
交互只能通过 WorldPort 合同
7.8 Sidecar 可靠性
当前 SSE 只有最佳努力语义，无法支撑世界事件可靠传递。

Sidecar 至少需要：

稳定 event_id
单调序列或游标
Outbox
ACK
重连续传
幂等消费
心跳
协议版本握手
进程崩溃恢复
8. 推荐实施顺序
Phase 0：建立基线与迁移安全网
建立 schema version 和迁移记录
固化关键现状测试合同
定义 Feature Flag
定义事件 ID、请求 ID、会话 ID、用户 ID
不改变现有用户体验
Phase 1：主动消息 P0
优先修复：

PushScheduler.trigger() 公共接口统一
Companion.desire 命名统一
PushEventEngine 生命周期
用户活动记录
idle_care 重复配置
主动消息本地事件投递
QQ 离线和安静时段行为
这是最新世界方案明确要求的前置步骤。

Phase 2：Persona 与身份合同
Persona Hub 成为唯一真源
旧 Persona API 作为兼容投影
统一用户、Channel、Persona 标识
清除 user_id=0 隐式假设
Phase 3：Conversation / Turn / Request 模型
新增 Conversation、Turn、Message、Request 或等价实体
回填历史数据
给现有助手片段增加 Response Group
改为按完整轮次读取历史
保持旧接口兼容
Phase 4：异步队列、取消和统一事件协议
去除 Renderer 全局 _loading 限制
引入请求状态机
支持排队、取消、重试
统一聊天事件 ID 和去重
再逐步降低轮询依赖
Phase 5：Context、Memory、Knowledge
Token Budget
最近完整轮次
滚动摘要
长期记忆检索
知识库检索
引用和附件内容
可审计的最终模型输入
Phase 6：拟人化流式输出
首 Token 事件
Typing 状态
复用现有语义切分
response_group_id + sequence
Pacing
中断
快速输入合并
情绪和对话能量适配
Phase 7：图片资产基础设施
统一上传合同
图片魔数、尺寸、EXIF、重压缩
Image Asset 元数据
SHA 去重
缩略图
安全路径和所有权
删除、引用检查和垃圾回收
视觉和生成 Provider 接口
Phase 8：Plugin Host 与 WorldPort
插件 Manifest
权限模型
WorldPort
In-process Adapter
专用 Electron preload 合同
暂不引入远程 Sidecar 的复杂运行时
Phase 9：确定性世界领域模型
ActionRegistry
WorldSimulation
RelationshipEngine
SelfModel
Regulator
模拟时钟
确定性重放测试
Phase 10：独立持久化与 Sidecar
world.db
Outbox
协议握手
Remote Adapter
Electron PluginSupervisor
CapabilityBroker
故障恢复和版本兼容
Phase 11：图片候选和主动消息融合
World 仅发布 image_candidate
Core 负责生成、安全判断和发送
接入 ProactiveJudge 和用户反馈
建立候选、审批、执行和结果审计链
Phase 12：World Dashboard 与 Creative Workshop
世界摘要
关系状态
动作时间线
图片候选
插件安装、升级、回滚
权限审查
崩溃和降级 UI
9. AI Vibe Coding 文档包建议
建议未来实际创建以下文档，但本次按只读要求未写入。

9.1 主控文档
建议路径：

E:\Agent_reply\documents\Level_up\AI_Vibe_Coding\00_Aerie_全面升级主控计划.md

内容：

唯一架构基线
术语表
全局目标与非目标
权威方案优先级
阶段依赖图
数据所有权
API 和事件版本策略
Feature Flag
风险登记
决策索引
每阶段完成状态
9.2 阶段任务文档
每份任务文档应固定包含：

9.3 验收清单
建议路径：

E:\Agent_reply\documents\Level_up\AI_Vibe_Coding\90_全局验收清单.md

按能力分组：

数据迁移
多轮对话
连续输入
取消和重试
流式事件
上下文和记忆
Persona 一致性
主动消息
图片资产
世界模拟
Sidecar 故障恢复
Electron 权限边界
回滚与兼容性
每项应包含：

9.4 决策记录
建议目录：

E:\Agent_reply\documents\Level_up\AI_Vibe_Coding\decisions\

建议至少建立：

9.5 Obsidian Base 看板
建议路径：

E:\Agent_reply\documents\Level_up\AI_Vibe_Coding\Aerie升级任务.base

建议字段：

字段	用途
task_id	稳定任务编号
title	任务标题
phase	所属阶段
subsystem	chat/proactive/persona/image/world/plugin
status	backlog/ready/doing/review/blocked/done
priority	P0/P1/P2
dependencies	依赖任务
risk	low/medium/high
decision_required	是否待用户决策
feature_flag	对应开关
migration	是否涉及迁移
files	影响文件
acceptance_ids	关联验收项
rollback_ready	是否具备回滚
owner	执行者
evidence	PR、日志、截图或测试结果
建议视图：

按阶段看板
P0 阻塞项
待用户决策
数据迁移任务
高风险任务
世界插件任务
验收未通过
可回滚状态
10. 需要用户确认的关键决策
在进入实现前，以下决策不能由编码 Agent 自行假设。

10.1 对话数据模型
选择：

在现有 chat_log 上增量增加 Conversation/Turn 字段
新建规范化 Conversation、Turn、Message、Request 表并保留兼容视图
建议选择 2，但需要确认历史数据兼容期限。

10.2 Channel 会话隔离
需确认：

桌面 UI 与 QQ 默认共享短期会话，还是独立会话
长期记忆是否跨 Channel
同一用户如何完成 QQ 身份和桌面身份绑定
10.3 Persona 唯一真源
需确认 Persona Hub 是否正式成为唯一真源。

建议：

10.4 流式协议
需决定：

继续基于 SSE
使用 WebSocket
Core 聊天用 SSE，世界 Sidecar 使用独立可靠协议
建议第三种：不要强行让现有 SSE 承担 Sidecar Outbox 语义。

10.5 主动消息投递策略
需确认：

三通道是同时投递还是按优先级降级
系统通知是否默认开启
QQ 离线时消息是否排队
每场景频率和全局日上限
用户是否可永久静音单一场景
10.6 图片后端
需确认：

本地模型优先还是远程 API 优先
允许的图片格式和最大体积
是否保存原图
是否默认移除 EXIF
生成图片保留周期
是否允许用户关闭主动图片分享
10.7 世界插件边界
需确认：

第一版是否先 In-process 验证，再迁移 Sidecar
Sidecar 是否必须独立发布和升级
world.db 是否允许用户导出
Core 是否保存世界摘要缓存
世界插件停机时聊天如何降级
建议采用文档中的渐进路线：先 WorldPort + In-process Adapter，再切 Remote Adapter。

10.8 关系模型对用户可见程度
需确认：

完整数值是否展示
只展示自然语言摘要
是否允许用户重置关系
Persona 切换时关系共享、继承还是隔离
10.9 “不受限制”的产品边界
需明确继续采用文档中的定义：

10.10 旧 API 的兼容期
需确定旧接口，例如 /api/chat/send、旧 Persona API 和轮询接口：

立即替换
保留一个版本周期
长期兼容
建议保留一个可观测迁移周期，并通过 Feature Flag 切换。

11. 关键代码与配置索引
    本次实际阅读和核验的关键文件远超要求的八个，核心索引如下：

E:\Agent_reply\main.py
E:\Agent_reply\electron\src\main.js
E:\Agent_reply\electron\src\preload.js
E:\Agent_reply\electron\src\renderer\js\chat.js
E:\Agent_reply\electron\src\renderer\js\chat-uploader.js
E:\Agent_reply\core\api_server.py
E:\Agent_reply\core\companion.py
E:\Agent_reply\core\pipeline.py
E:\Agent_reply\core\context_builder.py
E:\Agent_reply\core\brain.py
E:\Agent_reply\core\database.py
E:\Agent_reply\core\attachment_handler.py
E:\Agent_reply\core\chat_events.py
E:\Agent_reply\core\event_stream.py
E:\Agent_reply\core\push_scheduler.py
E:\Agent_reply\core\push_event_engine.py
E:\Agent_reply\core\proactive_judge.py
E:\Agent_reply\core\desire_engine.py
E:\Agent_reply\core\persona_hub\persona_manager.py
E:\Agent_reply\communication\router.py
E:\Agent_reply\config\persona_loader.py
E:\Agent_reply\config\persona.yaml
E:\Agent_reply\config\persona_behavior.yaml
E:\Agent_reply\config\proactive.yaml
E:\Agent_reply\config\settings.yaml
最终判断
六份文档提出的方向整体一致，但不能直接作为六条并行开发线。当前最关键的工程策略是：

先修复已经存在但失效的主动消息链路
先建立身份、迁移和事件合同，再改聊天体验
保留现有复杂 Pipeline，以实体模型和异步外壳增量升级
把图片基础能力与世界图片决策分层
以最新 Sidecar 世界方案覆盖旧的直接集成设想
所有大改动都必须具备 Feature Flag、回填、兼容读写和回滚路径
在这些约束下，Aerie 的全面升级具备可执行基础；若跳过 P0、身份和迁移层，直接同时推进流式聊天、图片和世界 Sidecar，则极易造成消息重复、上下文错乱、人格漂移、主动消息失效和跨数据库耦合。
