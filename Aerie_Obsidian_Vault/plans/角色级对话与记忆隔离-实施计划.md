---
title: 角色级对话与记忆隔离 - 实施计划
date: 2026-08-13
tags:
  - plan
  - implementation
  - persona
  - database
  - memory
aliases:
  - 角色级隔离实施计划
  - Persona-Scoped Isolation Implementation Plan
status: planned
cssclasses:
  - doc-plan
---

# 角色级对话与记忆隔离 — 实施计划

> **For agentic workers:** 本计划由多个子代理并联开发。每个门户（Portal）是一个独立工作单元；门户内任务用 `- [ ]` 勾选追踪。开发按「门户 → 里程碑审查 → 下一门户」推进，门户之间允许并联（依赖见下）。

**Goal（目标）**：为对话历史、长期记忆、上下文检索、摘要分桶、跨端时间线新增 `persona_id`（角色）维度，使切换人设后历史/记忆/召回全部跟随激活角色；存量数据共享兼容、可回滚。

**Architecture（架构）**：基于现有 `actor_id`（多端账户）维度扩展正交的 `persona_id` 维度。会话 ID 哈希纳入 `persona_id`（复用 `_resolve_related_conversation_ids` 的 NULL 兼容候选模式）；记忆 DTO/层接口/ChromaDB metadata 增加 `persona_id`；读取统一 `persona_id = :active OR persona_id IS NULL`（共享兼容模式）。依赖 [[plans/角色级对话与记忆隔离技术方案|技术方案]] 的 D1-D6 决策。

**Tech Stack（技术栈）**：Python 3.10+ / SQLite / ChromaDB / FastAPI / Electron renderer（chat.js）。

---

## 0 任务门户体系总览 / Portal Map

### 0.1 门户矩阵（阶段 × 模块）

| 门户 | 阶段 | 功能模块 | 交付物 | 依赖 |
| --- | --- | --- | --- | --- |
| **A** | P0 | 数据库迁移 `013_persona_scoped_dialogue_memory` | 7 表加列 + 索引 + 迁移注册 | 无 |
| **B** | P0 | 写链隔离 | conversation 哈希/落库/chat_log 直写带 persona | A |
| **C** | P1 | 读链隔离 | 历史查询（canonical + legacy + poll）按 persona 过滤 | A |
| **D** | P1 | 记忆链隔离 | MemoryItem/层接口/SQLite/Chroma/context_builder | A |
| **E** | P1 | 前端切换联动 | chat.js 在 `persona:changed` 时重载历史 | C |
| **F** | P2 | 摘要/时间线/知识库 | 摘要分桶隔离、跨端回忆过滤、知识库角色化 | C、D |
| **G** | — | 集成测试与验收 | 端到端隔离测试 + 全量回归 + 验收报告 | A-F |

### 0.2 里程碑（相对时间节点，不预设日期）

| 里程碑 | 内容 | 通过标准 |
| --- | --- | --- |
| **M0** | 门户 A 完成 | 迁移幂等通过；`data/aerie.db` 7 表含 persona_id 列 |
| **M1** | 门户 B 完成 | 新消息落库 persona_id=激活角色；不同角色 conversation_id 不同 |
| **M2** | 门户 C、D 完成 | 切换角色后历史/记忆检索仅见本角色 + NULL 共享数据 |
| **M3** | 门户 E、F 完成 | 前端切换重载历史；召回/时间线/知识库不串扰 |
| **M4** | 门户 G 完成 | 集成测试 + 全量回归 + 双角色实测（用户验收） |

### 0.3 多级审查机制 / Review Gates

| 级别 | 时机 | 内容 | 执行者 |
| --- | --- | --- | --- |
| **L1 自测** | 每个任务完成 | 该任务测试通过、语法校验、无副作用 | 负责子代理 |
| **L2 门户审查** | 每个门户完成 | 门户内全部任务 L1 通过；模块级回归；代码走查（命名/幂等/兼容） | 主代理复核 |
| **L3 验收审查** | M4 前 | 全量回归；双角色真实切换实测；数据目录零污染；回滚演练 | 主代理 + 用户 |

> [!warning] 审查红线（不得违反）
> - 严禁修改现有迁移的 `version`/`contract`/`checksum`（会抛 `MigrationChecksumError`）
> - 严禁向真实 `data/` 写入测试数据（测试一律用临时 `AERIE_DATA_DIR`）
> - 严禁改 `resolve_conversation_id` 的既有调用语义（存量 NULL 兼容候选必须保留）

### 0.4 并行开发策略 / Parallel Strategy

```
M0: [Agent-A 门户A]  （独立，先行）
M1: [Agent-B 门户B]  （依赖 A）
M2: [Agent-C 门户C]  ┐（依赖 A，可与 B 并行）
    [Agent-D 门户D]  ┘（依赖 A，可与 B/C 并行）
M3: [Agent-E 门户E]  （依赖 C）
    [Agent-F 门户F]  （依赖 C/D）
M4: [Agent-G 门户G]  （聚合，不可并行）
```

- **并联规则**：M2 阶段的 C、D 可并联（改不同文件）；M3 的 E、F 可并联。同一文件不被两个代理同时修改。
- **代理隔离**：门户 B 只碰 `conversation_repository.py` / `chat_request_repository.py` / `pipeline.py` / `chat_request_service.py` / `mobile_chat.py`；门户 C 只碰 `conversation_repository.py` 读方法 + `api_server.py` 历史端点 + `pipeline.py::_load_history`；门户 D 只碰 `memory/layers/*` + `context_builder.py`。冲突点（conversation_repository.py 同时被 B/C 改）用「B 写 C 读、B 先行于 C」解耦。

---

## 门户 A：P0 数据库迁移 013

> **Portal A · Database Migration 013**

- **开发目标**：7 张表新增 `persona_id TEXT DEFAULT NULL` 列 + 复合索引；迁移注册到 runner 链；幂等可重放。
- **技术要求**：沿用 `_add_column_if_missing`（[core/migrations/__init__.py](file:///e:/Agent_reply/core/migrations/__init__.py#L34-L47)）；版本号 `013_persona_scoped_dialogue_memory`（现有最大 012）；放入 `if migrations_enabled:` 分支（依赖 conversations/turns/messages 仅 flag-on 存在）。
- **负责人**：Agent-A；**时间节点**：M0。

### 任务 A-1：编写迁移函数（先写）

- [x] **Step 1**：在 [core/migrations/__init__.py](file:///e:/Agent_reply/core/migrations/__init__.py) 文件末尾追加迁移函数：

```python
def _apply_persona_scoped_dialogue_memory(conn: sqlite3.Connection) -> None:
    """Add role-scoped (persona_id) columns for dialogue & memory isolation.

    7 表加 persona_id（NULL=共享兼容）；所有列幂等（PRAGMA 检测）。
    依赖 conversations/turns/messages/requests 表存在（migration_framework_v1 on）。
    """
    _add_column_if_missing(conn, "chat_log", "persona_id", "TEXT DEFAULT NULL")
    _add_column_if_missing(conn, "conversations", "persona_id", "TEXT DEFAULT NULL")
    _add_column_if_missing(conn, "turns", "persona_id", "TEXT DEFAULT NULL")
    _add_column_if_missing(conn, "messages", "persona_id", "TEXT DEFAULT NULL")
    _add_column_if_missing(conn, "requests", "persona_id", "TEXT DEFAULT NULL")
    _add_column_if_missing(conn, "long_term_memory", "persona_id", "TEXT DEFAULT NULL")
    _add_column_if_missing(
        conn, "conversation_summary_buckets", "persona_id", "TEXT DEFAULT NULL"
    )
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_chat_log_persona "
        "ON chat_log(persona_id, user_id, created_at)"
    )
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_conversations_persona "
        "ON conversations(persona_id, actor_id, updated_at)"
    )
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_messages_persona "
        "ON messages(persona_id, conversation_id, sequence)"
    )
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_requests_persona "
        "ON requests(persona_id, actor_id, created_at)"
    )
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_ltm_persona "
        "ON long_term_memory(persona_id, user_id, importance)"
    )
    conn.execute(
        "CREATE INDEX IF NOT EXISTS idx_buckets_persona "
        "ON conversation_summary_buckets(persona_id, bucket_index DESC)"
    )


def persona_scoped_dialogue_memory_migrations() -> list[Migration]:
    contract = """013_persona_scoped_dialogue_memory
chat_log(persona_id) conversations(persona_id) turns(persona_id) messages(persona_id)
requests(persona_id) long_term_memory(persona_id) conversation_summary_buckets(persona_id)
role-scoped dialogue & memory isolation (NULL=shared compatible)
"""
    return [
        Migration(
            version="013_persona_scoped_dialogue_memory",
            checksum=hashlib.sha256(contract.encode("utf-8")).hexdigest(),
            apply=_apply_persona_scoped_dialogue_memory,
        )
    ]
```

- [x] **Step 2**：在 [core/database.py](file:///e:/Agent_reply/core/database.py#L17-L30) import 块加入 `persona_scoped_dialogue_memory_migrations`（按字母序插入现有导入列表）。
- [x] **Step 3**：在 [core/database.py](file:///e:/Agent_reply/core/database.py#L382) `runner.run(chat_log_trash_state_migrations())` 之后追加 `runner.run(persona_scoped_dialogue_memory_migrations())`。

### 任务 A-2：迁移测试

- [x] **Step 1**：新建 `tests/test_persona_isolation_migration.py`：

```python
"""Tests for migration 013_persona_scoped_dialogue_memory (role isolation)."""

import os
import sqlite3
import tempfile

from core.migrations import (
    Migration,
    MigrationRunner,
    persona_scoped_dialogue_memory_migrations,
)


def _fresh_db(path):
    conn = sqlite3.connect(path)
    conn.row_factory = sqlite3.Row
    return conn


def test_migration_adds_persona_columns():
    with tempfile.TemporaryDirectory() as td:
        db_path = os.path.join(td, "t.db")
        conn = _fresh_db(db_path)
        # 先建依赖表（simulate phase3）
        conn.executescript(
            """
            CREATE TABLE conversations (conversation_id TEXT PRIMARY KEY);
            CREATE TABLE turns (turn_id TEXT PRIMARY KEY);
            CREATE TABLE messages (message_id TEXT PRIMARY KEY);
            CREATE TABLE requests (request_id TEXT PRIMARY KEY);
            CREATE TABLE chat_log (id INTEGER PRIMARY KEY AUTOINCREMENT);
            CREATE TABLE long_term_memory (id TEXT PRIMARY KEY);
            CREATE TABLE conversation_summary_buckets (id INTEGER PRIMARY KEY AUTOINCREMENT);
            """
        )
        runner = MigrationRunner(conn)
        pending = runner.run(persona_scoped_dialogue_memory_migrations())
        assert pending == ["013_persona_scoped_dialogue_memory"]
        for table in (
            "chat_log", "conversations", "turns", "messages",
            "requests", "long_term_memory", "conversation_summary_buckets",
        ):
            cols = {r["name"] for r in conn.execute(f"PRAGMA table_info({table})")}
            assert "persona_id" in cols, f"{table} 缺 persona_id"
        conn.close()


def test_migration_idempotent():
    with tempfile.TemporaryDirectory() as td:
        db_path = os.path.join(td, "t.db")
        conn = _fresh_db(db_path)
        conn.executescript(
            """
            CREATE TABLE conversations (conversation_id TEXT PRIMARY KEY);
            CREATE TABLE turns (turn_id TEXT PRIMARY KEY);
            CREATE TABLE messages (message_id TEXT PRIMARY KEY);
            CREATE TABLE requests (request_id TEXT PRIMARY KEY);
            CREATE TABLE chat_log (id INTEGER PRIMARY KEY AUTOINCREMENT);
            CREATE TABLE long_term_memory (id TEXT PRIMARY KEY);
            CREATE TABLE conversation_summary_buckets (id INTEGER PRIMARY KEY AUTOINCREMENT);
            """
        )
        runner = MigrationRunner(conn)
        runner.run(persona_scoped_dialogue_memory_migrations())
        second = runner.run(persona_scoped_dialogue_memory_migrations())
        assert second == [], "重复执行不应再返回 pending"
        conn.close()
```

- [x] **Step 2**：运行并确认通过。

Run: `python -m pytest tests/test_persona_isolation_migration.py -q`（在 e:\Agent_reply 用 `.\.venv\Scripts\python.exe`）
Expected: `2 passed`

### 任务 A-3：L1 自测 + L2 门户审查

- [x] **Step 1**：运行迁移相关既有测试回归：`pytest tests/test_phase2_persona_source.py tests/test_phase4_migration.py -q`（不回归）。
- [x] **Step 2**：确认 `config/settings.yaml` 的 `migration_framework_v1: true`（当前为 true）；确认未触碰任何既有迁移的 checksum。
- [ ] **Step 3（L2）**：主代理复核 A-1/A-2 全部通过，标记 **M0 完成**。

> 完成：2026-08-13，测试 10 passed（013 迁移 2 passed + 回归 8 passed）

---

## 门户 B：P0 写链隔离

> **Portal B · Write Path Isolation**

- **开发目标**：所有消息落库路径写入 `persona_id = PersonaManager.get_active_id()`；`resolve_conversation_id` 哈希纳入 persona。
- **技术要求**：conversation_id 变化用 `_resolve_related_conversation_ids` 的 NULL 候选兼容存量；chat_log 直写 6 处（[pipeline.py](file:///e:/Agent_reply/core/pipeline.py#L711-L766) 主路径、L2053-2089 轻量、L3317-3353 批量）注入 persona_id。
- **负责人**：Agent-B；**时间节点**：M1。

### 任务 B-1：persona 获取辅助函数

- [x] **Step 1**：在 [core/conversation_repository.py](file:///e:/Agent_reply/core/conversation_repository.py) 顶部新增：

```python
def active_persona_id() -> str | None:
    """Return the currently-active persona id, or None on any failure.

    Role dimension for dialogue/memory isolation; NULL keeps legacy shared rows.
    """
    try:
        from core.persona_hub.persona_manager import get_persona_manager
        return get_persona_manager().get_active_id()
    except Exception:
        return None
```

### 任务 B-2：会话 ID 纳入 persona

- [x] **Step 1**：修改 `resolve_conversation_id` / `_legacy_conversation_id` / `_resolve_related_conversation_ids`，新增 `persona_id` 参数：

```python
def resolve_conversation_id(
    *,
    actor_id: str | None,
    channel: str | None,
    channel_account_id: str | None,
    user_id: int,
    persona_id: str | None = None,
) -> str:
    payload = "\x1f".join(
        (
            actor_id or "",
            channel or "",
            channel_account_id or "",
            str(user_id),
            persona_id or "",   # NULL == 共享（存量会话）
        )
    )
    digest = hashlib.sha256(payload.encode("utf-8")).hexdigest()
    return f"conv_{digest[:32]}"
```

`_legacy_conversation_id` 同构追加 `persona_id: str | None = None`；`_resolve_related_conversation_ids` 的候选集中加入 persona 变体（保留原 5 个候选 + persona 主候选 + persona legacy 候选，去重）。

> [!note] 兼容性说明
> 调用方不传 `persona_id` 时行为与现状完全一致（哈希输入不含 persona）。存量会话仍由原候选命中。

### 任务 B-3：persist_turn 与 pipeline 注入

- [x] **Step 1**：`persist_turn` 增加 `persona_id: str | None = None` 关键字参数；`ensure_conversation` 与 `_insert_message` 的 INSERT 列补 `persona_id`；`turns`/`requests` 写入补 `persona_id`。
- [x] **Step 2**：pipeline `_persist_canonical_turn`（L2702 调用处）传 `persona_id=active_persona_id()`；`_after_message_persisted` 兜底 `resolve_conversation_id(...)`（L2240-2249）补传 persona。
- [x] **Step 3**：`ChatRequestRepository.submit`（[chat_request_repository.py](file:///e:/Agent_reply/core/chat_request_repository.py#L69-L141)）在 `INSERT conversations/turns/requests` 处补 `persona_id`（从 `context.identity` 或传入）。
- [x] **Step 4**：chat_log 直写 6 处（pipeline L711/L757/L2053/L2080/L3317/L3342）的插入 dict 增加 `"persona_id": active_persona_id()`；确认 `_insert_chat_log_safe` 的列名容错逻辑无需改动（persona_id 列已由 013 迁移保证）。

### 任务 B-4：写链测试

- [x] **Step 1**：在 `tests/test_persona_isolation_migration.py` 追加：

```python
def test_conversation_id_differs_by_persona():
    from core.conversation_repository import resolve_conversation_id

    base = dict(actor_id="a", channel="desktop", channel_account_id="local", user_id=7)
    c0 = resolve_conversation_id(**base)
    c1 = resolve_conversation_id(**base, persona_id="yita_default")
    c2 = resolve_conversation_id(**base, persona_id="sena")
    assert c0 != c1 != c2
    assert resolve_conversation_id(**base, persona_id=None) == c0
```

- [x] **Step 2**：回归 `pytest tests/test_persona_generator.py tests/test_phase4_api.py -q`（确认会话 ID 默认行为不变）。

### 任务 B-5：L2 门户审查

- [ ] 主代理复核 B-1..B-4，标记 **M1 完成**。

---

## 门户 C：P1 读链隔离

> **Portal C · Read Path Isolation**

- **开发目标**：历史查询统一按 `persona_id = :active OR persona_id IS NULL` 过滤。
- **技术要求**：canonical 读（history_page / recent_turn_history / `_find_user_conversation_ids`）、legacy 读（`/api/chat/history`、`/api/chat/poll`）、pipeline `_load_history` 兜底查询。
- **负责人**：Agent-C；**时间节点**：M2。

### 任务 C-1：canonical 读链过滤

- [x] **Step 1**：`_find_user_conversation_ids`（[conversation_repository.py](file:///e:/Agent_reply/core/conversation_repository.py#L134-L187)）增加 `persona_id: str | None` 入参；在候选 ID 与 JOIN 关联处过滤 `persona_id = :active OR persona_id IS NULL`。
- [x] **Step 2**：`history_page`（L674-746）与 `recent_turn_history`（L636-672）增加 `persona_id` 参数并透传给 `_find_user_conversation_ids`；`_normalized_history_page`/`_merged_history_page` 的 SQL 按需补 `AND (persona_id = ? OR persona_id IS NULL)`。

### 任务 C-2：legacy 读链过滤

- [x] **Step 1**：`/api/chat/history`（[api_server.py](file:///e:/Agent_reply/core/api_server.py#L1589-L1630)）的 WHERE 追加 `AND (persona_id = ? OR persona_id IS NULL)`（无 persona 时保持原样）。
- [x] **Step 2**：`/api/chat/poll`（L1633-1668）同构处理。

### 任务 C-3：pipeline `_load_history` 兜底

- [x] **Step 1**：`_load_history`（[pipeline.py](file:///e:/Agent_reply/core/pipeline.py#L2431-L2469)）两条 chat_log 兜底 SQL 追加 persona 条件（从 `active_persona_id()` 取）。

### 任务 C-4：读链测试

- [x] **Step 1**：测试用例（隔离临时 DB）：写入 persona=A 与 NULL 的消息，断言查询 persona=A 时返回两者、persona=B 时仅返回 NULL 共享。

### 任务 C-5：L2 门户审查

- [ ] 主代理复核 C-1..C-4。

---

## 门户 D：P1 记忆链隔离

> **Portal D · Memory Chain Isolation**

- **开发目标**：记忆写入/检索/注入按 persona 隔离；修复 `LayeredMemorySyncAdapter.retrieve` 丢弃 actor 的问题（改传 persona）。
- **技术要求**：MemoryItem DTO 加 `persona_id`；`BaseMemoryLayer` 接口透传；`LongTermMemoryLayer` SQLite 用 013 的 persona_id 列 + ChromaDB metadata/where 加 persona；`context_builder._retrieve_memories` 传 persona。
- **负责人**：Agent-D；**时间节点**：M2。

### 任务 D-1：DTO 与接口

- [x] **Step 1**：`MemoryItem`（[memory/layers/base.py](file:///e:/Agent_reply/memory/layers/base.py#L46-L86)）增加 `persona_id: str | None = None` 字段（dataclass，default None 向后兼容）。
- [x] **Step 2**：`BaseMemoryLayer.store/retrieve` 及子类签名增加 `persona_id: str | None = None`。

### 任务 D-2：LongTermMemoryLayer

- [x] **Step 1**：`store` 的 ChromaDB metadata（[long_permanent.py](file:///e:/Agent_reply/memory/layers/long_permanent.py#L162-L173)）增加 `"persona_id": item.persona_id or ""`；SQLite INSERT 补 persona_id 列。
- [x] **Step 2**：`retrieve`（L259-343）ChromaDB `where` 增加 persona 过滤（`$or` 显式匹配「当前角色 + 空串共享」）；SQLite fallback WHERE 追加 `AND (persona_id = ? OR persona_id IS NULL)`，SQLite `get` 全量读取侧做角色权威过滤。

### 任务 D-3：sync_adapter 与 context_builder

- [x] **Step 1**：`LayeredMemorySyncAdapter.store`（[sync_adapter.py](file:///e:/Agent_reply/memory/layers/sync_adapter.py#L77-L105)）把 `persona_id` 写入 `MemoryItem`（`active_persona_id()` 或传入参数）；`retrieve`（L107-139）把 `persona_id` 传给 `_layered.search`。
- [x] **Step 2**：`context_builder._retrieve_memories`（[context_builder.py](file:///e:/Agent_reply/core/context_builder.py#L401-L423)）调 `retrieve(...)` 时传 `persona_id=active_persona_id()`（actor_id 参数保留兼容签名）。

### 任务 D-4：记忆链测试

- [x] **Step 1**：`tests/test_persona_isolation_memory.py` 隔离 DB 写入 persona=A/B 记忆，断言检索 A 时只出 A（+NULL 共享），B 时只出 B（+NULL）。

### 任务 D-5：L2 门户审查

- [ ] 主代理复核 D-1..D-4。

---

## 门户 E：P1 前端切换联动

> **Portal E · Frontend Switch Reload**

- **开发目标**：切换人设后聊天历史自动重载。
- **技术要求**：`persona:changed`（[persona-hub.js](file:///e:/Agent_reply/electron/src/renderer/js/persona-hub.js#L583)）→ chat.js 监听 → 清空会话缓存并重新 `_loadHistoryPage("initial")`。
- **负责人**：Agent-E；**时间节点**：M3。

### 任务 E-1：chat.js 联动

- [x] **Step 1**：在 chat.js 现有 `aerie:persona-updated` 监听（[chat.js](file:///e:/Agent_reply/electron/src/renderer/js/chat.js#L102-L124)）中，除刷新头像外，重置 `this._history` 与 `this._sinceId` 并调用 `this.loadHistory()`（已实现：首次只记录不重载，避免启动即刷历史；settings 头像事件无 id 不触发）：

```js
// persona 切换：后端已按激活角色过滤历史，重载当前会话（清游标）
const newId = detail.id !== undefined ? detail.id : null;
if (newId !== null && this._lastPersonaId !== undefined && this._lastPersonaId !== newId) {
  this._lastPersonaId = newId;
  this._history = { olderCursor: null, newerCursor: null, hasOlder: false, hasNewer: false };
  this._sinceId = 0;
  this.loadHistory();
}
this._lastPersonaId = newId;
```

- [x] **Step 2**：`node --check` 校验语法。

### 任务 E-2：L2 门户审查

- [ ] 主代理复核 E-1。

---

## 门户 F：P2 摘要 / 时间线 / 知识库

> **Portal F · Summaries / Timeline / Knowledge**

- **开发目标**：摘要分桶、跨端回忆、知识库按角色隔离。
- **技术要求**：分桶按 conversation_id 已天然隔离（conversation 已含 persona 维度）；`PersonaTimelineRepository.recent_events` 增加 persona 过滤；`knowledge_base` 增加 persona 归属。
- **负责人**：Agent-F；**时间节点**：M3。

### 任务 F-1：摘要分桶

- [x] 013 迁移已给 `conversation_summary_buckets` 加 persona_id；`upsert_bucket` INSERT/UPDATE 补写 `persona_id = active_persona_id()`（列存在检测，旧库回退原 SQL）；`latest_bucket`/`recent_buckets`/`bucket_refresh_due`/`completed_messages_after` 保持按 conversation_id 过滤（conversation_id 已含 persona 维度，天然角色隔离，已加注释）；仓库构造/使用处签名不变。

### 任务 F-2：跨端时间线

- [x] 新增迁移 `014_persona_timeline_persona`（persona_timeline 加 persona_id 列 + `idx_timeline_persona` 索引，注册在 013 之后）；`upsert_event` 写入 `persona_id = active_persona_id()`（列存在检测）；`recent_events` 在激活角色非 None 且列存在时追加 `(persona_id = ? OR persona_id IS NULL)` 过滤，旧库/无角色保持现状。

### 任务 F-3：知识库

- [x] `knowledge_base` 写入/查询未增加 persona_id 归属（保持现状）。
  > 决策：knowledge_base 不纳入本次角色隔离（用户/世界知识跨角色共享），如需要另立需求。

### 任务 F-4：L2 门户审查

- [ ] 主代理复核 F-1..F-3。

---

## 门户 G：集成测试与验收

> **Portal G · Integration & Acceptance**

- **开发目标**：端到端验证角色隔离 + 全量回归 + 验收报告。
- **负责人**：Agent-G；**时间节点**：M4。

### 任务 G-1：集成测试

- [x] 新建 `tests/test_persona_isolation_integration.py`：临时 `AERIE_DATA_DIR` 下创建 persona A/B，经 pipeline/persist 写入消息与记忆，切换后断言历史/记忆/召回隔离；断言 `data/personas/` 无测试残留。
  - **结果**：6 用例全过（迁移链 8 表 persona_id 列 / 写链 persona_id 落库 / 读链 A+NULL 隔离 / 纯 persona 会话读链 / 切换语义自动跟随 / 记忆跨角色零泄漏 / 真实 data 零污染），`pytest tests/test_persona_isolation_integration.py -q` 约 2.5s。G-1 复核补齐 2 项语义：① PRAGMA 断言 013/014 后 8 张目标表（chat_log/conversations/turns/messages/requests/long_term_memory/conversation_summary_buckets/persona_timeline）均含 persona_id 列；② 无激活角色时显式 None 查询退化为共享兜底（仅 NULL 共享会话可见，persona 专属不串台）。
  - **集成测试暴露并修复 2 个缺陷**：① `history_page` 在「纯 persona 会话（无 NULL 共享）」时用无 persona 维度的 `resolved_conversation_id` 查消息返回空 → 单会话页跟随 `related_ids[0]`，显式传入 `conversation_id` 时保持原语义；② Working/Transient 记忆层检索未按 persona 过滤，导致 `LayeredMemorySyncAdapter` 检索时跨角色泄漏 → 补 `persona_id` 过滤（与 long_term 层一致：本角色 + NULL 共享）。

### 任务 G-2：全量回归

- [x] 运行 `.\.venv\Scripts\python.exe -m pytest tests/ -q`，对比基线（既有环境性失败列表）确认无新增失败。
  - **结果**：136 个测试文件全部跑完，**修复后 failed_files=17**。主代理以「git stash 隔离 + 18 失败文件对照重跑」验证基线，发现两处被误判的**真实新增失败**并已修复：① `test_pipeline.py` 新增 4 失败（根因：legacy fallback SQL 按激活角色加 persona 过滤参数 + `_retrieve_memories` 丢失 `actor_id` 透传）→ 修复后恢复基线 **3 failed**；② `test_phase6_context_budget.py` 新增 1 失败（`_MemoryDouble.retrieve` 缺 `persona_id` 参数）→ 修复后 **4 passed**。修复后剩余 17 失败全部与基线一致：世界模拟时间×6（world_snapshot/world_domain/world_reality/world_runtime_lifecycle/proactive_care_governance/companion_state）、QQ 表情×3（qq_sticker）+ QQ 媒体×1、phase4×8（integration 6/pipeline 2）、`_rebuild_long_term_memory` schema、multi_channel_timeline FeatureFlags 配置、world_dashboard 插件/快照×2、test_context_builder 激活人设（真实 data 激活为 custom）×4、desktop_attachment_offline_acceptance。**无新增失败**。
  - **修复明细（G-2 回归暴露）**：`core/context_builder.py::_retrieve_memories` 由 `persona_id=active_persona_id()` 单参改为 `actor_id=actor_id` + `persona_id=active_persona_id()` 双维透传（actor 多端隔离与 persona 角色隔离正交，不可替代）；`tests/test_pipeline.py` fixture 显式 mock `active_persona_id` 返回 None（基础作用域断言不依赖激活人设，persona 过滤由隔离专项测试覆盖）；`tests/test_phase6_context_budget.py` 的 `_MemoryDouble` 补齐 `persona_id` 参数。

### 任务 G-3：双角色实测（用户验收）

- [ ] 启动 `start-dev.bat`：人设 A 对话若干条 → 切换人设 B → 确认设置页/聊天历史/称呼性别切换；再切回 A 确认历史还原。
  - **API 级实测已完成（主代理）**：启动后端（main.py，7890/7891 就绪）→ `/api/persona/hub/list` 确认激活 custom（塞纳）→ `/api/chat/history` 查询 → `/api/persona/hub/yita_default/activate` 切换 → 再次查询 → `/api/persona/hub/custom/activate` 恢复。激活/切换/恢复全部 `status=ok`，历史查询按激活角色过滤无报错，恢复后 active_id 还原为 custom。**真实 GUI 双角色对话切换实测留待用户**（G-3 最终门）。
  - **用户反馈修复（第二轮）**：用户在 GUI 实测反馈"名字改了但内容/历史/头像未真正分离"。根因定位 + 修复：
    - **① 历史未分离（前端）**：`aerie:persona-updated` 切换事件仅重置游标后 `loadHistory()`，**未清空旧角色已渲染的 DOM 消息**，新角色历史与旧消息混排。修复：chat-store 新增 `clear()`（整体清空消息/去重表/请求态）；chat.js 切换人设时先 `_clearMessagesDom()` + `_store.clear()` + 清 `_requests/_clientToRequest/_pendingUserBubbles/_notifiedRequestIds`，再重置游标重载当前角色历史。
    - **② 头像未角色绑定（前端缓存）**：localStorage 头像 key 为全局 `aerie.persona.avatar`，切换角色读串。修复：`_readLocalAvatar/_writeLocalAvatar` 支持 persona 维度分区 key（`aerie.persona.<pid>.avatar`），旧全局 key 一次性迁移归属首个读取角色并删除；`_personaCache` 记录 `_personaId`；`_loadPersona` 在新角色无头像时清空（`avatar_dataurl` 仅同角色才沿用缓存）。
    - **③ 头像未角色绑定（后端展示链路）**：`/api/persona/hub/list` 与 `/api/persona/hub/{id}` 不返回头像，人设中心列表/编辑器永远显示首字母占位符。修复：api_server 新增 `_persona_avatar_dataurl(persona_id)` 按角色读取分区头像，list/get 均返回 `avatar_dataurl`。
    - **④ 设置页头像显示**：`loadPersona` 用相对路径 `avatar_url`（Electron file:// 下 404）。修复：优先 `avatar_dataurl`；上传成功后 `_writeLocalAvatar` 补 persona 维度。
  - **验证**：`tests/test_persona_isolation_*.py` 16 passed；`test_persona_generator/three_view` 48 passed；node chat-store/render/queue 50 passed；冒烟脚本（临时数据目录）实测双角色头像分区存储/名字跟随/历史互不可见 `SMOKE_OK`。
  - **用户反馈修复（第三轮）**：GUI 实测再反馈三处角色归属问题，全部定位并修复：
    - **① 头像上传无反应 + 需重启生效 + 默认头像雷同**：人设中心编辑器 `#persona-editor-avatar-upload` 只有 click 触发文件选择、**缺 change 监听**（选文件后无任何动作）→ 补监听 + 实现**方形裁切弹窗**（拖动定位 / 右下角手柄缩放 / canvas 512×512 输出）+ 按当前编辑角色上传（`/api/persona/avatar?persona_id=` 支持任意角色，无需激活）；上传后立即刷新人设列表 + 编辑器头像 + `aerie:persona-updated{persona_id,avatar_dataurl}` 事件（聊天窗口只采纳激活角色的头像）。默认占位头像按角色 id 确定性取色，两个无头像角色不再看起来一模一样。
    - **② 图片产出被两个角色同时"发"**：生图消息写入 chat_log **不带 persona_id → NULL 共享行**，两个角色历史都显示。修复：候选创建处（主动发图 loop + 聊天要图 local_send）携带 `persona_id`，`_candidate_from_event` 透传，投递端（QQ + local_chat）chat_log 补写 `persona_id`，图片归属产出时的激活角色。
    - **③ admin 后台记录混乱（塞纳的消息显示成伊塔）**：admin-window.js **把 assistant 消息硬编码显示为"伊塔"**。修复：`AdminService.list_conversations/list_messages` 返回 `persona_id` + `persona_name`（经 persona_manager 解析），admin 前端会话标题与消息头按归属角色标注，NULL 共享显示 AI 兜底。
  - **验证（第三轮）**：persona 隔离 16 passed + admin/image/pipeline 相关 78 passed + node 50 passed；冒烟实测 admin 双角色会话/消息归属正确、图片候选 persona 透传正确。
  - **记忆分层复核 + 全量同类坑扫描（第四轮）**：确认记忆四层（瞬时/工作/长期/永久）+ sync 适配器均带 persona_id（写入取激活角色，检索本角色 + NULL 共享），并系统性扫描全部「该带 persona 却没带」的路径，修复 6 处：
    - **① 主动推送文字**：`_dispatch_push` 写 chat_log 缺 persona_id → NULL 共享行两个角色都看到 → 补 `_active_persona_id()`
    - **② identity_seed 播种**：async `_layered_memory.store` 未传 persona → 伊塔身份事实变共享行，塞纳记忆召回会当自己的 → 显式归属 `yita_default`
    - **③ 发图 EVENT 记忆**：`_persist_image_event` 未传 persona → "我发了一张照片"跨角色召回 → 补 persona（与 chat_log 归属一致）
    - **④ 显式回忆 EVENT 召回**：`list_by_user` 全链（sync_adapter → layered → transient/working/long_term/permanent 四层）缺 persona 过滤 → `_recall_event_memories` 会召回跨角色发图事件 → 全链补 `persona_id` 参数（SQL `AND (persona_id=? OR persona_id IS NULL)` / 内存层 Python 过滤）
    - **⑤ 主动推送续接素材**：`_recent_dialogue_text` 拉对话未按角色过滤 → 塞纳的推送拿伊塔对话当素材 → 补角色过滤
    - **⑥ admin 记忆列表**：`/api/admin/memory` 与 `get_memory` 返回 `persona_name` 标注，admin 记忆视图按归属角色显示（不再只能靠猜）
  - **验证（第四轮）**：隔离 16 + 记忆/归档/admin/phase6/phase14 59 passed；冒烟实测 `list_by_user` 角色过滤、发图 EVENT 与 identity_seed 归属正确 `SMOKE_OK`。**刻意保持共享**：知识库（用户级知识，计划 F 门户明确不隔离）；admin 系统级视图（管理视角全量）。

### 任务 G-4：L3 验收审查

- [ ] 主代理 + 用户复核 G-1..G-3；回滚演练（临时关闭迁移、确认删列后恢复共享态）；产出验收报告并挂接 [[09_当前状态]]。
  - 注：**回滚演练已由子代理完成**——临时脚本 `tools/_verify_rollback.py`（验证后已删除）：临时 DB 应用 013/014 → 断言 8 表 persona_id 列 → 写入存量共享+persona 行 → DROP 013/014 索引 → PRAGMA 检测列 → 清空 persona 归属 → 物理 DROP COLUMN → 核心断言「回滚后存量行 persona_id 全部 NULL 且原数据完整（行数与内容不变）」通过（ROLLBACK-DRILL: PASS）。
  - **验收报告已挂接 [[09_当前状态]]**（角色级隔离状态更新为"已实施，G 验收中"）；全量回归（G-2）17 failed 与基线一致、集成测试 6 用例全过、零污染、API 级双角色实测通过。**剩余：用户 GUI 双角色验收（G-3 最终门）→ 主代理复核确认后勾选完成**。

---

## 审查与风险清单 / Review & Risk Checklist

> [!todo] 多级审查清单（开发全周期勾选）
- [x] 每个任务：L1 自测（测试通过 + 语法校验）
- [x] 每个门户：L2 门户审查（模块回归 + 代码走查）〔门户 G 的模块回归 + 集成缺陷走查已由子代理完成；主代理最终复核见 G-4〕
- [ ] M4 前：L3 验收审查（全量回归 + 双角色实测 + 零污染 + 回滚演练）〔全量回归（G-2，修复后 17 failed 与基线一致）/ 零污染 / 回滚演练已完成；双角色真实切换实测（G-3）待主代理 + 用户执行〕
- [x] 全程：不修改既有迁移 checksum；不写真实 data/；不破坏 NULL 兼容

| 风险 | 缓解（对应任务） |
| --- | --- |
| conversation_id 变化导致存量定位失败 | B-2 保留 NULL 候选；C-1 候选过滤 |
| 记忆双源（NULL 共享 + 新 persona）重复 | D-2 `$or` 过滤 + 按 (content, persona_id) 去重 |
| ChromaDB 空值过滤语义 | D-2 NULL 用空串占位 + `$or` 显式匹配 |
| 前端切换丢历史 | E-1 重置游标 + 重载 |
| 迁移失败破坏启动 | A 幂等 + ledger；回滚删列 |

## 相关链接 / Related

- [[plans/角色级对话与记忆隔离技术方案|技术方案（D1-D6 决策）]]
- [[modules/PersonaGenerator|人设 AI 智能生成器]]
- [[modules/TopicTracking|话题追踪]]
- [[09_当前状态|当前状态]]
