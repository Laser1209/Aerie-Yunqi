---
title: 数据迁移核对
kind: control
tags: [aerie, migration, acceptance]
---
# 数据迁移核对
- [x] backup、dry-run、checksum
- [x] 重复运行幂等，cursor 中断续跑
- [x] 四表与 chat_log 的记录、附件、角色、Channel 守恒
- [x] QQ/local 分开回填，未知来源保持 `NULL`，不猜测规范身份
- [x] 旧 uploads 只读保留，backfill 后才允许 GC

> [!warning] 门槛
> 任一守恒项失败时停止后续批次。

## Phase 03 当前批次
- [x] Migration Ledger 版本 `004_conversation_model` 已建立并由固定 checksum 测试防止已发布迁移漂移。
- [x] 独立版本 `005_conversation_backfill` 已建立，负责兼容补列与历史回填；缺少 `chat_log` 时显式失败而非伪完成。
- [x] `conversations`、`turns`、`messages`、`requests` 四表骨架已由失败测试驱动创建。
- [x] 合成 legacy 数据验证记录数、正文、附件、角色顺序、Actor、Channel 与 Channel Account 守恒。
- [x] 连续 assistant 分段归入同一 `response_group_id` 并按 `sequence` 保序。
- [x] QQ 与 Desktop 为同一 Actor 生成不同短期 Conversation。
- [x] 未知 Actor/Channel 保持 `NULL`，不由 `user_id` 猜测规范身份。
- [x] 重复运行回填时 `messages.legacy_chat_log_id` 防止重复 Message；第二次插入数为 0。
- [x] 最小旧库缺少附件和身份列时，005 会先幂等补可空列再回填。
- [x] 自动化 live-path 合同已验证：FULL/BASIC 双写完整 Turn；规范历史按完整 Turn 读取；读取失败回退 legacy；Flag 关闭时 Pipeline 绕过规范读写并保持旧 SQL 合同。
- [x] live Turn 写入使用同一 SQLite SAVEPOINT；任一助手 Message 写失败时 Conversation、Turn、Request 与 Message 全部回滚，无孤立规范记录。
- [x] 已用 SQLite 在线备份 API 对默认生产库生成一致性快照；源库保持只读，主文件 SHA-256 前后未变化。
- [x] 已在生产数据独立 rehearsal 副本执行 dry-run：005 被识别为 pending，规范四表与 005 Ledger 行均保持零写入。
- [x] 副本迁移守恒：`chat_log=1754`、`messages=1754`、Conversation=4、Turn/Request=299；重复 legacy id=0；角色、正文、附件、Actor、Channel、Channel Account 的脱敏有序摘要一致；二次运行计数不变；quick check 为 ok。
- [x] 005 已实现每批 500 行的 cursor 回填：批次成功后写 Ledger cursor，失败保留最后成功位置，恢复后只补剩余行。
- [x] cursor 跨批次会从已回填 Message 恢复最后 Turn 与下一 sequence，user/assistant 边界不会拆成伪 Turn。
- [x] 已用临时真实 `Database` 构造真实 Companion，验证 `conversation_model_v1` 环境变量两态、Repository `enabled` 与 Pipeline 同实例注入一致；未启动后台任务或访问生产库。
- [x] 已在用户授权后完成生产运行态开关演练：真实 Companion 在 Flag true/false 两态均完成启动与停止，Repository 状态与 Pipeline 注入一致；未发送消息或调用模型。
- [x] 已通过 SQLite Backup API 从一致性备份实际恢复生产库，耗时 `0.047860s`；`quick_check=ok`，六张关键表数量与逐表脱敏摘要一致，数据损失为 0。
- [x] Phase 03 真实库、守恒、幂等、开关与回滚门槛已通过；最终 Python 回归 `353 passed, 6 warnings`。
