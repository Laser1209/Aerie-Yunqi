---
title: 发布与安全检查
kind: control
tags: [aerie, release, security]
---
# 发布与安全检查
> [!info] 门禁归属
> 本清单是最终发布门禁，不替代各 Phase 的阶段验收。阶段推进时只复核与当前 Phase 直接相关的条目；完整清单在发布前统一关闭。

- [x] Python 全测、Node 检查、Electron 启动烟测（2026-07-21：TTS provider hardening 后 `python -m pytest tests -q` → `538 passed, 6 warnings`；`node --test electron/tests/*.test.js` → `23 passed`；Electron 关键入口 `node --check` 通过；`npm run check:all` 在 `electron` 工作目录通过 emoji/forbidden/token 三段检查；Electron 以临时 `tmp/release-electron-smoke-20260721-033242`、`AERIE_DISABLE_QQ=true`、`AERIE_DISABLE_MODEL_CALLS=true` 启动，`/api/health` 返回临时 `data_path_id`、`components.backend=healthy`、QQ 离线导致预期 `degraded`，结束后 7890 无残留监听）
- [x] 迁移 dry-run、恢复、旧 API 观测（`91_数据迁移核对.md` 与 `92_回滚演练.md` 已无未勾项；2026-07-21 窄门禁 `tests/test_phase3_conversation_model.py tests/test_phase4_migration.py tests/test_phase4_api.py tests/test_phase4_integration.py::test_task12_flag_rollback_matrix_reenables_queue_without_data_loss tests/test_phase5_event_stream.py ...` → `84 passed, 4 warnings`，覆盖 dry-run 零写入、幂等、部分应用恢复、旧 API/Flag-off legacy 观测）
- [x] Renderer IPC 白名单且无任意 Sidecar 网络能力（Phase 11/15 目标测试随 84 passed 复验；`preload.js` 仅暴露 `worldDashboard` 专用 IPC，`main.js` 无 `world-dashboard:raw`，CapabilityBroker 拒绝 `qq.credential.read`/`shell.execute`，dashboard approval 仅上传 sanitized 字段）
- [x] 图片魔数/MIME/尺寸/EXIF/路径穿越检查（`tests/test_upload.py` 与 `tests/test_phase10_image_workflow.py` 随 84 passed/538 passed 复验；覆盖 flag-off legacy、魔数拒绝、pixel bomb、EXIF transpose 后重编码、sha256 去重、owner/audit、路径穿越拒绝、prompt/vision 审核与失败脱敏、引用保护和 GC；2026-07-21 新增显式图片/视觉 OpenAI-compatible provider 接线测试，未配置时保持无外呼 stub，配置时可返回 `image_bytes_b64`/answer 并接入资产持久化）
- [x] 语音/TTS Provider 显式配置边界（2026-07-21：`Brain.speak_text()` 在显式 `AERIE_TTS_*`/`OPENAI_TTS_*` 配置存在时调用 OpenAI-compatible `/audio/speech` 并返回 base64 音频字节；未显式配置时保持 `voice_tts` stub 且不外呼；`tests/test_brain_provider_routing.py` 随完整 `538 passed` 复验）
- [x] 日志、Fixture、截图、Evidence 脱敏（Phase 10/11/13/15 测试随 84 passed 复验 secret redaction；Electron smoke 使用临时 DB/data/log、禁用 QQ/模型、未采集截图、未连接真实外部通道；`python tools/scan_provider_key_patterns.py` 覆盖 `documents/logs/tmp/config/core/electron/src/tools/tests` 共 765 个文本文件，结果 `PROVIDER_KEY_SCAN_OK`）
- [ ] 疑似凭据未进入文档、提交、历史或日志
  - [!] 当前工作区/日志扫描通过，但 `python tools/scan_provider_key_patterns.py --history` 以脱敏摘要复现旧提交 `90e8a0466dae`/`68b295a39519` 的 `.env.example` 与旧规划文档 diff 曾包含 `SILICONFLOW_API_KEY=`、`DEEPSEEK_API_KEY=` 的 `sk-...` 形态值；另有 3 个 Git pickaxe 未复核候选提交，疑似 SVG/path-data 正则误报。此项需用户确认是否执行密钥轮换与 git history 清洗，不能由本批擅自标记完成。
- [x] Feature Flag 与回滚步骤可执行（`05_Feature_Flag与回滚矩阵.md`、`91_数据迁移核对.md`、`92_回滚演练.md` 均无未勾项；2026-07-21 Electron smoke 在迁移/身份/persona/conversation/queue/stream/context/image/world 相关 Flag 显式开启下启动成功；84 passed 窄门禁复验 migration flag、queue rollback、stream legacy、image flag-off、world/dashboard flag-off/降级路径）
